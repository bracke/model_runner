# What the published performance figures were measured against, earlier.
#
# The older half of docs/measured-figures.txt, moved when that file passed
# the megabyte a file in this repository may be. Nothing here is read by
# the suite; the fingerprint groups all stayed behind. It is kept because
# a measurement that was worth recording is worth keeping, and several of
# these entries are the reason a later one was not attempted twice.

# 2026-08-17, the shape behind the sweep's totals, which it now prints: nine
# architectures, fifteen formats and five shapes, with 864 of the 24939
# sequences run on a device. A total says nothing about what it covers, and
# what it covers is the whole of the claim the number is quoted for.
#
# The gate is bounded as a whole as well as stage by stage -- 1500 s against
# the twelve minutes it takes -- because four stages each inside their own
# bound still add to a run nobody notices growing, which is exactly how half
# an hour became an hour.

# 2026-08-17, what the reference's remaining time is. With one load a
# fixture, its 397 s divides into 274.7 s decoding each fixture into
# binary64 and 122.7 s computing with it -- so decoding is sixty-nine per
# cent of the reference and about forty-five per cent of the whole sweep,
# and it is already one pass a fixture. That is the floor of having a second
# implementation in a wider format, and it is where it should be rather than
# somewhere nobody looked.
#
# The gate holds each stage to roughly twice what it took here and fails
# when one exceeds it -- repository checks 30 s, conformance 1250 s,
# fixtures 200 s, fuzzing 10 s. Twice, because a busy machine takes half
# again as long and that is not news, while a stage that doubles has had
# something added to it. The gate went from half an hour to an hour once and
# nothing said so until somebody timed it by hand.

# 2026-08-17, the sweep loaded the reference four times a fixture. Splitting
# its cost by sequence is what found it: the four sequences cost 281, 289,
# 310 and 333 seconds across a sweep though they are one, two, five and eight
# tokens long. Costs that flat under an eightfold difference in work are not
# the work -- they are the load, which is every matrix of the fixture decoded
# into binary64, done once for each sequence asked about.
#
# One load a fixture now, running all four. The sweep went from 1431 s to
# 599 s, and the reference within it from 1213 s to 386 s, on the same
# figures to the digit: 24939 sequences, none outside tolerance, none
# unlearned. The gate is about eleven minutes where it was twenty-five.
#
# The parallelism I had written down as the next thing is not needed for
# this, and would have bought a quarter of what a second glance at the
# measurement did.

# 2026-08-17, where the sweep's twenty-four minutes go, which it now says:
#
#   fixtures built          59.9 s    4 per cent
#   the reference         1206.9 s   85 per cent
#   the engine             ~157   s   11 per cent
#
# The independent implementation is eighty-five per cent of the sweep, and
# the sweep is ninety-four per cent of the gate: four fifths of every gate
# run is one binary64 transformer computing what a binary32 one has already
# computed. That is the price of having a second implementation and it is
# the point of having one, so it is not waste -- but anybody who sets out to
# make the gate faster should know they are being asked to make the
# reference faster, and nothing else will matter.
#
# It already runs once a fixture rather than once a comparison: caching that
# took the sweep from nine minutes to two when it was written. What is left
# is four sequences a fixture, each independent of the others and each on one
# task.
#
# 2026-08-17, what the gate costs, stage by stage, as it now says itself:
#
#   repository checks     10.1 s
#   conformance         1425.1 s
#   fixtures              77.4 s
#   fuzzing                0.1 s
#
# Conformance is ninety-four per cent of it. That is worth knowing before
# anybody optimizes anything else: the fixture check's seventy-eight seconds
# looked like the thing that grew the gate, and it is five per cent of it.
#
# It also settles a question I had written down as work to do -- running the
# gate's halves in parallel. Overlapping the repository checks, the fixture
# check and both fuzzing campaigns with each other would save at most eighty-
# seven seconds of a twenty-five minute run, and they contend for the device
# besides. The only parallelism worth having is inside the conformance sweep,
# which is a different piece of work with a different argument.
#
# 2026-08-17, what the fixture check costs the gate. 78.3 s, the median of
# three on the host above: 76.30, 78.28 and 78.35, of which about 67 s is
# user time. It moves 22195 tensors, evaluating a tiny model twice for each.
#
# It cost 28 s when it was written, three days ago, and grew for two reasons
# worth separating: gemma3 is built with six blocks now rather than two,
# which is a third of the tensors it moves; and every fixture is also loaded
# by the independent implementation once, to compare what the file holds
# against what that implementation asks for.
#
# Not a measured figure in the sense the others are -- nothing publishes it
# and no fingerprint covers it -- but a gate that grew from thirty minutes
# is a thing somebody should be able to find the cause of, and 78 s of it is
# here.

# 2026-08-18, gpt2 on a published file. Six prompt tokens and twelve
# generated: 0.253 s wall -- 0.045 s evaluating the prompt and 0.210 s
# generating -- and 1.28 s of processor time, loading in 0.235 s, digest
# 6260472a109d3651.
#
# model: gpt2 Q8_0 as mradermacher published it -- gpt2.Q8_0.gguf,
# 177669376 bytes, 149 tensors, 163 million parameters, context 1024.
#
# Against TinyLlama-1.1B Q8_0's 1.871 s for the same twelve tokens. Worth
# saying what that does and does not show: it is seven times faster and has
# about a seventh of the parameters, so what the figure measures is size. It
# is the first architecture here that does not rotate, and the rotation is
# too small a part of a token's work to see at this distance -- a figure
# that isolated it would have to hold the size fixed, which no pair of
# published models does.

# 2026-08-18, what a fixture has in common with a file somebody ships.
# `tests fixture-likeness --model PATH` folds layer indices together and
# compares tensor names both ways. Against the ten published files:
#
#   llama, phi2, phi3, falcon    every name on both sides
#   gpt2                         blk.*.ffn_norm.bias in the file, not in the
#                                fixture -- and not read by the engine
#   qwen2, qwen3, gemma,         output.weight written by the fixture and
#   gemma2, gemma3               carried by none of them: they tie the
#                                embedding, and no fixture here does
#
# The gpt2 line was a live defect, not a cosmetic difference. Every published
# gpt2 carries a shift beside its feed-forward normalization; the engine read
# every other layer-norm shift and not that one, and the feed-forward ran off
# a normalization that was neither centred nor shifted. Twenty-six thousand
# conformance sequences agreed with the reference throughout, because the
# reference did not read it either and the fixture did not write it. Three
# implementations sharing one wrong idea agree perfectly.
#
# The tied-embedding line is not a defect -- the engine falls back to the
# embedding when there is no output matrix, which is why those five models
# load and generate -- but it is a gap: no fixture here ties, so the fallback
# is exercised only by files a caller supplies.

# 2026-08-24, the weights stopped being copied.
#
# The file was mapped and the mapping served reads; the tensor section was
# then read into one arena, which is what "loading took 5.248 s" was on an
# eleven-gigabyte model, and what eleven gigabytes of resident memory was.
# The weights are the file's own pages now, and the arena is what a source
# that cannot be mapped still gets.
#
#   loading, TinyLlama-1.1B Q8_0   0.065 s against 0.55 s
#   twelve tokens                  1.182 s wall, 0.262 evaluating and 0.920
#                                  generating, 8.33 s processor
#
# Loading is what moved and generation is not, which is the whole shape of
# the change: the same arithmetic reads the same bytes, and what stopped
# happening is a copy of them.
#
# What it does to a model too large to be comfortable, Qwen3-30B-A3B at
# 10.5 GiB, twelve tokens at a context of 512 and fifteen threads:
#
#   cold, cache dropped first      15.84 s wall, peak resident 4.6 GB
#   warm                           6.71 s wall, peak resident 4.6 GB
#
# Against 38.684 s of loading alone when the pages were dropped and 5.248 s
# when they were not, and against an arena of 11 252 639 744 bytes that was
# resident whether it was read or not. The resident figure is the mixture
# showing through: a token routes to eight experts of a hundred and
# twenty-eight, so most of the file is never touched and never faulted in.
# A dense model of the same size would touch all of it.
#
# Two things this cannot have at once, and the second is the one that had to
# be chosen against. A device opened to read the weights where they lie
# imports the host's own pointer, and this driver will not import a file's
# pages: asked for both, the run read 0 matrices where they lie and copied
# 633 233 408 bytes to the device. So a caller who names --device-memory 0
# gets the weights read into memory the device will take, and everyone else
# gets the file's pages. Written down because it is a fact about a driver
# rather than about this program, and the next driver may not have it.
#
# The rest of the figures below were taken again because quantization.adb,
# llama.adb and the tensor layer are sources behind all six groups. Nothing
# in them moved: the loading column did.

# 2026-08-24, one unit compiled twice, and four formats that wanted it.
#
# Q5_0 and Q5_1 keep the fifth bit of each element at a varying place in a
# thirty-two bit word, so the shift amount varies with the element; IQ4_NL and
# IQ4_XS index a table of sixteen levels, which is a gather. Neither
# vectorizes on baseline x86-64. The README has said so for months beside a
# note that building for a host with those instructions measured slower
# everywhere else, and that note was right and was never quantified. It is
# now, and then acted on.
#
# Three readings a build, alternating, the two binaries built before any of
# them so that no compile ran between measurements, each started below a load
# of 0.65. Whole build at -march=x86-64-v3 against the baseline, ns an
# element:
#
#   iq4nl 1.36 -> 0.75    q2_k  0.70 -> 0.81    q4_k 0.37 -> 0.52
#   q5_0  1.02 -> 0.64    q8_0  0.38 -> 0.47    q6_k 0.37 -> 0.55
#   q5_1  1.06 -> 0.68    q5_k  0.40 -> 0.53    bf16 0.32 -> 0.47
#   iq4xs 0.90 -> 0.68    q3_k  0.47 -> 0.63    q4_0 0.31 -> 0.48
#                         q4_1  0.43 -> 0.59    f32  0.25 -> 0.43
#                         f16   0.55 -> 0.59
#
# Four faster and eleven slower, by between seven and forty-two per cent. So
# not a build switch: the decoders are a generic instantiated twice, once with
# the project's switches and once with -march=x86-64-v3 -ffp-contract=off, and
# the four are sent to the second where the host says it has the instructions.
#
# Compiling only the decode wide beats compiling everything wide, and by more
# than the eleven-format loss explains. Accumulate_Dot decodes a span into a
# buffer and then multiplies it by each vector, and the second half is shared
# by every format: wide, it is slower, and it was dragging the four down with
# it. Split, on the same quiet machine:
#
#   iq4nl 1.36 -> 0.63, 2.16x     q5_1  1.06 -> 0.57, 1.86x
#   q5_0  1.02 -> 0.54, 1.89x     iq4xs 0.90 -> 0.50, 1.80x
#
# Eleven of the other formats sit within three per cent of where they were.
# The twelfth is q4_1, which reads 0.43 before and 0.50 after, eleven per cent
# slower, and nothing here explains it. The first guess was the call across
# the new unit boundary, which q4_1 pays per thirty-two elements where a
# k-quant pays it per two hundred and fifty-six; the block loop was moved
# inside the instance to test that and the number did not move. What is left
# is the thing the 2026-08-15 entry above describes -- these row products move
# with the layout of the unit they are compiled in, by up to a factor of two,
# in both directions, with no decoder changed -- and eleven per cent is well
# inside that band. It is written down rather than smoothed over.
#
# Everything else this touched, re-measured on the same quiet machine because
# quantization.adb is a source behind five groups of figures:
#
#   kernels, ns an element  f32 0.24, bf16 0.31, q4_0 0.31, q8_0 0.37,
#                           q6_k 0.38, q4_k 0.38, q5_k 0.40, q3_k 0.48,
#                           iq4xs 0.48, q4_1 0.49, q5_0 0.53, f16 0.54,
#                           q5_1 0.57, iq4nl 0.61, q2_k 0.71
#   twelve tokens           1.182 s wall, 0.262 evaluating and 0.920
#                           generating, 8.33 s processor, loading 0.065 s
#   at fourteen threads     1.258 s wall, 16.43 s processor
#   shares, pinned, q8_0    2660 Me/s at one, 13339 at seven, 14541 at eight;
#                           thirty-two a pass 4878, 24869, 27199
#   reference against cpu   8.986 s wall against 0.570 s, 5.397 and 3.602
#                           against 0.270 and 0.299, same digest; at the
#                           kernel 2.4x q8_0, 2.3x q4_k, 3.2x f32
#   device, short           1.191 s cpu against 0.950 s, of which 0.253 s
#                           and 0.164 s evaluating the prompt and 0.937 s
#                           and 0.785 s generating; 8.49 s processor against
#                           0.24 s
#   device, 110 tokens      4.841 s cpu against 2.043 s, 30.89 s processor
#                           against 1.18 s
#   drafting                1.174 s, 1.439 s and 4.801 s, 24 proposed and 9
#                           accepted
#
# The twelve-token figure is 1.155 s against yesterday's 1.315 on the same
# model, which is Q8_0 and takes none of the wide decoders. That is the layout
# again, in the direction that flatters, and it is quoted because it is what
# the tool says rather than because anything here earned it.

# 2026-08-23, the device table taken again, after the device path changed.
#
# The two-row table under "The device backend", all four cells adjacent, each
# waiting for the machine to fall below 1.50 before it started, medians of
# three:
#
#   cpu, six-token prompt, twelve generated   1.351 s wall, 0.271 s
#                                             evaluating and 1.087 s
#                                             generating, 8.71 s processor,
#                                             load 0.38 to 0.70
#   device, the same                          1.165 s wall, 0.200 s and
#                                             0.903 s, 0.30 s processor,
#                                             load 0.70 to 0.72
#   cpu, 110-token prompt, nothing generated  5.405 s evaluating, 32.46 s
#                                             processor, load 0.72 to 2.40
#   device, the same                          2.057 s evaluating, 1.17 s
#                                             processor, load 1.47 to 1.43
#
# Both backends print the same digests, 5abff916 and cbf29ce4.
#
# Why it was taken again: the sources behind these figures changed. The bound
# on a product's size stopped being a number chosen here and became the
# device's own answer, which is the change the checklist noticed and asked
# for these to be re-measured -- and it was right to, because the device
# column moved. It did not move because of the change. The same four cells
# were taken this morning on the code before it and again this afternoon on
# the code after it, and then the change was stashed and the afternoon pair
# taken a third time on the code before it: the long device run reads 1.212 s
# in the morning and 2.05 to 2.09 s in the afternoon whichever code is
# running, while the processor column repeats to within a per cent all day.
# What moved is the device's own state after a day of being measured, on a
# fifteen-watt part that shares its power with the processor beside it.
#
# The afternoon pair is published because both its columns were taken in one
# sitting. The morning device figures -- 1.321 s short and 1.212 s long -- are
# recorded here rather than dropped, because a reader taking a third reading
# should have two to put it beside rather than one.

# 2026-08-23, the whole Speed section taken again in one sitting. Every figure
# the README publishes there was retaken on a host that is its own again --
# the virtual machine that took two thirds of a processor through the last two
# sittings is gone -- so the section can be read against itself for the first
# time since it acquired that caveat.
#
# The library was at the release profile, which is what the tests crate pins
# it to; the harness around it was measured at both profiles and the kernel
# table came out the same to a hundredth of a nanosecond either way, which
# settles a question this file had not asked before: what the profile of the
# harness does to these numbers is nothing, because the work is in the
# library.
#
#   twelve tokens, processor    1.315 s wall, 0.282 evaluating and 1.033
#                               generating, 8.51 s processor, load 0.23-0.91
#   the same at fourteen threads 1.278 s wall, 16.40 s processor
#   twelve tokens, device       1.321 s wall, 0.192 and 0.947, 0.32 s processor
#   110 tokens, prompt only     5.376 s processor path, 32.00 s processor;
#                               1.212 s on the device for 0.26 s of processor
#   four tokens, reference      9.605 s wall against 0.648 s, same digest
#   drafted, q2_k drafting q8_0 5.112 s, 24 proposed, 9 accepted
#   repack, q8_0 generation     1.014 s stored, 1.499 f32, 1.022 bf16
#   repack, q2_k generation     1.304 s stored, 1.363 f32, 0.955 bf16
#   batch sweep, 110 tokens     10.49 s at one, 5.05 s at 128
#   shares, pinned, q8_0        2501 Me/s at one, 13771 at eight
#   device residency            14.38, 3.07 and 1.65 tokens/s
#
# Three of these reversed a published reading rather than moving it. Fourteen
# threads now takes three per cent off the wall for ninety-three per cent more
# processor time, where the last sitting had it eleven per cent worse; the
# eighth share now pays where it used to cost six per cent; and repacking to
# f32 now costs time on both files where it used to pay on the two-bit one.
# All three have the same shape of cause: the kernels got fast enough that the
# run waits on memory, and every one of those three trades buys arithmetic
# with bytes.
#
# The device table stopped being a four-to-one win on the short run and became
# a dead heat, and nothing about the device moved. The processor side got
# three times faster since that table was taken. What the device still wins by
# is processor time -- 0.32 s against 8.51 -- and the prompt, where there is
# arithmetic enough to hide what a submission costs.
#
# The four row-product rows that swing with code layout swung back. q4_0 0.33,
# 0.59, 0.32; IQ4_NL 1.44, 0.59, 1.38; IQ4_XS 0.95, 0.48, 0.92; Q2_K 0.79,
# 1.01, 0.73. The 2026-08-15 entry above explains the first swing and no
# decoder has changed since, so it explains this one: these four are the
# gather-bound and shift-bound loops, and what they measure is the code and
# its layout together. Three runs at loads from 1.20 to 1.88, on two build
# profiles, agree to a hundredth of a nanosecond, so the reading is not the
# question.
#
# Two figures wanted a four-bit file of TinyLlama-1.1B that this machine did
# not have. One was fetched -- TheBloke's published Q4_K_M, 668 788 096 bytes,
# sha256 9fecc3b3cd76bba89d504f29b616eedf7da85b96540e490ca5824d3f7d2776a0,
# which replaces the locally requantized file the provenance page used to
# name -- and both were taken:
#
#   repack, q4_k_m generation   0.807 s stored, 1.107 f32, 0.835 bf16
#   four-bit against eight-bit  86.8 and 92.4 ms a token against 92.7 and
#                               89.3, alternating, two readings each
#
# Reading them needed something the old pair did not say. The four-bit file
# answers the short prompt in ten tokens and the eight-bit one in twelve, so
# a wall time compares two different amounts of work: 1.133 s against 1.400 s
# is the token count and not the format. The pair this replaces, 2.06 and
# 2.18 s against 2.06 and 2.26, was quoted as a wall time and had the same
# ten against twelve inside it with nothing saying so. The figures above are
# per token, and the repacking row carries its count.

# 2026-08-23, qwen3moe on a published file. Five prompt tokens and twelve
# generated: 6.665 s wall -- 1.715 s evaluating the prompt and 4.950 s
# generating -- and 41.68 s of processor time, digest 0721a909c5c304bb.
# Median of three at the default thread count, load 1.14 to 3.02.
#
# model: Qwen3-30B-A3B as published -- 11258610240 bytes, 579 tensors,
# 30 532 122 624 parameters of which about three billion run for a position,
# 128 experts of 768 with 8 of them chosen, context 40960, its tensors in
# Q2_K, Q3_K, Q4_K, Q6_K and F32.
#
# Beside TinyLlama-1.1B Q8_0, taken the same hour on the same machine by the
# same command: 1.306 s for the same twelve tokens. Five times the wall for
# about twenty-eight times the parameters, which is the mixture doing what a
# mixture is for -- what a token costs is the eight experts that run and not
# the hundred and twenty that do not. Against the 1.871 s this file publishes
# for that model the ratio is three and a half rather than five; that figure
# is an older engine, so the same-hour pair is the one to read.
#
# Loading is the part this figure does not carry, and for a file this size it
# is the part a caller notices. Eleven gigabytes cost 5.248 s to open with the
# file in the page cache and 38.684 s with its pages dropped from the cache
# first. The twelve tokens cost the same either way -- 6.458 s wall on the
# cold run, the same digest -- so what the cold read buys is entirely in the
# opening. A first run of the day is thirty-eight seconds of disk before it
# is anything else.
#
# And what `run` costs end to end, which is more than either: 15.6 s at the
# context the file declares and 12.1 s at 512, both warm and at fifteen
# threads. The difference is the session rather than the tokens -- 40960
# positions of key and value across 48 layers is the 8.4 GB `inspect` prints,
# allocated before the first token and not wanted by a caller who asked for
# twelve. The figure above is the tokens; this is what asking for them on
# this file costs.

# 2026-08-18, which architectures have been read from a file somebody else
# wrote. Every one of the ten the sweep crosses, validated with `tests
# external-model` -- it loads the file, generates, and checks that two runs
# agree and that the worker count does not change the answer:
#
#   llama   TinyLlama-1.1B-Chat Q8_0        201 tensors, deterministic
#   gpt2    gpt2 Q8_0 (mradermacher)        149 tensors, deterministic
#   qwen2   Qwen2-0.5B-Instruct Q8_0        290 tensors, deterministic
#   qwen3   Qwen3-0.6B Q8_0                 310 tensors, deterministic
#   gemma   gemma-2b-it Q4_K_M (lmstudio)   164 tensors, deterministic
#   gemma2  gemma-2-2b-it Q4_K_M            288 tensors, deterministic
#   gemma3  gemma-3-1b-it Q4_K_M (unsloth)  340 tensors, deterministic
#   phi2    phi-2 Q4_K_M (TheBloke)         325 tensors, deterministic
#   phi3    Phi-3-mini-4k-instruct q4       195 tensors, deterministic
#   falcon  falcon-7b-instruct Q4_K_M       196 tensors, deterministic
#
# The eleventh architecture, qwen3moe, had no published file behind it for as
# long as this list said so: the smallest published one is a thirty-billion
# parameter mixture. It has one now.
#
# 2026-08-23, qwen3moe read from a published file.
#
#   qwen3moe  Qwen3-30B-A3B (Q2_K/Q3_K/Q4_K/Q6_K mixed, 10.5 GiB)
#             579 tensors, 30 532 122 624 parameters, deterministic,
#             thread-stable, twelve tokens generated on the processor
#
# `fixture-likeness` agrees with it name for name -- fifteen tensors a layer
# published, fifteen written by the fixture here, none unwritten and none
# invented -- which is the check that found gpt2's missing normalization
# shift on a file nobody here had written. It found nothing to report here:
# the mixture keys, the router and the three stacked expert tensors are
# spelled as this engine reads them.
#
# It is still not crossed by the sweep -- it is qwen3 with its metadata under
# another prefix, compared against the reference on its own -- but what
# stands behind it is no longer a fixture alone. That distinction is worth
# keeping visible for the architectures that still have only a fixture: a
# fixture and an engine can agree about a model nobody ships, which is what
# gpt2's output bias was, and no amount of agreement between two
# implementations reading the same fixture will say so.
#
# What it costs is below. What the file did find was in the template rather
# than in the arithmetic, and then in the device path.
#
# 2026-08-23, the mixture on a device, which had never been tried. It was
# refused, and the refusal named a capability the device had: an invented
# bound of 268 million elements stood where the device's own answer should
# have been, and every model whose output projection is wider than that --
# 151936 by 2048 is 311 million -- came to it. With the bound taken from the
# device instead, the mixture runs there:
#
#   weights read where they lie   12 tokens, prompt 85.495 s, generating
#                                 450.396 s, 0.03 tokens a second, 4094 of
#                                 4096 matrices read where they lie, 255 MB
#                                 on the device, context 512
#
# The same text the processor gives, which is the point of running it at all.
# It is a slow figure and it is a figure: what it says is that the device path
# survives 128 experts and a 30-billion-parameter file, and what it costs to
# read every weight across the bus for every token. Copied instead of read
# where they lie, the same run is refused before it starts -- 11251795968
# bytes wanted against a limit of 8472029184 -- which is the honest refusal
# the memory plan was always able to give and which the invented bound was
# standing in front of.
#
# 2026-08-23, and the tool-calling loop closed on it. The template rendering
# a tool conversation byte for byte against jinja2 says the prompt is right;
# it does not say the model does anything with it. Offered one tool -- a
# weather function taking a city -- and asked for the weather in Aarhus, the
# model answered with a call and nothing else: get_weather {"city":
# "Aarhus"}, reported by name on standard error as the caller needs it to be.
# Handed its own reply back with the tool's answer as a turn of its own, it
# read all three fields out of that answer and wrote them as a sentence: 17
# degrees, light rain, wind 22. Asked something that needs no tool with the
# same tool still offered, it answered directly and called nothing, which is
# the other half of the same check and the half a mis-rendered tools block
# would fail by calling for everything.
#
# Three runs at fifteen threads, thinking turned off, 78 s, 110 s and 73 s
# wall. That is the first time the calls a model asks for and the answers
# handed back to it have been through a published mixture rather than a
# fixture or a template comparison.
#
# Then two tools at once -- the weather function beside one that converts a
# currency -- which is where a single call stops proving much. Asked for both
# in one question, the model asked for both in one turn: get_weather {"city":
# "Aarhus"} and convert_currency {"amount": 100, "from": "EUR", "to": "DKK"},
# two calls in one reply, both read out of it and both reported by name.
# Handed that reply back with two answers as two turns, in the order the calls
# were made, it used both and kept them apart -- seventeen degrees and light
# rain for the first, 746.5 kroner for the second, in one sentence each.
# Asked something only one of the two tools can answer, it called that one and
# left the other alone, with the arguments read off the question rather than
# copied from the example: {"amount": 250, "from": "EUR", "to": "DKK"}.
#
# That is the branch of the template with the most in it -- the loop over a
# turn's calls, loop.first deciding where a newline goes, and the two answers
# folded into one user turn -- exercised by the model it was written for
# rather than by a conversation assembled here. 146 s, 202 s and 141 s wall.
# Nothing needed changing for any of it.
# The engine loaded it, generated from it and agreed with itself; `run`
# refused it, because the template a current mixture ships is written in a
# larger subset than the one a current dense model ships. Five things were
# missing and are there now, and the figures for them are in the changelog
# rather than here, because a template renders or it does not and there is
# nothing to time.
#
# One refusal along the way was not a defect. A gemma-2 file reported
# MR-GGUF-0022, a tensor running past the end of the file, which is exactly
# what a published gpt2 had legitimately found hours earlier. The download was
# 225 of 1708 megabytes. Whole, the same file loads and generates. The reader
# was right both times, and the second time it was right about the file rather
# than about the engine.

# covers:" line before a group says which published figures it stands
# for, and the check prints it when that group fails. A group whose figures
# are not named is a group nobody can answer: the reader learns that something
# changed and is left to guess what to measure again. If you add a figure to
# the README, add it to a covers line, and if it depends on a source no group
# lists, list it.
#
# On a mismatch the check prints the fingerprint it computed. Paste that in
# after re-measuring -- not before.
#
# These groups have twice been moved without re-measuring, which the rule
# above allows only with a reason. Both reasons are recorded here, because a
# reason that is not written down is a re-measure that was skipped.
#
# Supports_Batched in Backend.CPU.Describe: Describe had no caller in the
# program at all, so a field in it was not on any path these figures measure.
# Checked by searching the tree for callers, not assumed.
#
# The backend format check in Llama.Resolve: this one does run, once per
# resolved tensor. It runs during Prepare, and neither figure this group
# covers includes Prepare. The twelve-token figure says so outright -- load
# is quoted separately, "because the two are worth separating" -- and the
# batch-size table times prompt evaluation. The change is outside the window
# both figures measure, by their own definitions, which is a better answer
# than a number taken on a machine that at the time was on the powersave
# governor with other work on it.
#
# An A-B build against the previous commit was attempted and abandoned: a git
# worktree cannot resolve this crate's relative path pins.
#
# The capability checks in Llama: the format and alignment checks run during
# Prepare, outside the window as above. The Supports_Batched check runs inside
# it, once per Evaluate_Batch call, so that one was measured rather than
# argued. A-B-A on the same tree, the 131-token prompt with one token
# generated, three runs each: with the check 7.23 7.57 7.60, without it 7.47
# 7.56 7.89, with it again 7.35 7.26 7.76. The whole spread is wider than any
# difference between the states, which is the answer. The published table is
# not contradicted by these: they include load and one generated token.
#
# The Selecting_Backend progress event in Prepare: one Publish per model
# load, outside the window as above. The Prompt_Rendered event is in the
# command layer, which no group here fingerprints.
#
# The memory accounting at session open: a handful of additions, once per
# session, and session open is inside the window the twelve-token figure
# spans. Measured, not argued. A-B-A on the same tree, three runs each:
# with it 8.77 8.89 8.65, without one of the charges 8.73 9.04 9.06, with it
# again 8.71 9.08 8.68. The spread is wider than any difference between the
# states. These runs are on the 131-token prompt and include load, so they
# are not the published figure and are not offered as one.
#
# The session phases: an assignment at each phase boundary, inside the
# window. Measured: with them 8.66 8.35 9.07, with Enter doing nothing 9.09
# 9.38 8.88, with them again 8.84 8.97 9.12. Two evaluator assignments were
# also removed, which is why the middle state is not the faster one.
#
# The replaced-file check in Prepare: one stat of the file, once per model
# load, outside the window as above.
#
# Clearing the token history on reset, and recording a conversion: both
# happen once per reset and once per converted tensor at load, outside the
# window as above.
#
# The rotary pairing choice: two conditionals in the rotary inner loop,
# which is as inside the window as anything gets. Measured: with the choice
# 8.27 9.00 9.09, with the interleaved indices written straight in 9.34 9.28
# 9.04, with the choice again 8.94 9.03 8.86. The spread is wider than any
# difference between the states.
#
# Re-indenting three files to clear compiler style warnings moved all three
# groups at once. Not measured, and not argued either: the release build's
# instructions were compared before and after, and every difference is an
# immediate constant in exception-raising code -- the line number a raise
# reports, moved because blank lines were removed above it. Nothing that
# computes anything changed, which is a stronger statement than a timing and
# cheaper to make.
#
#   objdump -d --no-show-raw-insn obj/release/<unit>.o
#
# Routing every matrix product through the chosen backend: one case per
# product, inside the window. Measured, three runs each: through the choice
# 8.65 9.00 9.14, straight to the pool 8.73 8.97 8.79, through the choice
# again 8.78 8.82 8.84. There are about two thousand products in that run
# and the spread between repeats is wider than the difference between the
# states.
#
# The Capability accessor, read once per request when the batch size is
# clamped: with it 8.59 8.71 8.72, without it 8.65 8.77 8.97, with it again
# 8.76 8.78 8.84.
#
# Anything touching these files that is not outside the measured window gets
# a re-measure like that one, or a comparison like this one.

# Re-measure the two groups below with `tests benchmark`, which takes the
# median of three rounds itself. The pinned figures need
# `taskset -c 0-7 tests benchmark` on this machine -- one processor per core,
# read from the thread_siblings_list files; the README says so and why.
#
# Repacking moved both groups, and both were re-measured rather than argued
# about: the repacking code sits in llama.adb, which the engine group lists,
# and it changes the model's own preparation. Taken again on the machine and
# model above, with --repack off, which is what those figures describe:
# twelve tokens 0.32 s evaluating and 1.11 s generating against 0.26 and
# 1.02 published, 9.3 s of processor time at seven workers against 9.3 and
# 16.7 at fourteen against 17.2; the reference backend 0.32/1.15 against
# 5.67/11.43, which is 11.6x against the 12.5x published, and 2.44x, 2.45x
# and 3.14x at the kernel against 2.3x, 2.4x and 3.1x. Every one of those is
# inside the ten per cent this machine varies by, so the published figures
# still describe this code and were left alone.
#
# Moved again for the repacking rework -- a second target format, a skip for
# matrices already in it, and the decoding handed to tasks -- and this one was
# re-measured rather than reasoned about, because it is too large a change to
# read one's way through. With repacking off: twelve tokens 0.32 s evaluating
# and 1.02 s generating against 0.26 and 1.02 published, 9.3 s of processor
# time at seven workers and 17.1 at fourteen against 9.3 and 17.2; the
# reference backend 0.30/1.15 against 5.68/11.39, which is 11.8x against the
# 12.5x published, and 2.35x, 2.38x and 2.96x at the kernel against 2.3x,
# 2.4x and 3.1x. All inside the ten per cent this machine varies by.
#
# Moved twice more for repacking: a progress line, and the release of the
# file's bytes once the copy holds them. Both additions are inside
# `if Repack then`, and neither group is measured with repacking on -- the
# branch is one boolean test at load on the measured path and nothing else.
# The diffs are ten lines and two lines, which is cheaper to read than a
# measurement is to take.
#
# The earlier note stands for the first of them: The whole
# change to llama.adb is two lines: a Publish inside `if Repack then`, which
# neither group's figures use -- both are measured with repacking off, where
# that branch is one boolean test at load and not on any path either figure
# times. The diff is two added lines and nothing else, which is cheaper to
# read than a measurement is to take, and this is the kind of change the rule
# above allows a reason for instead.
#
# 2026-08-24, attention reads a position's values where they lie, and the
# rotation computes an angle once instead of once a head. Two changes,
# neither of which moves a bit: the blend held one component still and
# walked the whole cache for it, once for each component of a head, where a
# position's values are contiguous and one pass over them serves sixty-four
# components at a time; and Apply_Rotary sat its head loop outside its pair
# loop, so a power, a cosine and a sine were computed for every pair of
# every head when they depend on the pair and the position alone. Thirty-two
# heads is thirty-two times the transcendental calls the rotation needs.
#
# The same additions in the same order in both cases, so the run's digest is
# unchanged -- 448c2ed68ec342ee over sixty-four tokens and 5abff916f9d83ca6
# over twelve, as before -- and the conformance sweep moved no logit. That
# is what licenses reading the difference as the program:
#
#   110-token prompt, processor    4.558 s against 5.128 s, 24.1 t/s from 21.5
#   64 tokens generated, processor 5.230 s against 5.834 s, 12.2 t/s from 11.0
#   110-token prompt, device       1.982 s against 2.033 s
#   64 tokens generated, device    3.780 s against 3.940 s
#
# Everything below was re-measured in one sitting on this date, back to back,
# each run waiting for the machine to fall below 1.50 before it started. The
# twelve-token figure was taken three times within it and read 1.266, 1.198
# and 1.149 s; the middle is published and the spread is quoted in the README,
# because twelve tokens is a tenth of a second of arithmetic a token and the
# first run of a sitting pays for weight pages the later ones find touched.
#
# The kernel and share-scaling tables come from a `tests benchmark` run of
# their own, started at a load of 0.49: the first attempt ran at 2.75, inside
# the sitting that had just raised it, and a figure taken there is not one
# this repository publishes.
#
# The row-product table as taken, nanoseconds an element: f32 0.26, q4_0 0.31,
# bf16 0.32, q8_0 0.38, q4_k 0.39, q6_k 0.39, q5_k 0.41, q3_k 0.48, q4_1 0.50,
# iq4xs 0.51, q5_0 0.54, f16 0.56, q5_1 0.58, iq4nl 0.63, q2_k 0.72. Every one
# of them is a hair slower than the reading before, uniformly, on kernels
# nothing in this change touches -- which is what a machine looks like when it
# is a little slower today, and is why the table is re-taken whole rather than
# corrected where it looks wrong.
#
# The share-scaling figures are from a second run of the same tool under
# `taskset -c 0-7`, started at a load of 0.86, because the pinned case is the
# one the README argues from. Pinned, q8_0 with one vector a pass reads 2540
# Me/s serially and 13966 at eight shares against 12764 at seven; batched
# thirty-two a pass, 4655 serially and 26221 at eight against 24176 at seven.
# Unpinned, at 0.49: 13440 at eight against 12607 at seven with one vector,
# and 25598 against 22982 batched -- so unpinned eight is above seven on both
# now, where it fell below on the narrow one before. Four bits against eight,
# pinned: 14097 against 13966 at eight shares with one vector, 26625 against
# 26221 with thirty-two, 2525 against 2540 serially.
#
# The reference backend against the cpu one, serial, from the same quiet run:
# 2.4x for q8_0, 2.3x for q4_k, 3.1x for f32.

# 2026-08-24, activations quantized to a byte, and what re-measuring for it
# found. The engine gained Model_Runner.Quantization.Integers and a second
# entry into Tensors, both reached only when a run asks for --arith int8.
# The default path gained one null test per worker's share and nothing else,
# and every group below carries a source that changed, so every one of them
# was taken again in one sitting on this date.
#
# What came back is the sitting rather than the program. Loads of 1.3 to 2.9
# against 0.16 to 2.7 for the reading these figures were published from,
# because twenty-five measurements back to back are themselves the load, and
# every figure two to ten per cent slower with every digest identical:
#
#   twelve tokens             1.236 s against 1.198 published
#   110-token prompt          5.040 s against 4.558
#   64 tokens generated       5.630 s against 5.230
#   110-token prompt, device  1.975 s against 1.982
#   64 generated, device      4.703 s against 3.780
#   reference, four tokens    5.739 s and 3.826 against 5.541 and 3.704
#   drafting, the three runs  1.335, 1.512 and 4.951 s against 1.149, 1.453
#                             and 4.819
#
# The published cells stand and this reading is written down beside them
# rather than replacing them, for a reason the file should state plainly: a
# figure is only worth replacing when what moved is the program, and what
# moved here is the machine. The device generating row moved most, from
# 3.780 to 4.703 s, and it is the row this file has already called the
# noisiest -- three sittings have now read 13.3, 16.2 and 16.9 tokens a
# second on code that did not change it.
#
# The share-scaling table is the exception and is replaced, because its own
# run was the quietest any of them has had: started at a load of 0.10 under
# `taskset -c 0-7`, against 0.86 for the reading before. Pinned, q8_0 with
# one vector a pass reads 2633 Me/s serially and 14352 at eight shares
# against 13095 at seven; batched thirty-two a pass, 4801 serially and 26959
# at eight against 24583 at seven. Four bits against eight: 14312 against
# 14352 at eight shares with one vector, 26560 against 26959 with thirty-two,
# 2652 against 2633 serially.
#
# What the new arithmetic costs and buys is measured under its own option and
# published in "Quantized activations": 2.210 s against 4.940 on the prompt
# and 2.922 against 5.642 generating, at a conformance bound of 5.0E-2
# relative and 5.0E-1 absolute with nothing outside it over 28311 sequences.
# Those are not figures of these groups -- they are figures of a mode none of
# these groups runs -- and they are named here so that the next person to
# re-measure knows there are two arithmetics to re-measure in.

# 2026-08-24, the token budget re-taken, in the arithmetic a run uses.
#
# The budget measured the floating-point path, because `tests benchmark`
# never asked for the other one -- so since the default changed it had been
# describing a run nobody makes. It sets the arithmetic itself now, and
# reports the other one beside it. Pinned, at a load of 0.14, generating:
#
#   feed forward, gate up down       21.467 ms   69.1 %
#   attention projections, q k v o    6.521 ms   21.0 %
#   the vocabulary projection         1.985 ms    6.4 %
#   activation and gate               0.670 ms    2.2 %
#   softmax over the vocabulary       0.175 ms    0.6 %
#   normalization, twice a layer      0.156 ms    0.5 %
#   rotation                          0.101 ms    0.3 %
#   normalization, once at the end    0.004 ms    0.0 %
#   what a token costs of these      31.078 ms
#
# The same at --arith f32 is 73.752 ms; reading a prompt at thirty-two a pass
# is 12.066 ms a token.
#
# What it is for is deciding what to do next, and it decides two things.
#
# The products are 96.5 per cent of what it measures, so the two remaining
# ideas about the rest are dead: the rotation is 0.101 ms since its angles
# were tabulated and the transcendental kernels are 0.845 ms together, so a
# wider instruction set for the exponential -- tried and lost once already --
# is worth about two per cent of a token even if it wins outright.
#
# And a generated token really costs 44.2 ms against the 31.078 accounted
# for. The missing third is attention over the cache, which this tool cannot
# reach: the three blend kernels are inside Model_Runner.Llama with no entry
# point. At the first reading of this budget that gap was about a ninth of a
# token. The products have got 2.4 times faster since and attention has not,
# so it is now the largest single thing left on the processor -- and it is
# the one part of a forward pass that still runs entirely on the calling
# task while the worker pool sits idle.
#
# The row-product and share-scaling tables come from this same run. The row
# product reads f32 0.26, q4_0 0.30, bf16 0.32, q8_0 0.38, q4_k 0.39, q6_k
# 0.39, q5_k 0.41, q4_1 0.48, q3_k 0.49, iq4xs 0.51, q5_0 0.53, f16 0.56,
# q5_1 0.58, iq4nl 0.63 and q2_k 0.71 nanoseconds an element. Pinned, q8_0
# with one vector a pass reads 2542 Me/s serially and 14137 at eight shares
# against 12779 at seven; batched thirty-two a pass, 4648 serially and 25913
# at eight against 24253 at seven. Four bits against eight: 13987 against
# 14137 at eight shares with one vector, 26700 against 25913 with thirty-two,
# 2553 against 2542 serially.

# 2026-08-24, the integer product built twice, and a plan that turned out to
# be wrong about how much work it needed.
#
# The plan for this was a hand-written kernel reaching VPDPBUSD through a
# machine code insertion, with a third instruction-set level and a new host
# question to gate it. What it needed was one switch clause. The kernel is
# written around a sixteen-bit multiply-add, which is an instruction the
# wider sets have more and wider lanes of, and the compiler finds it without
# being told anything except which machine it is compiling for:
#
#                                 110-token prompt   64 generated
#   the baseline                       1.476 s          2.672 s
#   -march=x86-64-v3                   1.297 s          2.402 s
#   -march=x86-64-v4, 256-bit wide     1.308 s          2.366 s
#
# v4 is level with v3 and excludes far more hardware, so v3 is what is built,
# and the host question it needs -- avx2 and bmi2 -- is the one this program
# already asks for the decoders. No new level, no new host body, no assembly.
#
# One source and two compilations of it, as Decoders is, with contraction off
# on the wider one so that the two answer bit for bit and a test can say so.
#
# Everything taken again in one sitting at loads of 0.85 to 2.5:
#
#   twelve tokens              0.525 s wall, 0.079 evaluating and 0.442
#                              generating, 3.09 s processor
#   the same at --arith f32    1.298 s wall, 8.72 s processor, back to back
#   fourteen threads           0.519 s wall, 5.68 s processor
#   110-token prompt           1.319 s wall, 8.03 s processor
#   64 tokens generated        2.447 s wall, 14.86 s processor
#   1419-token prompt         29.286 s wall, 197.85 s processor
#   110-token prompt, device   1.679 s wall, 1.04 s processor
#   64 generated, device       3.510 s wall, 0.84 s processor
#   reference, four tokens     cpu 0.088 s and 0.158 s against 5.731 s and
#                              3.779 s
#   drafting, the three runs   0.532 s, 1.668 s and 3.639 s, 24 proposed and
#                              7 accepted
#   batch sizes 1 to 128       4.062, 2.337, 1.742, 1.510, 1.382, 1.392,
#                              1.434 and 1.300 s on the processor
#   llama.cpp on the same file 377.69 and 39.92 tokens a second on the
#                              processor, 1666.53 and 56.40 on the device,
#                              762.61 and 39.97 with -ngl 0
#
# The device table, cell for cell: six-token prompt with twelve generated,
# cpu 0.532 s against device 1.025 s; evaluating the prompt 0.097 s against
# 0.189 s; generating 0.453 s against 0.769 s; processor time 3.11 s against
# 0.27 s; 110-token prompt cpu 1.384 s against device 1.679 s, processor time
# 8.15 s against 1.04 s.
#
# The row-product and share-scaling tables come from the pinned run of the
# same sitting, started at a load of 0.72. The row product reads f32 0.27,
# q4_0 0.31, bf16 0.32, q8_0 0.38, q4_k 0.39, q6_k 0.39, q5_k 0.41, q3_k
# 0.49, q4_1 0.50, iq4xs 0.51, q5_0 0.55, f16 0.57, q5_1 0.60, iq4nl 0.64 and
# q2_k 0.73 nanoseconds an element -- all a shade slower than the sitting
# before, uniformly, on kernels this change does not touch. Pinned, q8_0 with
# one vector a pass reads 2538 Me/s serially and 13761 at eight shares
# against 12798 at seven; batched thirty-two a pass, 4632 serially and 25616
# at eight against 24247 at seven. Four bits against eight: 13732 against
# 13761 at eight shares with one vector, 26613 against 25616 with thirty-two,
# 2535 against 2538 serially.
#
# The device's 110-token prompt is 1.679 s here, which is the open question
# the entry above describes and not a new one.

# 2026-08-25, the tile a product takes is chosen from the batch rather than
# fixed at four, so every group naming the kernel or the tensor package moved
# and all seven were taken again in one sitting, load under 1.20 before each
# run.
#
#   twelve tokens, seven workers    0.550 s wall, 3.10 s processor
#   the same at fourteen threads    0.527 s wall, 5.65 s processor
#   four tokens, reference          9.708 s wall against 0.545 s
#   six-token run                   cpu 0.545 s   device 0.556 s
#     evaluating the prompt         cpu 0.092 s   device 0.066 s
#     generating                    cpu 0.465 s   device 0.489 s
#     processor time                cpu 3.10 s    device 0.03 s
#   110-token prompt                cpu 1.284 s   device 0.589 s
#     processor time                cpu 7.64 s    device 0.12 s
#   sixty-four generated            cpu 2.528 s   device 2.744 s
#   batch sweep, 110 tokens         4.264 s at one, 1.208 s at 128
#   drafted, q2_k drafting q8_0     3.582 s, 24 proposed, 7 accepted
#   row dot, q8_0                   2511 Me/s, 0.40 ns an element
#   device ratios                   q8_0 0.56 one vector, 0.14 at eight,
#                                   0.043 at thirty-two; load 1.14 to 2.61
#   llama.cpp, processor            389.6 t/s prompt, 40.2 t/s generating
#   llama.cpp, device              1671.5 t/s prompt, 57.7 t/s generating
#   llama.cpp at -ngl 0             782.3 t/s
#
# The tile is eight where a pass carries more than one vector and four where
# it carries one, and what that is worth is three per cent of the prompt with
# generation level -- three rounds alternated, the prompt faster in every one.
#
# What the change is really about is that four had gone stale without
# anything noticing. It was measured -- 2.120 s at two, 1.948 at four, 2.122
# at eight -- when a prompt was read thirty-two vectors at a time. A tile
# reads the activation once and the activation is re-read once per tile, so
# what a larger tile saves grows with the batch, and when the default became
# a hundred and twenty-eight the answer moved: 1.379 at four, 1.351 at six,
# 1.281 at eight. Nothing asked the question again, because a constant
# carries no fingerprint. That is the same failure as a figure resting on a
# source no group names, one level down.
#
# 2026-08-25, later. Two changes, and a hole in this file that is worth more
# than either of them.
#
# The hole: no group named the integer kernel. Not one of the seven lists
# above held model_runner-quantization-integers.adb or any of its children,
# and that is the arithmetic every processor figure in this file is measured
# through -- it has been the default since --arith int8 became it. The kernel
# could be rewritten, and was, with nothing here asking for a re-measure.
# All seven groups name its six files now.
#
# That is the third time in two days: the default batch lived in a file no
# group named, the tile a product takes was a constant no fingerprint could
# cover, and now the kernel itself. The pattern is that a fingerprint follows
# what a figure is *computed by* and keeps missing what it is *decided by*,
# and the three of them together are the argument for reading these lists
# against the code rather than trusting that they grew right.
#
#   The sitting, load under 1.20 before every run:
#
#   twelve tokens, seven workers    0.565 s wall, 3.10 s processor
#   the same at fourteen threads    0.522 s wall, 5.52 s processor
#   four tokens, reference          9.733 s wall against 0.536 s
#   six-token run                   cpu 0.536 s   device 0.600 s
#     evaluating the prompt         cpu 0.082 s   device 0.073 s
#     generating                    cpu 0.454 s   device 0.527 s
#     processor time                cpu 3.10 s    device 0.05 s
#   110-token prompt                cpu 1.292 s   device 0.534 s
#     processor time                cpu 7.16 s    device 0.11 s
#   sixty-four generated            cpu 2.519 s   device 3.039 s
#   batch sweep, 110 tokens         4.303 s at one, 1.279 s at 128
#   drafted, q2_k drafting q8_0     3.769 s, 24 proposed, 7 accepted
#   row dot, q8_0                   2501 Me/s, 0.40 ns an element
#   device ratios                   q8_0 0.55 one vector, 0.14 at eight,
#                                   0.042 at thirty-two; load 1.10 to 1.72
#   llama.cpp, processor            384.8 t/s prompt, 40.1 t/s generating
#   llama.cpp, device              1672.3 t/s prompt, 57.5 t/s generating
#   llama.cpp at -ngl 0             773.9 t/s
#
#   The device generating row reads 3.039 s here against 2.744 in the sitting
#   before, which is eleven per cent where the alternated rounds that chose
#   the wider group put the cost at two. That row has read 13.3 to 24.2
#   tokens a second across a dozen sittings on code that did not change it,
#   and this file says so beside the table; the figure is what this sitting
#   measured and the three rounds are what the choice rests on.
#
# The two changes, both measured by alternating three rounds:
#
#   The block product goes through a machine code insertion. The eight
#   partial sums a sixteen-bit multiply-add leaves were reduced to a scalar
#   at every block -- about seven instructions of shuffling for three of
#   arithmetic -- and an ablation put the whole multiply-add step at 39 per
#   cent of a prompt at about a fifth of the rate this part can issue. The
#   insertion folds them to four, keeps those across a row and reduces once.
#   The prompt reads 1.280 s against 1.323, better in each of three rounds;
#   generating is level and the digest does not move. There are two
#   insertions, one per instruction set, arranged so a sum holds the same
#   four of a block's sixteen pairs either way: the test that both
#   compilations answer the same bits is what forced that, and it caught the
#   first version, which did not.
#
#   A device workgroup is 256 invocations rather than 64. Nothing had ever
#   varied it -- it was the wave width of the part this was written against.
#   The device prompt reads 0.547 s against 0.579, better in each of three
#   rounds; generating reads about two per cent slower, which is inside that
#   row's spread.
#
# Two more were measured and dropped. Rows a tile beyond eight buy nothing:
# 1.305 s at eight, 1.273 at twelve, 1.309 at sixteen, 1.316 at twenty-four,
# with the spread inside a setting as wide as the gaps between them -- so the
# activation traffic a larger tile saves has stopped being what the prompt
# waits on. And naming the next block of each row with a prefetch makes
# generating worse in each of three rounds, 2.591 s against 2.533, with the
# prompt a wash: the streams are sequential and the part already had them.
#
# 2026-08-25, the byte dot product. A third compilation of the integer
# kernel, for x86-64-v4, reaching VPDPBUSD through a machine code insertion.
# All seven groups name that kernel, so all seven were taken again.
#
#   twelve tokens, seven workers    0.514 s wall, 3.01 s processor
#   the same at fourteen threads    0.509 s wall, 5.35 s processor
#   four tokens, reference          9.644 s wall against 0.513 s
#   six-token run                   cpu 0.513 s   device 0.437 s
#     evaluating the prompt         cpu 0.074 s   device 0.051 s
#     generating                    cpu 0.428 s   device 0.385 s
#     processor time                cpu 3.04 s    device 0.03 s
#   110-token prompt                cpu 1.196 s   device 0.583 s
#     processor time                cpu 7.34 s    device 0.12 s
#   sixty-four generated            cpu 2.353 s   device 2.082 s
#   batch sweep, 110 tokens         4.183 s at one, 1.231 s at 128
#   drafted, q2_k drafting q8_0     3.548 s, 24 proposed, 7 accepted
#   row dot, q8_0                   0.39 ns an element
#   device ratios                   q8_0 0.41 one vector, 0.09 at eight,
#                                   0.038 at thirty-two; load 1.09 to 2.15
#   llama.cpp, processor            381.5 t/s prompt, 40.1 t/s generating
#   llama.cpp, device              1681.3 t/s prompt, 57.8 t/s generating
#   llama.cpp at -ngl 0             776.5 t/s
#
# What the instruction bought, alternated three rounds against the sixteen-bit
# path: the prompt 1.222 s against 1.271 and generating 2.354 against 2.496,
# better in every round of both.
#
# And what nearly buried it. VPDPBUSD is unsigned against signed, so the
# weight byte is biased by 128 and the bias taken back out with the
# activation block's own sum -- the Totals table, which this kernel is handed
# and does not otherwise read. Accumulated as a scalar read-modify-write in
# the innermost loop, that correction cost fifteen per cent of a prompt,
# which is more than the instruction saved and would have been recorded here
# as another negative. Built once a block as a vector holding the whole
# correction in its first lane, and added inside the insertion as one integer
# add, it is the figures above. Same instruction, same arithmetic, different
# place to put four bytes.
#
# A single reading taken straight after that fix said 1.289 s -- still losing.
# Three alternated rounds said 1.222. Every single reading this session that
# disagreed with a three-round median has been the wrong one.
#
# This is also the first change in the program whose answer depends on the
# host's instruction set. The two sixteen-bit compilations still agree bit for
# bit; the byte one groups a block's products differently into its lanes and
# is held to the sweep's five per cent instead, which is written up under
# "And a third, which does not answer the same bits".
#
# 2026-08-25, the block loop moved inside the insertion for a single
# vector. All seven groups name the kernel and were taken again.
#
#   twelve tokens, seven workers    0.470 s wall, 2.68 s processor
#   the same at fourteen threads    0.459 s wall, 4.74 s processor
#   four tokens, reference          9.743 s wall against 0.479 s
#   six-token run                   cpu 0.479 s   device 0.449 s
#     evaluating the prompt         cpu 0.081 s   device 0.062 s
#     generating                    cpu 0.396 s   device 0.387 s
#     processor time                cpu 2.69 s    device 0.03 s
#   110-token prompt                cpu 1.225 s   device 0.586 s
#     processor time                cpu 7.33 s    device 0.12 s
#   sixty-four generated            cpu 2.148 s   device 2.058 s
#   batch sweep, 110 tokens         3.728 s at one, 1.292 s at 128
#   drafted, q2_k drafting q8_0     3.549 s, 24 proposed, 7 accepted
#   row dot, q8_0                   0.40 ns an element
#   device ratios                   q8_0 0.40 one vector, 0.09 at eight,
#                                   0.037 at thirty-two; load 1.13 to 3.13
#   llama.cpp, processor            387.1 t/s prompt, 40.2 t/s generating
#   llama.cpp, device              1678.8 t/s prompt, 57.6 t/s generating
#   llama.cpp at -ngl 0             779.9 t/s
#
# What it bought, alternated three rounds: sixty-four generated in 2.187 s
# against 2.400, better in every round and steady to a hundredth. The prompt
# does not enter the path at all -- the condition is one vector -- and its
# readings across the same six runs were 1.192 to 1.300 s. That accidental
# control is the most useful number in this entry: it puts the noise floor on
# this machine at about five per cent, which is the size of several changes
# measured today, and is why single readings keep being wrong here.
#
# The shape, and why it is only for a generated token. A block at a time
# loads the accumulator, multiplies, and puts it back; a whole row inside one
# insertion keeps it in a register from the first block to the last. That is
# the load and the store gone and a fused multiply-add in place of a separate
# multiply and add -- about eleven instructions a block down to seven.
#
# It works here because a token multiplies one vector, so the accumulators
# are one a row: four registers. A prompt multiplies a hundred and
# twenty-eight, which is a thousand accumulator sets against sixteen
# registers, and holding them means re-reading the weights once per vector
# tile -- sixteen passes over a gigabyte -- unless the weights are packed
# into cache-sized panels first. That packing is the remaining work and is
# not here.
#
# 2026-08-25, two things measured and neither kept. Nothing here moved;
# this is recorded so the next reader does not build them again.
#
# A register-blocked tile for the prompt, four rows by four vectors.
#
# The shape a blocked matrix product has: sixteen accumulators living in
# registers from a row's first block to its last, the weights packed by block
# so a block's four rows are contiguous, the scale applied by an embedded
# broadcast, and the whole thing in the deepest compilation because sixteen
# accumulators want more than sixteen registers. It is written, it is
# correct -- the sweep passed with nothing outside tolerance -- and across
# three attempts it does not beat the kernel it replaces:
#
#   as first written          1.234 s against 1.254, spread 1.192 to 1.357
#   packing hoisted out of
#     the vector-group loop   1.243 s against 1.291, better in three rounds
#   scales gathered before
#     the innermost loop      1.299 s against 1.249, worse in two of three
#
# Against a noise floor of about five per cent, none of those is separated
# from nothing. Two real defects were found and fixed on the way -- the
# weights were being packed once per group of four vectors rather than once
# per chunk, thirty-two times over; and the innermost loop computed an index
# with a division in it -- and fixing them moved the figure inside the noise
# and no further.
#
# Why it cannot pay, which is the part worth keeping. Tiling amortises loads
# and nothing else. Per row, per vector, per block the work is a zero, a
# multiply-add, a widening and a fused multiply-add -- four vector
# instructions -- plus a scale multiply and a bias multiply-add in scalar.
# None of those six is shared between the tile's elements, because every one
# of them carries its own pair of scales. A tile of sixteen shares four
# weight loads and four activation loads across sixteen elements, and that is
# the whole of what it buys: about 5.3 multiply-accumulates an instruction
# against 4.6. Fifteen per cent of ceiling, and the packing and the gathering
# spend it.
#
# Which says something about the four-times gap to the other runtime that no
# amount of this will close. Its inner loop pays the same six operations per
# element -- the same instruction mix, the same scale multiply. At 387 tokens
# a second it is sustaining about 2.5 instructions a cycle a core; this
# program at 91 is sustaining about 0.7. The prompt gap is not a
# multiply-accumulates-per-instruction problem and cannot be fixed by
# arranging the arithmetic better. It is stalls, and finding them wants a
# profiler: perf_event_paranoid is 4 on this host, so nothing here has ever
# seen a cycle counter.
#
# And the device shader's decode, measured rather than repacked.
#
# Built with the eight-bit branch's decode replaced by a constant -- wrong
# answers, right shape -- the device prompt reads 0.415, 0.427 and 0.452 s
# against 0.573 with the real one. So unpacking the weights is about a
# quarter of what the shader spends, and a repack that removed all of it
# would take the device prompt from 191 tokens a second to about 258.
#
# That is worth having and it is not what it looks like. The bytes are
# already bytes; what costs is that the buffer is bound as words and every
# weight is extracted with a shift and a mask. The way to it is
# GL_EXT_shader_8bit_storage and a binding of the right type, not a repack of
# the model -- a repack that kept the byte count could not remove the
# extraction, and one that did not keep it would cost four times the device
# memory.
#
# 2026-08-25, the sitting that goes with the counter entry below. All seven
# groups name the kernel, so all seven were taken again.
#
#   twelve tokens, seven workers    0.480 s wall, 2.71 s processor
#   the same at fourteen threads    0.445 s wall, 4.75 s processor
#   four tokens, reference          9.729 s wall against 0.481 s
#   six-token run                   cpu 0.481 s   device 0.428 s
#     evaluating the prompt         cpu 0.086 s   device 0.051 s
#     generating                    cpu 0.397 s   device 0.378 s
#     processor time                cpu 2.70 s    device 0.04 s
#   110-token prompt                cpu 1.207 s   device 0.567 s
#     processor time                cpu 7.18 s    device 0.12 s
#   sixty-four generated            cpu 2.203 s   device 2.280 s
#   batch sweep, 110 tokens         3.863 s at one, 1.195 s at 128
#   drafted, q2_k drafting q8_0     3.674 s, 24 proposed, 7 accepted
#   row dot, q8_0                   0.40 ns an element
#   device ratios                   q8_0 0.36 one vector, 0.08 at eight,
#                                   0.036 at thirty-two; load 1.08 to 1.28
#   llama.cpp, processor            386.7 t/s prompt, 40.4 t/s generating
#   llama.cpp, device              1686.7 t/s prompt, 57.4 t/s generating
#   llama.cpp at -ngl 0             778.5 t/s
#
# The device generating row reads 2.280 s where the sitting before it read
# 2.001, on a shader that did not change between them. That row has read 13.3
# to 32.0 tokens a second across a dozen sittings and this file says so beside
# the table; the figure is what this sitting measured.
#
# 2026-08-25, a counter, at last. perf_event_paranoid was 4 on this machine
# for every figure in this file until now; lowered to 1, the 110-token
# prompt on the processor reads 96.3 thousand million instructions in 28.0
# thousand million cycles -- 3.43 a cycle -- with a 1.3 per cent cache miss
# rate and 1.9 per cent of cycles stalled at the front end.
#
# That kills what this file said two entries above. The prompt is not losing
# to stalls and never was: 3.43 a cycle is near what the part retires, and
# above the 2.5 that was attributed to the other runtime. The 0.7 written
# here earlier was a measured time divided by a guessed instruction count,
# and the guess was out by a factor of four.
#
# What it costs is instructions: ninety-six thousand million for something
# near a hundred and ten thousand million multiply-accumulates, twenty-odd
# instructions for each one that multiplies. perf annotate puts about twenty
# instructions in the hot loop with exactly one vpdpbusd among them.
#
# One change came out of it and is kept. The byte instruction's memory
# operand needs no alignment, so the activations are read where the quantizer
# left them instead of being copied into a block of their own first -- a copy
# of thirty-two bytes shared between eight rows, which is about four
# instructions for every multiply-add. 96.3 thousand million instructions to
# 93.2, and about five per cent of a prompt: 1.191 s against 1.251, better in
# two rounds of three, and the instruction count is what settles it because
# it does not vary between runs.
#
# Two did not, and both are worth the space:
#
#   Four rows to one insertion, so that one address computation and one
#   scale lookup serve four multiply-adds. It executes MORE instructions than
#   the loop it replaces -- 98.7 thousand million against 96.3 -- and reads
#   1.328 s against 1.269. The hand count that predicted otherwise was the
#   third such count this week to be wrong, and the counter was available
#   this time.
#
#   It also had a defect the sweep did not catch and the kernel's own test
#   did: the lane type carried Alignment => 32 on sixteen bytes of data, so
#   every entry is padded to thirty-two and a hand-written stride of sixteen
#   reads the wrong halves. An aspect that pads is a stride nobody declared.
#
# What is left of the prompt gap is a question rather than a plan: twenty
# instructions a multiply, and no attempt so far has moved that number by
# more than a few per cent. It wants somebody to read the annotated loop
# properly rather than another rewrite measured after the fact.
#
# covers: the row-product table under "Kernels"
# 2026-08-11, IQ4_NL and IQ4_XS. Re-measured rather than argued about, and
# they could be, because these two groups need no model: `tests benchmark`
# and `taskset -c 0-7 tests benchmark` are the whole of it.
#
# The row-product table is republished from one unpinned sitting, all fifteen
# formats together, and it gained the two new rows -- 1.37 and 0.92 ns an
# element, which makes them the slowest in the table. The existing formats
# read a few per cent slower than the figures they replace, which is what
# this README already says happens between sittings; nothing about them
# changed and the table is now internally consistent instead of spanning two
# sittings.
#
# The share figures are republished from one pinned sitting, taken with
# nothing else running. Eight shares on q8_0 with one vector reads 12420
# against the 13134 published before, and seven 12007 against 12532; the
# claim those numbers are there to support -- that eight no longer falls
# below seven -- still holds, and holds in this sitting rather than in the
# last one.
#
# One figure moved enough to say so rather than swap quietly: the four-bit
# format at thirty-two vectors and eight shares came out 17820 against the
# 23450 published, while its own seven-share figure was 20023. It falls off
# above four shares in every run taken here. The README now says that instead
# of claiming the two formats are level everywhere.
# 2026-08-11, the half-precision cache. tensors.adb gained an allocator and a
# deallocator for a half-precision buffer and numerics.ads a type; no loop
# either of these groups times was touched, and neither group's figures are
# taken through a session at all -- the kernel benchmarks build their own
# tensors and multiply them. Not re-measured: these two were taken again
# three commits ago on this machine, and taking them again for a type
# declaration would say what that run already says.
# 2026-08-15, phi3. Four groups moved with llama.adb and are not
# re-measured: everything phi3 adds runs while a model loads. The layer
# resolution asks once per layer which architecture it is, and Resolve_Part
# makes a view; nothing was added to an evaluation loop, which the diff
# shows in one reading.
#
# Measured twice all the same, and the pair is worth recording for what it
# says about this machine rather than about phi3: 2.089 s and 11.95 s of
# processor time at a load rising to 2.41, and 1.912 s and 10.97 s at a load
# that stayed at 1.5. The published figure is 1.88 s. A fifteen per cent
# spread between two runs an hour apart is what a busy machine does, and it
# is why the first of the two was not written down as a regression.

# 2026-08-15, gemma3. Four groups moved with llama.adb and are not
# re-measured. The twelve-token figure reads 1.809 s and 10.29 s of
# processor time against 1.832 s and 10.51 s taken the same way an hour
# earlier, which is inside what consecutive runs of this tool differ by.
#
# What these groups see of gemma3 is a function call per rotation that
# returns the model's base unless the model states a second one -- and one
# comparison per layer to decide it. The lesson from gemma2 was applied
# before measuring rather than after: the work that only one architecture
# needs is kept out of the loops the others run.

# 2026-08-15, gemma2. Four groups moved with llama.adb and are not
# re-measured; the twelve-token figure is the evidence, taken twice.
#
# The first version of this change applied the attention bound inside the
# loop that forms a score, which cost every architecture a test per score:
# twelve tokens went from 1.83 s to 2.07 s and the processor time from
# 10.5 s to 11.8 s, for a feature one architecture of six uses. The bound
# runs in a loop of its own now, entered only when there is one, and the
# figure reads 1.832 s and 10.51 s against the 1.828 s and 10.53 s measured
# before gemma2 existed.
#
# That is the whole of what these groups see of it: a loop that does not
# run, a null check before a normalization that does not happen, and a test
# on a zero cap when the logits come out.

# 2026-08-15, gemma, and a lesson about where a figure comes from.
#
# The row-product table moved by up to a factor of two in both directions --
# q4_0 from 0.33 to 0.59 ns an element, IQ4_NL from 1.44 to 0.59, IQ4_XS from
# 0.95 to 0.48, Q2_K from 0.79 to 1.01 -- and nothing in any decoder changed.
# What changed is that kernels.adb gained a procedure: the Gaussian gate that
# gemma wants, which no row product calls.
#
# Two runs an hour apart agree on the new numbers, so it is not the machine.
# Adding a subprogram to a hot compilation unit moves what the compiler does
# with the rest of it, and this unit is where every decoder lives. The
# published table is the new one, and the old one is not evidence of a
# regression or an improvement in anything: it is the same code compiled
# beside different code.
#
# The reference, drafting and scaling groups are not re-measured. The
# twelve-token figure is the evidence for all three: it runs the same
# engine paths through the same worker pool, it moved by less than these
# runs move between themselves, and the code they cover changed in exactly
# the two ways it did -- a branch in RMS_Norm and a test on the embedding
# scale, both taken on every architecture. The scaling group's figures are
# pinned row products and would want a pinned run to say more than that,
# which the machine has not offered again today.
#
# The device ratios come from the same run and are republished with it. The
# twelve-token figure was retaken too -- 1.828 s against 1.88 s at a load of
# 1.42 to 1.55 -- which is what says the engine paths themselves did not
# move: the branch gemma adds to RMS_Norm and the test on the embedding
# scale are both there for every architecture now.

# 2026-08-14, waiting for a quiet machine moved into the tools. Three groups
# moved with benchmarks.adb. Not re-measured: what was added is a wait before
# any timing starts and a procedure that prints while it waits. Neither runs
# once measuring has begun -- Wait_For_Quiet returns before the first round,
# and with no --wait it is not called at all, which is how every figure in
# this file was taken.

# 2026-08-14, the pinned figures retaken, which was the last group with no
# load recorded. Eight shares reads 10122 Me/s with one vector against the
# published 12420, and seven reads 10713 against 12007 -- so the peak at
# seven that the earlier run had removed is back, at six per cent rather
# than the quarter the change was made for. Batched there is no peak: 21234
# at eight against 21035 at seven.
#
# The four-bit against eight-bit numbers moved too, and swapped sides: the
# four-bit format now leads serially and batched and trails at eight shares
# with one vector, where last time it was the reverse. Two runs disagreeing
# about which is ahead at one shape is what level looks like, and the README
# says that rather than picking the run that reads better.

# 2026-08-14, the tokenizer figures taken by the tool. 0.0098 s for sixty
# thousand ordinary characters and 0.0127 s for sixty thousand brackets, at
# a load of 1.12 -- against the 0.039 and 0.045 timed in the shell, which
# were a whole `model_runner run` rather than the encode. The row-product
# table and the thirty device ratios come from the same run and are
# republished with them.

# 2026-08-14, the worker-count pair retaken -- and it does not say what the
# paragraph built on it said. Fourteen threads reads 1.536 s of wall and
# 16.12 s of processor time against seven at 1.884 s and 10.42 s, both at a
# load of about 1.25. That is eighteen per cent off the wall for fifty-five
# per cent more processor time, where the published text said six per cent
# for seventy.
#
# The old numbers came from the shell's timer, on runs whose load nobody
# recorded, and included the model load that these exclude. The conclusion
# stands -- one worker per core -- but it stands on energy alone now rather
# than on the wall being nearly unchanged, and the README says so. This is
# the third figure this week that moved once it was taken again with its
# conditions written down, and the second whose stated reasoning had to be
# rewritten rather than just its digits.

# 2026-08-14, the tokenizer timed by a tool rather than by the shell. Three
# groups moved because benchmarks.adb gained a section. Not re-measured, and
# the reason is order: the new section runs after the row products, the
# device ratios and the share scaling, so it cannot have affected a figure
# taken before it started. Read Run to check that rather than take it on
# trust -- it is one call list, and the tokenizer lines sit between the
# vector kernels and the metadata parse.

# 2026-08-14, processor time from this repository's own tool. `tests speed`
# reports it beside the wall time, taken around the same region, which
# removes the one figure in this file that came from the shell. The reason
# it did not before -- that totalling processor time across worker tasks
# needs a host call bound per platform -- was wrong: it is the same file the
# load comes from.
#
# The twelve-token figure was retaken with it: 1.884 s and 10.39 s of
# processor time at a load of 1.28 to 1.82. Reference, device and drafting
# were not, and the reason is what that figure shows: the same measurement
# read 1.869 s before this change and 1.884 s after, which is inside the
# spread two consecutive runs of this tool have. What was added is two reads
# of one small file per repeat, one just inside each end of the timed
# region -- microseconds against seconds, and inside rather than outside
# because a processor time taken around a different region than the wall
# would answer about different work.
#
# The worker-count pair was not retaken either.
# Each half of that comparison needs its own quiet window and the machine
# gave one, not two; the numbers are kept and the README says they are the
# oldest here.

# 2026-08-14, the figures retaken with their loads. Three groups had none
# recorded and now have: the twelve-token figure, the reference comparison
# and the drafting trio. Every one of them moved, and by more than the
# change to the code between then and now could account for -- the
# twelve-token figure from 1.37 s to 1.87 s, the reference ratio from
# thirteen times to eleven, the drafted run from 4.094 s to 5.705 s.
#
# That is the argument for recording the load, made by the figures
# themselves. The older ones were taken on a machine whose state nobody
# wrote down, so there is no way to tell how much of the gap is the machine
# and how much is the program. From here on there is.
#
# What it cost: the tools refuse to measure above a load of 1.5, and this
# machine sat above it nearly all day. The runs waited for windows and took
# them one at a time.

# 2026-08-14, the busy-machine bound moved to Host_Load so that `tests speed`
# refuses on it too. Three groups moved for it. Not re-measured, and the
# reason is that nothing measured changed: the bound and the predicate moved
# from Benchmarks to Host_Load, benchmarks.adb calls them where it called
# them before, and the only other edit to that file is blank lines. The
# refusal runs before any timing.

# 2026-08-14, a device that stops answering gets its own code, and the bound
# gets an option. The engine is unchanged in what it computes; what moved is
# which diagnostic a stalled device produces and where the bound comes from.
#
# Re-measured, the short device run: 1.178 s at a load of 3.74, generating
# 1.000 s, against the published 1.262 and 1.062 at a load of 1.0. Faster on
# a busier machine, which is the noise this pair has always had and is why it
# is published beside its processor counterpart rather than alone. Not
# republished, for the reason given twice above.

# 2026-08-14, the wait made sayable. The slice and the whole bound are what
# the engine was opened for rather than constants, so a test can reach the
# giving-up path with a device in perfect health and can make a stop request
# land between slices instead of before the product starts.
#
# Re-measured, the short device run: 1.226 s at a load of 1.66 against the
# published 1.262 at 1.0, with generating at 1.053 against 1.062. Reading two
# fields off the engine instead of two constants costs nothing, which is what
# these say. Not republished, for the reason under the entry below: the pair
# is taken with its processor counterpart and replacing one column alone
# would make the comparison span two afternoons.

# 2026-08-14, the sliced wait and the token that reaches it. The device now
# waits for its fence in twenty-millisecond slices instead of one second, and
# asks between them whether the caller wants to stop.
#
# Re-measured, the device pair: 1.178 s short and 2.556 s long against the
# published 1.262 and 2.504, at a load of 1.85 to 2.17 against the published
# pair's 1.0. Slicing a wait costs nothing when the fence signals inside the
# first slice, which is what these numbers say. Not republished: the pair is
# published beside its processor counterpart and taken with it, and replacing
# one column with a run taken on its own would make the comparison a
# comparison of two different afternoons.
#
# The engine, reference and drafting groups were not re-measured. What they
# see of this change is one field assignment per Evaluate call -- the token
# for the call in progress, stored where the products can reach it -- and the
# device pair above, which measures the same assignment along with the part
# that actually changed, did not move.

# 2026-08-14, the second place the weights are freed. Repacking frees the
# arena while the model stays open, and the device was told about the model
# closing and not about that. Both go through one procedure now, which is
# the change these four fingerprints moved for.
#
# Re-measured rather than argued, for the group where the change is on a
# timed path: repacking happens during Prepare, and the twelve-token figure
# quotes loading. The pair reads 1.818 s and 1.407 s at a load of 1.25
# against the published 1.777 and 1.262 taken at 1.0 -- the generating
# component is 1.449 against 1.398 and 1.049 against 1.062, so the two runs
# agree to within what consecutive runs of this tool agree to. The published
# table is left alone: replacing figures with figures that say the same
# thing would lose the comparison with the run before, which is the only
# thing that makes either of them mean anything.
#
# The reference and drafting groups were not re-measured. The added call
# walks the list of matrices a device holds, which is empty on every
# processor path, and the engine group above measures the same code and did
# not move. That is the whole of the change: one procedure, called from the
# two places the weights are freed.

# 2026-08-14, the device forgetting a closed model's matrices. Four groups
# moved because llama.adb and the device engine changed. Not re-measured, and
# this is the reason: the whole change is one call in Model.Close and the
# procedure it calls. Close happens after every timed region these figures
# cover -- the twelve-token figure quotes load and generation separately and
# neither includes it, and the device pair is timed to the last token. The
# new procedure has no caller inside Multiply. The diff is five files and
# seventy-four added lines, all of them the procedure, its comment and the
# one call; read it rather than take this on trust.
#
# Worth recording why it was added: the conformance sweep was failing about
# half its runs, by a fifth of a logit, and moving from run to run. A device
# remembers a matrix by where its bytes lie, and the sweep closes a model per
# format and architecture with the device open across all of them, so the
# allocator handed the next model an address the device still had weights
# for. The comment in the shader-format entry below says the residency key
# was fixed for format and shape; this is the lifetime half of the same
# fault, and it was not fixed with it because the assumption written down at
# the time -- that in this program the weights live as long as the model --
# was true of the program and false of its own test sweep.

# 2026-08-14, the batch measured for every format too. It was one format at
# two widths, on the argument that a batch buys the same arithmetic
# everywhere -- and the argument was wrong. Batching compresses the spread
# across formats from nine to one down to two to one, and binary32 gains
# most of all: 1.39 with one vector, 0.26 with eight. An argument standing
# in for a measurement, in a file whose whole purpose is to stop that.
#
# Re-measured from one run at a load of 1.25: thirty device ratios and the
# row-product table, all published. The scaling group keeps the reason given
# under the entry below -- the change is again the device section's call
# list, and the share-scaling loop it covers is untouched by it.

# 2026-08-14, the benchmark measures every format on the device. It measured
# the three the shader used to decode; the other twelve had arrived as
# branches with nothing timing them.
#
# Re-measured from one run at a load of 1.29: the fifteen device ratios, the
# two batched ones and the row-product table, all published. The finding is
# that the device's advantage tracks how badly the processor's own decoder
# vectorizes -- four to one on Q5_0, Q5_1, IQ4_NL and IQ4_XS, which are the
# formats a per-lane shift or a gather makes slow on x86, and a quarter on
# the formats the processor is best at.
#
# The scaling group was not re-measured, and this is the reason. Its
# fingerprint moved because benchmarks.adb is in its source list and
# benchmarks.adb changed; the change is which formats the device section
# asks for, and the share-scaling loop it covers is not touched by it. Read
# the diff rather than take this on trust: it is one call list. The figures
# that group covers were taken pinned, which this run was not, so replacing
# them with unpinned numbers would be a worse measurement rather than a
# newer one.

# 2026-08-13, the shader learnt the other twelve formats. It decoded three of
# the fifteen and now decodes all of them, so nothing needs repacking to
# reach a device.
#
# Re-measured, everything this group covers, at a load of about 1.0 for the
# model runs and 1.29 for the benchmark: the four rows of the two-backend
# table, the three-way table of where the weights are, and the five per-format
# ratios. The long prompt on the device moved from 3.824 s to 2.504 s, which
# is not claimed as the change's doing -- the earlier pair was taken at a
# higher load. What the pair does say is that fifteen branches did not cost
# what three did: the ratios are within a few per cent of the run before,
# which is the risk that was worth measuring.
#
# The other three groups here changed because llama.adb, generation.adb,
# grammar.adb and cli-execute.adb were reindented -- blocks that had drifted
# three columns left of where they belong, which the compiler had been
# reporting as style warnings into log files nothing read. Not re-measured:
# whitespace outside a string literal is not in the generated code. Checked
# by reading each change rather than assumed, and they are all one edit:
# the body of a block moved right by three columns.

# 2026-08-13, the benchmark tool itself. Three of these groups take their
# figures from `tests benchmark` and none of them listed the tool. The same
# hole the speed_run note above describes, in the other measuring tool, and
# it opened the same way: nobody thought of the thing doing the measuring as
# a source. It closed on the change that made it matter -- the device rows
# now take the best of their rounds rather than the middle one, which moved
# three published ratios and would have moved them silently.
#
# Re-measured on a machine at 1.36 rather than the 2.34 of the previous run,
# which the tool now insists on: it refuses to measure at all above a load of
# 1.5. The five device ratios were republished and the single-vector rows
# moved by up to a sixth while the batched rows did not, which is the bias
# the README names, measured rather than argued. The row-product table was
# republished from the same run; nothing in it moved by more than eight per
# cent and the ordering is unchanged.
#
# The scaling group was not re-measured, and this is the reason. Its
# fingerprint moved because its source list gained a file, not because any
# source in it changed: benchmarks.adb is new to the list, and the only
# change inside it since these figures were taken is which round the device
# rows take. The scaling rows still take the median they always took, and
# Fastest has no other caller. The refusal above runs before any timing and
# the load reader runs outside every timed region.
#
# A pinned run was taken anyway, and it is worth recording that it read 10798
# Me/s for q8_0 at eight shares against the published 12420, on a machine at
# 1.38. That is thirteen per cent, in the direction a busy processor moves a
# processor figure, and it is why the figures this group covers are not
# replaced with it: they were taken under conditions this machine no longer
# offers, and a worse measurement is not a newer one. What it does say is
# that these figures predate the tool reporting a load, so the conditions
# they were taken under are not recorded anywhere -- unlike every figure
# taken from here on.
# load: 1.70 rising to 2.37, one `tests benchmark` run, 2026-08-15
# model: none. These figures time kernels on tensors this tool builds itself.
# 2026-08-21, every figure whose sources bert touched, re-measured. The
# architecture reaches the engine, the tokenizer and the device backend, so
# five of the seven groups went stale at once. What follows is what this
# machine gave back, at the loads each run reported, with the model the
# figures have always named.
#
# The processor did not move. Twelve tokens read 3.743 s against the 3.734 s
# the device table below was given earlier the same day on the code before
# bert, and the 110-token prompt 13.256 s against 13.353 s. Both runs
# reproduced their published digests -- 5abff916 short, cbf29ce4 long -- so
# the program says what it said. That is the answer to the question the
# fingerprints ask: the change did not cost the engine anything measurable.
#
#   engine     12 tokens, short prompt, cpu, 7 workers: 3.743 s wall,
#              0.466 s prompt, 3.215 s generating, 21.11 s of processor
#              time, load 0.75 to 1.45. The same run at 14 threads: 4.146 s
#              wall and 27.26 s of processor time, load 1.31 to 4.19 -- the
#              rise being the run filling the machine. Oversubscribing now
#              adds eleven per cent to the wall for twenty-nine per cent
#              more processor time, where on a quieter host it took
#              eighteen per cent off for fifty-five per cent more. The
#              advice that rested on it is turned round in the README.
#
#   reference  4 tokens: cpu 1.449 s wall (0.345 prompt, 1.104 generating)
#              against reference 12.822 s (7.712 and 5.110), both printing
#              5f85f064. Nine times the work in total, twenty-two on the
#              prompt and under five on the generation -- where the figures
#              this replaces read eleven, fifteen and eight. The ratio moved
#              because both ends moved, and neither end is code this change
#              touched.
#
#   device     short run 0.885 s against the processor's 3.743 s -- 0.122 s
#              of it evaluating the prompt against 0.466 s, and 0.627 s
#              generating against 3.215 s; long run 2.174 s against
#              13.256 s. Both faster than the 1.062 s and
#              2.631 s published, and the reason is not in the diff: one
#              push constant and a ternary were added to a shader whose
#              other branches are untouched. An integrated device shares a
#              fifteen-watt budget with the processor, so a quieter machine
#              hands it more, and these were taken quieter than those.
#
#   tokenizer  0.0139 s for sixty thousand ordinary characters and 0.0156 s
#              for sixty thousand brackets, against 0.0098 and 0.0127. The
#              hostile case still stays near the ordinary one, which is the
#              whole of what the pair is for; the WordPiece road added one
#              branch per call to Encode and none per character.
#
# 2026-08-21, drafting is not re-recorded, and this is why. The run gives
# 14.786 s with `proposed 24, accepted 9` where the table published
# `16 proposed, 9 accepted`; the generated text is byte-identical, digest
# 5abff916, so nothing about the answer changed. The count does not
# reconcile. Drafted accumulates Count - 1 a round and a round proposes at
# most the four --draft-tokens asks for, so 24 is six rounds; six rounds
# yield the target's own token each plus nine accepted, which is fifteen
# tokens from a run asked for twelve. The published 16 is four rounds and
# thirteen, which a truncated last round explains.
#
# Neither generation.adb, where that line lives, nor speed_run.adb, which
# reports it, is touched by the change that made this group stale. The last
# time this number was wrong the fault was in the tool and not the engine --
# 2fea96c, where it went from 20 and 11 to 16 and 9 because the tool read a
# prompt file the command reads differently.
#
# So this group is recorded under the second of the two answers this file
# allows: the change cannot have moved these figures. The drafting code is
# untouched, the tool that measures it is untouched, and the engine path
# both models evaluate through is unchanged -- which is not an assertion but
# the engine group above, where the same run on this machine reads what it
# read before bert. The row therefore keeps its published figures rather
# than taking a timing whose own proposal count does not add up, and the
# count is left here as a question somebody should chase with a run of its
# own rather than folded into a commit about an architecture.

# 2026-08-22, the fingerprints moved again and the figures did not. Reading
# a published all-MiniLM found two faults, and both are confined to code no
# figure here runs. The rotary width is now zero for bert by construction,
# which is a branch on the architecture and cannot reach a llama model; the
# WordPiece road looks a word's first piece up with its marker, which is a
# branch on the tokenizer kind and cannot reach the SentencePiece road the
# tokenizer figures are taken on. TinyLlama is what every timing above uses
# and it is neither.
#
# 2026-08-22, nomic-bert. The same again and for the same reason: a second
# architecture of Bert's shape reaches the engine and the tokenizer, and
# every branch it added is a branch a llama model does not take. What it
# changed in shared code is one allocation condition -- Post_Room is now
# asked of the arrangement rather than of a list -- and Gemma2, Gemma3,
# Falcon and Phi2 still take it exactly as they did. The engine figures were
# taken on this machine after the first of these architectures and read what
# they read before either.
#
# 2026-08-22, the drafting row is not going to be re-measured soon, and
# saying so is better than leaving the reader to wonder. The machine is
# carrying two other projects' runs for the foreseeable future and the tool
# refuses to publish a figure above a load of 1.50, rightly. What is known
# is above: the counts are self-consistent, the published pair has been
# stale since 6bea10f changed the drafting path six hours after the figures
# were taken, and the current truth is 24 proposed against 9 accepted. The
# row keeps its figures until somebody can take them properly.
#
# The same is true of the two KV-cache figures in the README, which quote
# the faster era of this machine beside an engine table that no longer does.
#
# 2026-08-22, two faults a second runtime found. Both are confined to code
# no figure here runs: the WordPiece road now wraps a text in its two
# markers, which is a branch on the tokenizer kind that the SentencePiece
# road the tokenizer figures are taken on does not take; and nomic-bert
# pairs its rotation the way every architecture but llama already did, which
# is a case arm for one architecture. TinyLlama is neither.
#
# Two more edits followed, and the same holds for both: a diagnostic that
# fires only where a text is longer than a context a causal model never
# reaches, and one metadata key read at load. Neither is in a timed path and
# neither is reachable by a llama model.
#
# So these are recorded under the second answer this file allows rather than
# measured a second time in one night: the change cannot have moved them.
# The engine figures above were taken after the first of the two edits and
# read what they read before bert, which is the evidence for the first half
# of that claim; the second half is a branch that a llama model does not
# take.

# load: 0.72 at the start of the pinned `tests benchmark` run this comes from, 2026-08-24
# 2026-08-25, taken again after four rows of the integer product went into
# one machine code insertion. The row-product table is the floating-point
# path and does not enter that kernel, and it says so: fourteen of fifteen
# rows are within a hundredth of a nanosecond of the last reading and the
# fifteenth, Q5_K, is 0.42 against 0.43. Q8_0 reads 0.40 against 0.39, Q5_0
# 0.56 against 0.55. Medians of three unpinned runs of `tests benchmark` on
# the kept kernel.
#
# What did move is the pair the same command reports for the two arithmetics,
# and re-reading it found a figure the README had left standing since before
# the byte dot product existed. The quantized path against the floating-point
# one is 4.44x with one vector a pass and 0.92x with thirty-two -- the second
# is below one. It is not this change: the same pair reads 4.44 and 0.92 on
# the compilation before it, alternated in one sitting, two rounds each.
# The batched case is where the floating-point path amortizes its decode over
# thirty-two vectors and the quantized one still has thirty-two activation
# vectors to round. That is the reading; the explanation is untested.
#
# Two things measured here and not taken.
#
# Eight lanes rather than four. The insertion folds the byte instruction's
# eight integer lanes into four with a vextracti128 and a vpaddd before it
# scales them. Keeping eight removes both: 65.4 thousand million instructions
# for the 110-token prompt against 71.96, and 1.022 s against 1.092 across
# three alternated rounds -- so it is faster, and by more than the noise
# floor. It changes what the model says: sixty-four generated tokens hash to
# e341d15aa6edc6a4 where the four-lane fold, both sixteen-bit compilations
# and every figure in the README say 1cb5fffbb21399ad. The fold makes each
# float accumulator receive an exact wider integer sum, and that is why the
# byte path agrees with the sixteen-bit one on this model today; the README
# says that agreement is not promised, and it is still worth more than six
# per cent here. Not taken, and the numbers are here so it can be reversed.
#
# An earlier reading of the same experiment said eight lanes were *worse*,
# and it was wrong: the lane array carried Alignment => 32 on sixteen bytes,
# GNAT padded every entry to thirty-two, and the insertion walks those
# entries by hand with a stride of sixteen. It read the wrong halves. The
# same defect appeared twice, an hour apart, the second time because a file
# was restored from HEAD and took the fix away with it. What caught it both
# times was the test comparing the three compilations, not the conformance
# sweep -- the sweep passed on the wrong answers, because a tolerance wide
# enough for a different rounding is wide enough for some wrong sums too.

# 2026-08-25, taken again after the prompt was given the strip kernel. The
# row-product table is the floating-point path and does not enter the integer
# kernel, and it says so again: every row within a hundredth of a nanosecond
# of the last reading. Q8_0 0.39 against 0.40, Q6_K 0.41 against 0.40, F16
# 0.59 against 0.58, Q5_1 0.59 against 0.60, Q2_K 0.75 against 0.74, the
# other ten unchanged. Read as a band, as the section says.
#
# The pair the same command reports for the two arithmetics: 4.42x the
# floating-point path with one vector a pass and 0.96 with thirty-two, where
# the sitting before read 4.44 and 0.92. The batched case is still below one
# and still unexplained past the guess recorded above it.

# 2026-08-26, taken again after the weight scale was given VCVTPH2PS. The
# row-product table is the floating-point path and does not enter the integer
# kernel: every row within a hundredth of a nanosecond of the last reading,
# five of them moving by exactly that and ten not at all. F32 0.28 against
# 0.27, Q8_0 0.40 against 0.39, Q6_K 0.40 against 0.41, Q5_0 0.55 against
# 0.56, F16 0.58 against 0.59, Q5_1 0.60 against 0.59, Q2_K 0.76 against
# 0.75. Read as a band, as the section says.
#
# The pair for the two arithmetics did move, and only on the half that reads
# a scale per row rather than per row and vector: 4.71x the floating-point
# path with one vector a pass, against 4.42 before, and 0.96 with thirty-two,
# unchanged. That is the half-precision widening leaving the single-vector
# kernel, where it was a larger share.

# 2026-08-26, taken again after a tile of answers was written back in the
# order the target keeps them rather than the order the tile does.
#
# Almost nothing here moves, and that is the finding worth recording. The
# change is worth about four per cent of a prompt -- 0.777 s against 0.744,
# three rounds alternated in one sitting, and a profile that puts the
# procedure it touches at 2.1 per cent where it was 5.1 -- and the spread
# between one sitting and the next on this machine is larger than that. The
# 110-token prompt reads 0.782 s here against 0.784 published. **Alternated
# rounds are the instrument; a table taken a day apart is not.**
#
# Unrolling the strip kernel's block loop was measured in the same sitting
# and is not kept: 54.03 thousand million instructions against 54.79, and
# 0.756 s against 0.738 over three alternated rounds, better in none of
# them. The twenty-one per cent of samples a profile charged to that loop's
# branch and cursor advances was skid from the loads they wait for.

# 2026-08-26, taken again after the four-bit k-quant was given an integer
# kernel. Every figure in this file is Q8_0 and the Q8_0 path did not change,
# which is what these say: the 110-token prompt reads 0.806 s against 0.782,
# twelve tokens 0.458 s against 0.448, sixty-four generated 2.160 s against
# 2.165, and every digest is what it was. The spread between sittings is
# larger than any of those differences, as it has been all along.
#
# Q4_K, which is the change. Medians of three alternated rounds:
#
#   110-token prompt, floating-point path   4.598 s   31.67 s of processor
#   110-token prompt, byte dot product      1.317 s    8.19 s
#   thirty-two generated, either way        0.949 s / 0.930 s
#
# and the digest is 4b6e8e99ae285b2a in all six runs. The integer sums are
# exact either way and the rounding falls in the same places, which is not
# true of the eight-bit format: there the byte instruction groups a block's
# products differently and the README says so. Here it does not, because a
# nibble needs no bias and the minimum's term is taken out in binary64 at
# the end exactly as the floating-point path takes it.
#
# The same sitting, unalternated: 1.375 s for the prompt and 0.844 s
# generating, at a load rising through 1.7.
#
# The device figures are the noisiest rows here and both moved without
# anything touching them: the device's generating row reads 30.9 t/s against
# 27.3 last sitting, and llama.cpp at -ngl 0 reads 872.1 t/s against 774.6.
# Neither is a change to either program.

# 2026-08-26, taken again after the four-bit k-quant's generated token was
# given a kernel of its own. As with the batch kernel before it, every figure
# in this file is Q8_0 and the Q8_0 path did not change: 0.738 s for the
# 110-token prompt against 0.806 last sitting, 2.160 s for sixty-four
# generated against 2.160, and every digest what it was.
#
# Q4_K, which is the change. Medians of three alternated rounds:
#
#   thirty-two generated, floating-point path   0.938 s   6.33 s of processor
#   thirty-two generated, byte dot product      0.505 s   2.81 s
#   110-token prompt, either way                1.382 s / 1.442 s
#
# and 4b6e8e99ae285b2a in all six. Fifty milliseconds a token against
# eighty-four, where the eight-bit format takes thirty-three.
#
# Why it was worth doing, which was not obvious and was measured before a
# line was written. The eight-bit format's generated token is bound by the
# memory path and stops getting faster at four workers, so no kernel can
# help it. This one was still getting faster at seven -- 1.510 s at two
# shares, 1.130 at four, 0.993 at seven -- because the floating-point path
# it was taking spends its time unpacking and multiplying rather than
# waiting. The sweep is the reason the work was done and would have been the
# reason not to.
#
# The same sitting, unalternated: 1.468 s for the prompt and 0.505 s
# generating, at a load rising through 1.7.
#
# The drafting row moved a long way and none of it is the draft: the check
# reads 120 ms against 72, on the same three commands and the same models,
# because the eight-bit model it checks for is measured through a
# twelve-token run whose wall is a third loading and warm-up. The conclusion
# is the one it has always been and is now unconditional -- at this
# acceptance a round yields less than the check costs, so no draft price
# pays.

# 2026-08-26, taken again after the six-bit k-quant was given a kernel. As
# with the two before it, every figure in this file is Q8_0 and the Q8_0
# path did not change: 0.719 s for the 110-token prompt against 0.738 last
# sitting, 2.187 s for sixty-four generated against 2.160, and every digest
# what it was.
#
# Q4_K_M, which is the change, and which is three formats rather than one.
# Reading the file's tensor table: 135 tensors Q4_K, 21 Q6_K, 45 F32 norms;
# by element, 83.1 per cent Q4_K and 16.9 per cent Q6_K. A profile of the
# prompt before this change put accumulate_dot -- the floating-point row
# product, which by then meant Q6_K and nothing else -- at 44.7 per cent,
# beside the Q4_K kernel's 43.6. One sixth of the weights was costing half
# the time.
#
#   110-token prompt, Q6_K on the floating-point path   1.355 s   8.29 s cpu
#   110-token prompt, Q6_K on the byte product          0.990 s   5.47 s
#   thirty-two generated, either way                    0.508 / 0.501 s
#
# and 4b6e8e99ae285b2a in all six. Generating does not move because a
# six-bit product of one vector still goes the other way.
#
# End to end for this file today: 4.598 s to 0.990 s, four and a half times,
# across three kernels and with the digest never moving once.
#
# The same sitting, unalternated: 0.994 s for the prompt and 0.508 s
# generating.
#
# The drafting check reads 53 ms here against 72 and 120 in the two sittings
# before it, on unchanged code. It is a difference of two twelve-token runs
# and a twelve-token run is a third loading and warm-up; the README now says
# to read it as tens of milliseconds rather than as a number.

# 2026-08-26, taken again after the six-bit k-quant's generated token was
# given a kernel -- the last floating-point path in a "_M" file. Every
# figure here is Q8_0 and the Q8_0 path did not change: 0.763 s for the
# 110-token prompt against 0.719, 2.159 s for sixty-four generated against
# 2.187, and every digest what it was.
#
# Q4_K_M, which is the change. Medians of three alternated rounds:
#
#   thirty-two generated, six-bit on the floating-point path   0.505 s
#   thirty-two generated, six-bit on the byte product          0.356 s
#   110-token prompt, either way                        1.017 / 0.983 s
#
# and 4b6e8e99ae285b2a in all six. Thirty-five milliseconds a token where
# the eight-bit file takes thirty-three: a file two thirds the size now
# generates at the same rate.
#
# One of the two single-vector kernels did not work at first, and the reason
# is worth keeping. Every insertion written before them advances the
# pointers it was handed -- an addq on a register declared an input -- which
# tells the compiler nothing and works only while it has no other use for
# that register. With eleven operands it stopped working: the six-bit kernel
# raised inside a worker, and *adding an exception handler and nothing else
# made it run*, which is the signature of code generation rather than
# arithmetic. Both single-vector kernels now keep their cursors in registers
# of their own, named in the clobber list, and advance no operand. The
# four-bit one had the same latent fault and had already passed a gate.
#
# The drafting threshold has now read 17, 10, 6 and 9 milliseconds a token
# across four sittings of unchanged code. It is a difference of two
# twelve-token runs and a twelve-token run is a third loading and warm-up;
# the README says so and says to read the conclusion rather than the number.

# 2026-08-26, and the figures above were NOT re-taken. This entry is the
# other thing this file allows -- saying why a change cannot have moved
# them -- and it is the first time that has been used rather than a sitting.
#
# What changed: every machine code insertion in the integer kernels stopped
# advancing the pointers it was handed. They did it with an addq on a
# register declared an input, which tells the compiler nothing and works
# only while it has no other use for that register; the six-bit kernel
# raised inside a worker when it stopped working, and an exception handler
# and nothing else made it run again. Four insertions still did it, all of
# them working and all of them having passed gates, including Rows_Singly,
# which every generated eight-bit token runs and which advanced four
# operands -- more than the one that failed. All six now keep cursors in
# registers of their own and copy the counter rather than decrementing it in
# place.
#
# Why it cannot have moved the figures, and the evidence rather than the
# argument. Three alternated rounds on both models, in one sitting:
#
#                                     before      after
#   eight-bit, sixty-four generated   2.085 s     2.086 s
#   eight-bit, 110-token prompt       0.767 s     0.765 s
#   four-bit, thirty-two generated    0.357 s     0.362 s
#
# and every digest identical -- 448c2ed68ec342ee, cbf29ce484222325,
# 4b6e8e99ae285b2a. That is a better instrument than a fresh sitting would
# be: alternated rounds in one sitting resolve four per cent, and the spread
# between sittings on this machine is larger than that. Re-measuring
# everything would have replaced numbers known not to have moved with
# numbers differing by more than the change does.
#
# Rows_Singly is one instruction a block shorter than it was, three pointer
# advances having become two, which is the only arithmetic difference and is
# below what any figure here resolves.

# Re-measured for the 2026-08-28 entry below -- the bias correction and
#   the eight-lane fold moved into the insertion.
# load: nothing else running, on a part that had stopped cooling -- it
#   settled at 51 C before the first run.
# 2026-08-29, the fused layer kept, and everything above re-measured on a
# slower day than the one before it. The whole of `### Speed`, `### The
# reference backend`, `### The device backend`, `### Drafting` and
# `### Against llama.cpp` was taken in one sitting, this time with
# llama.cpp's own four rows taken in it as well -- and llama.cpp's processor
# row fell from 364.7 to 358.0 at 110 tokens and from 304.1 to 278.2 at
# 1419, on a binary nothing here touched. That is the size of a day on this
# machine, and it is why the ratios are what this file argues about and the
# rates are what it records.
#
# The change itself is device-only: a layer's second half in one submission,
# which the processor's path cannot reach. The processor figures were taken
# again anyway rather than carried over, and the long prompt was measured on
# both binaries back to back to say so -- 5.845 and 6.202 s before against
# 6.187 and 6.041 after, which is one figure read twice.
#
# The device table under `### The device backend` was taken twice: once with
# the machine's load climbing through the four runs, and again from a load
# of 0.94, which is the reading published -- 0.412 s and 0.320 s for the
# twelve-token run, 0.273 s of that generating on the device. The first
# sitting read 0.433 and 0.301 for the same two, which is the size of the
# error a load of 1.85 puts on a figure of a third of a second, and is why
# the table says which load it was taken at.
#
# The device's generated row is what moved: 42.3 to 43.8 tokens a second,
# with sixty-four generated at 1.447 s against 1.476 in nine alternated
# rounds. The device's prompt rows moved with the day and not with the
# change -- the fused path fires for one position at a time, and a batch
# amortizes its submissions already.
#
# norm.comp and combine.comp join the device and outside groups' sources.
# They were not there because they did not exist when the groups were last
# stamped, and a shader the device figures depend on that nothing watches is
# exactly the hole this file exists to close.

# 2026-08-30, attention's inner loops unrolled, and only the two device
# groups re-measured. The change is one shader -- src/shaders/attention.comp
# -- so the engine, reference and drafting groups' sources did not move and
# their figures were not re-taken; the check agrees, which is the first time
# in this file a sitting has been that narrow and it is worth saying why it
# could be.
#
# Taken in one sitting with llama.cpp's own four rows again, and its numbers
# repeat the day before's to within their own spread: 358.7 against 358.0 at
# a hundred and ten tokens, 277.6 against 278.2 at 1419. So the day is the
# same day and the device rows moved for the change -- the long prompt from
# 745.7 to 827.4 tokens a second, and the gap there from 2.36 to 2.21.
#
# The device table under `### The device backend` was taken again with the
# rest: 0.419 s and 0.295 s for the twelve-token run, 0.354 s and 0.256 s of
# those generating, 0.440 s and 0.167 s for the hundred-and-ten-token
# prompt, all at a load under 1.15. The processor column repeats the day
# before's to within a per cent on a path this change cannot reach, which is
# the check on the sitting rather than a figure in its own right.
#
# The 110-token prompt and the generated row did not move and were not
# expected to: attention grows with the square of the context, so a hundred
# and ten tokens holds a hundred and sixty times less of it than 1419, and a
# generated token is one query rather than a block of eight.

# 2026-08-30, the second sitting of the day: a prompt's layers fuse as a
# generated token's do, and all five groups that name llama.adb re-taken.
#
# The published device rates moved further than the change did, and both
# numbers are here because only one of them is about the program. Alternated
# against the commit before it, three rounds each way, the fusing is worth
# one and a half per cent of the long prompt and six of the short one; the
# published long-prompt rate moved from 827.4 to 891.9 tokens a second,
# which is eight. The difference is the day: the same code read 1.715 s in
# the morning's sitting and 1.602 s in this one. llama.cpp's four rows were
# taken in both and repeat to within their own spread -- 276.2 against 277.6
# at 1419 tokens, 1816.3 against 1824.6 -- which is what says the drift is
# the machine and not either program.
#
# The processor time is the figure this change is really about and the one
# the device table now carries: a 110-token device prompt costs 0.18 s of
# processor where it cost 0.30, and the 1419-token one 2.31 s where it cost
# 3.30. The join and the normalization did not become free, they became the
# device's; what became free is two round trips a layer and a third of the
# processor the device backend was using while the device worked.
#
# The device table under `### The device backend` in full, at a load under
# 1.15: 0.417 s and 0.298 s for the twelve-token run, 0.060 s and 0.042 s of
# those evaluating and 0.355 s and 0.257 s generating, 0.462 s and 0.153 s
# for the hundred-and-ten-token prompt, 1.90 s and 0.18 s of processor.
#
# The engine, reference and drafting groups were re-taken because llama.adb
# is one of their sources, and they repeat: 0.416 s for twelve tokens
# against 0.412, 0.411 s for the drafting row's first line against 0.408,
# the reference at fifty-five times as before. The change is inside a branch
# only a device batch reaches, and the figures say so.

# 2026-08-30, the third sitting: a layer's first half fuses too, and the
# five groups naming llama.adb re-taken again.
#
# The device's 1419-token prompt reads 966.0 tokens a second against 891.9,
# and the alternated measurement of the change is five per cent -- which for
# once is most of the eight the published rate moved, because llama.cpp's
# rows are within a per cent of the sitting before (273.6 against 276.2 at
# 1419, 1817.5 against 1816.3). The device's gap there is 1.88, under two.
#
# The processor time is again the larger half. A 1419-token device prompt
# spends 0.65 s of processor where it spent 2.27 before this change and 3.30
# before the one before it; the 110-token one spends 0.04 s where the table
# published 0.32 this morning. Three sittings of the same day moved that
# figure by a factor of eight, and none of them moved the wall by more than
# five per cent -- which is what a backend whose point is that the processor
# is free ought to be measured on, and is why the device table carries both.
#
# The device table in full at a load under 1.15: 0.417 s and 0.293 s for the
# twelve-token run, 0.063 s and 0.037 s evaluating, 0.348 s and 0.259 s
# generating, 0.405 s and 0.144 s for the hundred-and-ten-token prompt,
# 1.89 s and 0.04 s of processor.
#
# The engine, reference and drafting groups drifted more than the sittings
# before -- 1.411 s for the binary32 run against 1.300, 0.507 s at fifteen
# threads against 0.468, 1.782 s for the two-bit draft against 1.645. None
# of those paths changed; the machine had been running measurements for
# three hours by then, and the figures are recorded as taken rather than
# taken again on a colder machine, because the sitting is the unit.

# 2026-08-30, the fourth sitting: the rotation moved to the device, and the
# machine was faster than it had been all day.
#
# Everything moved, including llama.cpp: its processor row reads 388.6
# tokens a second at a hundred and ten against 351.7 three hours earlier,
# and 279.7 at 1419 against 273.6, on a binary nothing here touched. So the
# rates below are not comparable with this morning's and the ratios are.
# The device's gap on the long prompt is 1.81 and on a generated token 1.17,
# from 1.88 and 1.26.
#
# What the change is worth, alternated against the commit before it in one
# sitting: two per cent of the long prompt, three rounds each way, at the
# same digests. The published rate moved from 966.0 to 986.8 tokens a
# second, which is 2.2 -- the first time in four sittings that the published
# move and the measured one agree, and they agree because the machine held
# still for both halves of the pair rather than between the sittings.
#
# The device generating row reads 48.5 tokens a second against 44.5, which
# this change cannot have caused: it fires only for a batch, and a generated
# token is one position. The processor's own generating row moved with it,
# 35.1 against 33.5, which is what says so.
#
# The device table in full, at a load under 1.15: 0.394 s and 0.284 s for
# the twelve-token run, 0.057 s and 0.040 s evaluating, 0.336 s and 0.243 s
# generating, 0.375 s and 0.141 s for the hundred-and-ten-token prompt,
# 1.81 s and 0.04 s of processor.
#
# src/shaders/rotate.comp joins the device and outside groups' sources.

# 2026-08-30, the fifth sitting: a layer in one submission -- and the
# sitting before it published the sitting before that, which is corrected
# here.
#
# The bookkeeping first, because it is the more important of the two. The
# script that rewrites the published figures raised on the first anchor it
# could not find and wrote nothing; a second, smaller script then landed and
# updated the prose around the tables. So the rotation commit carried new
# fingerprints, this file's account of a fresh sitting, prose from that
# sitting, and tables from the one before it. `### Against llama.cpp` already
# records one commit that went wrong this way and it has now gone wrong
# twice, so the fix is not care: the script applies what matches, reports
# what does not, and always writes.
#
# The figures below are this sitting's, measured after the change, and the
# rotation sitting's are gone -- they were correct when taken and there is
# nowhere to put them that would not be a third set of numbers in a file
# whose whole point is that there is one.
#
# The machine was slower than the sitting before: llama.cpp reads 355.6
# tokens a second on the processor at a hundred and ten against 388.6, and
# 273.9 at 1419 against 279.7. The device's own prompt went the other way,
# 1030.5 against 986.8, and its gap there is 1.70 -- which is the change,
# and the alternated pair says three and a half per cent of it.
#
# The device table in full, at a load under 1.15: 0.414 s and 0.298 s for
# the twelve-token run, 0.060 s and 0.038 s evaluating, 0.353 s and 0.259 s
# generating, 0.446 s and 0.134 s for the hundred-and-ten-token prompt,
# 1.83 s and 0.03 s of processor.
#
# src/shaders/place.comp joins the device and outside groups' sources.

# 2026-08-30, the sixth sitting: the angles kept the way the activation is
# kept, and the whole layer paying for a generated token.
#
# Alternated against the commit before it: three and a half per cent of
# sixty-four generated tokens, three rounds each way, and level on both
# prompts. The published generating row moved 44.4 to 46.2 tokens a second,
# which is four -- close to the alternated figure for once, and llama.cpp's
# own generating rows repeat the sitting before to a tenth (39.7 against
# 39.6 on the processor, 56.4 against 56.1 on the device).
#
# The processor rows drifted the other way: 254.0 tokens a second at a
# hundred and ten against 258.2, and llama.cpp's 356.6 against 355.6, which
# is the same machine reading the same thing. The device's long prompt is
# 1059.7 against 1030.5 and its gap there 1.71 -- level, and this change was
# not expected to move it.
#
# The device table in full, at a load under 1.15: 0.426 s and 0.283 s for
# the twelve-token run, 0.062 s and 0.034 s evaluating, 0.351 s and 0.249 s
# generating, 0.400 s and 0.134 s for the hundred-and-ten-token prompt,
# 1.91 s and 0.03 s of processor.

# 2026-08-30, the width of an attending workgroup, and three attempts at
# the matrix product that were measured and not kept.
#
#   attention.comp is compiled a second time with -DWIDE beside -DSUBGROUPS,
#   which runs two hundred and fifty-six invocations to a workgroup rather
#   than sixty-four. The third compilation, the tiled one, is not widened
#   and neither is the first.
#
#   The compiler and flags, all twelve as 'tests shader' takes them:
#     glslang 15.1.0 (Ubuntu glslang-tools 15.1.0-2)
#     attention.comp      -V
#     attention.comp      -V --target-env vulkan1.1 -DSUBGROUPS -DWIDE
#     attention.comp      -V --target-env vulkan1.1 -DSUBGROUPS -DQUERY_TILE
#     combine.comp        -V
#     row_product.comp    -V
#     row_product.comp    -V -DSINGLE
#     half_batch.comp     -V
#     matrix_product.comp --target-env vulkan1.3
#     matrix_product.comp --target-env vulkan1.3 -DMORE_FORMATS
#     norm.comp           -V
#     rotate.comp         -V
#     place.comp          -V
#
#   What it is worth, alternated, three rounds of each, TinyLlama-1.1B Q8_0
#   on the device, medians of two:
#
#     64 generated after a 1419-token prompt
#       wide      1.595, 1.634, 1.603 s
#       narrow    1.717, 1.715, 1.692 s
#
#     the 1419-token prompt itself, both compilations widened
#       wide      1.712, 1.750, 1.740 s
#       narrow    1.631, 1.641, 1.635 s
#
#     64 generated after a six-token prompt, medians of three
#       wide      1.323, 1.349, 1.340 s
#       narrow    1.340, 1.352, 1.348 s
#
#   So it is worth six and a half per cent where the cache is long, level
#   where it is short, and costs six per cent on a prompt -- which is why
#   only the compilation a generated token enters is widened.
#
#   Digests unchanged: 1a26d24d33b8957b for the long prompt, 7ec6b755e53e16b4
#   for the same with sixty-four generated, 448c2ed68ec342ee for sixty-four
#   after a six-token prompt, cbf29ce484222325 for the 110-token prompt. The
#   sweep reads 28344 sequences, none outside tolerance, though the value a
#   lane carries is now four partial sums added at the end rather than one
#   running sum.
#
#   The figures re-measured for this, one sitting, each waiting for the
#   machine to fall below 1.20:
#
#     six-token prompt, 12 generated   cpu 0.434 s wall, 0.067 and 0.368,
#                                      1.94 s processor
#                                      device 0.274 s wall, 0.035 and 0.240,
#                                      0.09 s processor
#     110-token prompt, none generated cpu 0.428 s, 2.62 s processor
#                                      device 0.138 s, 0.03 s processor
#     1419-token prompt                cpu 6.285 s, device 1.284 s
#     64 generated, six-token prompt   cpu 1.905 s, device 1.333 s
#     llama-bench 95b8e33e1, -r 3      pp110 351.74 +/- 22.52 --device none,
#                                      1499.15 +/- 142.67 -ngl 99
#                                      pp1419 271.14 +/- 0.95 --device none,
#                                      1802.17 +/- 9.87 -ngl 99
#                                      tg64 39.24 and 55.90
#                                      pp110 -ngl 0 with a device present
#                                      707.47, which is why the command
#                                      names --device none
#
#   And three changes to the matrix product, each measured and none kept.
#   They are written down because each of them was argued for from
#   arithmetic that turned out to be wrong, and the arithmetic is the part
#   worth keeping.
#
#   First, where a device prompt goes, by removing one piece at a time.
#   1419 tokens, median of three, baseline 1.282 s:
#
#     weight staging replaced by a constant       1.082 s
#     that and the activation loads as well       0.983 s
#     thirty-one of every thirty-two coopMatMulAdd 0.916 s
#
#   So the matrix instruction is 0.366 s of 1.282, twenty-nine per cent. A
#   kernel whose multiplies were free would read 0.92 s where llama.cpp
#   reads 0.74.
#
#   The packed half-precision dot product, v_dot2_f32_f16, reached through
#   a SPIR-V intrinsic exactly as llama.cpp reaches it, in place of the
#   cooperative matrix: 1.806, 1.773, 2.066 s against 1.280, 1.295, 1.328.
#   Thirty-nine per cent slower. The disassembly holds 2048
#   v_dot2acc_f32_f16 in one run with no other arithmetic between them, 144
#   VGPRs against 256 and six subgroups a SIMD against four -- better on
#   every static measure. And llama.cpp does not owe its speed to it either:
#   GGML_VK_DISABLE_COOPMAT=1 reads 1418 t/s against 1408 with it, so both
#   its paths land in the same place.
#
#   A taller tile, 128 rows over four subgroups with the activations staged
#   in shared memory: 2.665 s. The argument was that a 32-row workgroup
#   re-reads its half-megabyte activation tile 64 times for a 2048-column
#   matrix -- 33 MB against 4.5 MB of weights -- and it was wrong because
#   those re-reads are L2 hits, not memory.
#
#   A wider tile, 512 vectors over four subgroups sharing one decoded copy
#   of the weights, the step deepened to 128 columns so every invocation
#   decodes: 1.624 s, at the same 256 VGPRs and the same four subgroups a
#   SIMD. 512 vectors is a two-megabyte activation tile, which is the whole
#   L2. Batch width alone is neutral: 1.319, 1.261, 1.336 s at 128, 512 and
#   256, because a workgroup re-reads the weights whatever the batch is.
#
#   32 rows by 128 vectors is where this part's cache hierarchy puts the
#   optimum, and the matrix product is not where the remaining gap is.
#
# 2026-08-31, a prompt attends through the matrix instruction, on a cache
# kept in half precision.
#
#   src/shaders/attention_matrix.comp, a fourth attending kernel, compiled
#   with -V --target-env vulkan1.3 as the matrix product is. The twelve
#   compilations 'tests shader' takes, in order:
#     glslang 15.1.0 (Ubuntu glslang-tools 15.1.0-2)
#     attention.comp        -V
#     attention.comp        -V --target-env vulkan1.1 -DSUBGROUPS -DWIDE
#     attention.comp        -V --target-env vulkan1.1 -DSUBGROUPS
#                           -DQUERY_TILE
#     combine.comp          -V
#     row_product.comp      -V
#     row_product.comp      -V -DSINGLE
#     half_batch.comp       -V
#     matrix_product.comp   --target-env vulkan1.3
#     matrix_product.comp   --target-env vulkan1.3 -DMORE_FORMATS
#     attention_matrix.comp -V --target-env vulkan1.3
#     norm.comp             -V
#     rotate.comp           -V
#     place.comp            -V
#
#   Alternated, three rounds each, medians of three, 1419-token prompt on
#   the device:
#     matrix attention   1.222, 1.236, 1.254 s
#     scalar attention   1.333, 1.319, 1.323 s
#   Every matrix run faster than every scalar one.
#
#   What it took to get there, all measured, the first two not kept:
#     staged from the binary32 cache, one pass          1.535 s
#     staged, two passes so nothing is ever rescaled    1.785 s
#     half-precision cache, read where it lies          1.236 s
#   The two staged forms differ by one more staging of the keys and one
#   more score product, a quarter of a second, which is what said the
#   staging was the cost rather than the arithmetic.
#
#   And two shapes inside the kept kernel:
#     row reductions clustered over half a subgroup     1.236 s
#     one invocation to a row, walking its columns      1.606 s
#
#   The cache is reserved at six bytes an element rather than four -- the
#   binary32 cache and a half-precision copy after it -- and is zeroed when
#   it is taken, because the kernel reads a whole tile of positions whether
#   or not every one has been written and a weight of zero against a
#   not-a-number is a not-a-number.
#
#   A push constant was not added for where the copy begins. Widening the
#   attention block from sixty-four bytes to sixty-eight, or to seventy-two,
#   changed what the device computed -- 1a26d24d33b8957b became
#   c5d5de3282b2f709 with the new kernel not even bound -- on a part whose
#   maxPushConstantsSize is 256. That is not understood and is written down
#   rather than explained. What is pushed instead is the key and value bases
#   already pointing into the copy, decided at the call site by which kernel
#   is about to be bound, which needs no new field.
#
#   Correctness: the sweep reads 28344 sequences, none outside tolerance,
#   with half-precision keys and values. Digests held: 1a26d24d33b8957b for
#   the long prompt, cbf29ce484222325 for the 110-token one,
#   448c2ed68ec342ee for sixty-four generated, 5abff916f9d83ca6 for twelve,
#   33f48397f89839f6 and 3248ac1bb7011de0 on the processor.
#
#   Why attention and not the matrix product, from llama.cpp's own profiler
#   (GGML_VK_PERF_LOGGER=1, build 95b8e33e1), microseconds per position per
#   layer, theirs at a 512-position ubatch and ours at 128:
#     attention              ours 6.12   theirs 2.85
#     five matrix products   ours 22.5   theirs 21.6
#     everything else        ours  3.3   theirs  3.1
#   Their 2048x512x2048 product runs at 4154 gigaflops and ours at 4050, so
#   the matrix product was already level and attention was fifty-two per
#   cent of the remainder. GGML_VK_DISABLE_COOPMAT=1 takes their attention
#   from 2945 gigaflops to 1610 and pp1419 from 1744 t/s to 1240 -- which
#   also corrects an earlier reading of that flag taken at pp512, where it
#   showed 1408 against 1418 and was taken to mean the instruction was not
#   what made them fast.
#
#   The figures re-measured for this, one sitting, each waiting for the
#   machine to fall below 1.15:
#
#     six-token prompt, 12 generated   cpu 0.405 s wall, 0.061 and 0.343,
#                                      1.83 s processor
#                                      device 0.267 s wall, 0.035 and 0.231,
#                                      0.08 s processor
#     110-token prompt, none generated cpu 0.441 s, 2.71 s processor
#                                      device 0.131 s, 0.02 s processor
#     1419-token prompt                cpu 5.957 s, device 1.225 s
#     64 generated, six-token prompt   cpu 1.859 s, device 1.353 s
#     llama-bench 95b8e33e1, -r 3      pp110 352.40 +/- 32.21 --device none,
#                                      1584.49 +/- 16.77 -ngl 99
#                                      pp1419 271.29 +/- 0.38 --device none,
#                                      1759.14 +/- 2.81 -ngl 99
#                                      tg64 39.38 and 55.79
#                                      pp110 -ngl 0 with a device present
#                                      734.18
#
# 2026-08-31, sixteen query rows to an attending workgroup, and two shapes
# measured against it and not kept.
#
#   attention_matrix.comp's BR and the engine's Matrix_Queries go from
#   thirty-two to sixteen together. Alternated, three rounds, medians of
#   three, 1419-token prompt on the device:
#     sixteen rows   1.212, 1.207, 1.221 s
#     thirty-two     1.236, 1.261, 1.237 s
#   Sixteen every round. The kernel goes from 256 VGPRs and four subgroups
#   a SIMD to 96 and ten, and its shared memory from 14336 bytes to 7168.
#   Sixteen is the floor: it is the instruction's own tile.
#
#   Not kept, both aimed at the workgroup count of the matrix product,
#   which follows the rows -- 176 workgroups for a feed-forward arm, 64 for
#   a projection, 8 for the keys and values, against twenty-four SIMDs.
#   Their rates, from the per-step timestamps: 4390 and 4570, 4050 and
#   3814, and 1040 gigaflops.
#
#     the narrow products sent to the row product instead, which
#     dispatches thirty-two workgroups for two hundred and fifty-six rows:
#       row product   1.246, 1.246, 1.253 s
#       matrix        1.234, 1.213, 1.223 s
#
#     a workgroup's rows split between two subgroups, sixteen each, with
#     the step deepened to sixty-four columns so that all hundred and
#     twenty-eight invocations decode: 1.360 s. Twice the waves for the
#     same workgroups and half the accumulators each, and slower. That is
#     the third workgroup of more than one subgroup to lose in this kernel,
#     after the taller tile and the wider one; its barriers are free at one
#     subgroup and are not at two.
#
#   What is left untried is a split of the columns across workgroups, each
#   accumulating a partial answer, added up in a pass of their own -- the
#   only way at the workgroup count that does not put two subgroups behind
#   one barrier.
#
#   The figures re-measured, one sitting, each waiting for the machine to
#   fall below 1.15 (the two device runs re-taken after the processor runs
#   drove the load up):
#
#     six-token prompt, 12 generated   cpu 0.425 s wall, 0.067 and 0.356,
#                                      1.91 s processor
#                                      device 0.274 s wall, 0.033 and 0.242,
#                                      0.09 s processor
#     110-token prompt, none generated cpu 0.443 s, 2.68 s processor
#                                      device 0.129 s, 0.03 s processor
#     1419-token prompt                cpu 6.272 s, device 1.188 s
#     64 generated, six-token prompt   cpu 1.922 s, device 1.321 s
#     llama-bench 95b8e33e1, -r 3      pp110 374.41 +/- 6.52 --device none,
#                                      1659.03 +/- 59.53 -ngl 99
#                                      pp1419 272.53 +/- 5.64 --device none,
#                                      1817.90 +/- 9.49 -ngl 99
#                                      tg64 39.68 and 55.77
#                                      pp110 -ngl 0 with a device present
#                                      705.44
#
#   Digests held throughout: 1a26d24d33b8957b, cbf29ce484222325,
#   448c2ed68ec342ee, 5abff916f9d83ca6, 33f48397f89839f6, 3248ac1bb7011de0.
#   The sweep reads 28344 sequences, none outside tolerance.
#
# 2026-08-31, the split, measured in the cheap form first and not built in
# the expensive one.
#
#   The workgroup count of the matrix product follows the rows, and the
#   narrow products make too few: eight for a two-hundred-and-fifty-six-row
#   projection over a batch of a hundred and twenty-eight, on a part with
#   twenty-four SIMDs, running at 1040 gigaflops where a feed-forward arm's
#   hundred and seventy-six run at 4390. The fix proposed was to split the
#   columns across workgroups and add the partial answers up in a pass of
#   their own.
#
#   Before building that, the same occupancy was bought a cheaper way: a
#   third compilation of matrix_product.comp with NARROW, taking
#   thirty-two vectors to a workgroup instead of a hundred and
#   twenty-eight. That is four times the workgroups for the same rows --
#   eight becomes thirty-two -- at the price of decoding the weights four
#   times over, which for a half-megabyte matrix is nothing.
#
#   Alternated, three rounds, medians of three, 1419-token prompt:
#     narrow tile for the keys and values   1.192, 1.191, 1.206 s
#     wide tile for everything              1.193, 1.193, 1.215 s
#   Level. Quadrupling the workgroups moved nothing measurable.
#
#   The pipeline was live: bound for every product instead of only the
#   narrow ones, the same prompt reads 1.709 s, which is the four-fold
#   decode on the wide matrices and proof the selection works.
#
#   So the workgroup count is not what binds those products, and the
#   column split is not worth building. What it would add over the tile
#   measured here is the decode it does not repeat -- three extra passes
#   over half a megabyte a layer, 360 MB across the prompt, about six
#   thousandths of a second of eleven hundred and ninety. Against that: a
#   scratch region of rows by vectors by splits, a combining pass, and the
#   engine plumbing for both. Not built, and this is why.
#
#   Nothing kept, nothing restamped: the tree is as commit a25f993 left it.
#
# 2026-08-31, the prompt's time accounted against llama.cpp's own
# accounting, and their narrow-shape configuration matched.
#
#   Per-step GPU timestamps again, a top-of-pipe at the command buffer's
#   start and a bottom-of-pipe after each of the seventeen steps and at the
#   end, read every eighth submission so that the read does not stall each
#   one. Thirty-three batched samples spread over the prompt.
#
#   Per layer-batch of a hundred and twenty-eight positions, milliseconds:
#     down projection      0.785      gate                 0.695
#     up                   0.665      attention            0.505
#     query projection     0.271      output projection    0.266
#     key                  0.132      value                0.131
#     gate combine         0.133      the two norms        0.126
#     the other two joins  0.117      rotate               0.042
#     the two cache writes 0.014
#     total                3.882
#
#   Which is 30.3 microseconds a position a layer, against llama.cpp's
#   25.55 from their own logger. Attention is 3.95 now where it was 6.12
#   before the half-precision cache and the sixteen-row tile.
#
#   And the accounting, which is the point of taking these:
#     span inside Submit_And_Wait     93.7 per cent of the wall there
#     the seventeen steps summed      1025 ms across the prompt
#     the prompt itself               1190 ms
#   So eighty-six per cent of a device prompt is inside a dispatch.
#   llama.cpp's logger reads 784 ms of GPU against an 813 ms prompt --
#   ninety-six. About a hundred and sixty-five milliseconds of ours, a
#   seventh of the prompt, is somewhere no kernel change can reach, where
#   theirs has thirty.
#
#   Their configuration for the narrow shape, read out of
#   ggml_vk_guess_matmul_pipeline and ggml_vk_guess_split_k rather than
#   guessed: the tile is chosen by shape -- m or n at most 32 takes a
#   32x32 tile, at most 64 a 64x64, otherwise 128x128 with a hundred and
#   twenty-eight invocations -- and split_k is taken only where k is at
#   least 2048 and the tiles are at most two thirds of the core count. Run
#   over this model's shapes that gives split_k of one everywhere except
#   the keys and values, where it gives three.
#
#   Matched here as far as the shape allows -- a compilation with two
#   subgroups to a workgroup, sixteen rows each, the step deepened to
#   sixty-four columns so that all hundred and twenty-eight invocations
#   decode, bound only where the rows are under five hundred and twelve:
#     paired for the keys and values   1.195, 1.181, 1.203 s
#     one wave everywhere              1.199, 1.191, 1.232 s
#   Faster in each of three rounds and by three parts in a thousand. Not
#   kept: a third pipeline and a fourth compilation for that is not a
#   trade, and it settles that the narrow products are not where the
#   remaining gap is. The earlier reading of the same idea applied to every
#   product measured 1.360 s, and a narrower vector tile measured level;
#   this is the third and last of that family.
#
#   Nothing kept, nothing restamped: the tree is as commit 2811c4d left it.
#
# 2026-08-31, the rotation table tabulated once a batch, and the sitting
# that five groups needed for it.
#
#   Rotary_Table is called once a position a layer and works out a cosine
#   and a sine a pair in binary64. It does not depend on the layer:
#   everything it reads is fixed for one call except the rotation base, and
#   Turn_Base states one base unless the architecture states a local one
#   for windowed layers. Cached on the base; a reallocation clears it.
#
#   Alternated, three rounds, medians of three, 1419-token prompt:
#     tabulated once a batch    1.123, 1.139, 1.131 s   device
#     once a layer              1.209, 1.184, 1.210 s
#     tabulated once a batch    6.234 s                 processor
#     once a layer              6.331 s
#   The slowest cached device run is faster than the fastest uncached one.
#
#   How it was found, which is the part worth keeping. Per-step GPU
#   timestamps summed to 1025 ms of a 1190 ms prompt -- eighty-six per cent
#   of a device prompt inside a dispatch, where llama.cpp's own logger
#   accounts for ninety-six, 784 ms of an 813 ms prompt. Timing Run from
#   the caller's side split the remainder, with the one-time weight upload
#   netted out:
#     waiting on the device                1093 ms
#     inside Run and not waiting              40 ms
#       (recording 10, activation over 9, answers back 21)
#     outside Run altogether                 190 ms
#   Three quarters of a millisecond a layer of host work, and most of it was
#   the table. What is left outside a dispatch is a little over a hundred
#   milliseconds: the keys and values copied into the host cache, the
#   sequence built a layer at a time, the residual bookkeeping. None of
#   those is measured apart yet.
#
#   Five groups list model_runner-llama.adb and all five moved. One sitting,
#   each run waiting for the machine to fall below 1.10:
#
#     twelve tokens, six-token prompt  cpu 0.428 s wall, 0.065 and 0.355,
#                                      1.92 s processor
#                                      device 0.277 s wall, 0.037 and 0.246,
#                                      0.09 s processor
#     the same at --arith f32          1.318 s, 9.91 s processor
#     110-token prompt                 cpu 0.435 s, 2.59 s processor
#                                      device 0.123 s, 0.02 s processor
#     1419-token prompt                cpu 6.261 s, device 1.141 s
#     64 generated, six-token prompt   cpu 1.938 s, device 1.327 s
#     four tokens, cpu                 0.062 s prompt, 0.117 s generating
#     four tokens, reference           5.911 s prompt, 3.935 s generating
#     the two-bit draft model          1.833 s twelve tokens
#     drafted, --draft-tokens 4        3.921 s, 24 proposed, 7 accepted
#     --batch-size 64/128/256/512/1024 1.828, 1.137, 1.156, 1.159, 1.154 s
#                                      device, 1419-token prompt
#     llama-bench 95b8e33e1, -r 3      pp110 353.27 +/- 37.36 --device none,
#                                      1553.38 +/- 58.68 -ngl 99
#                                      pp1419 272.60 +/- 6.43 --device none,
#                                      1760.51 +/- 8.38 -ngl 99
#                                      tg64 39.08 and 55.77
#                                      pp110 -ngl 0 with a device present
#                                      690.16
#
#   The worker-count and share-count comparisons the engine group also
#   covers were NOT re-taken. They are 'tests benchmark' against the worker
#   pool and a --threads pair against the device, and this change is a table
#   of cosines in the batched evaluator: it is not reached by either. The
#   same reasoning as 2026-08-11 above, and written down for the same
#   reason.
#
#   Digests held throughout: 33f48397f89839f6, 5abff916f9d83ca6,
#   cbf29ce484222325, 448c2ed68ec342ee, 3248ac1bb7011de0, 1a26d24d33b8957b,
#   5f85f06494f88de4, 7fbdc947563a2fde. The sweep reads 28344 sequences,
#   none outside tolerance.
#
# 2026-08-31, the rest of the host time, accounted and not found in one
# place.
#
#   After the rotation table was cached, the prompt was taken apart again,
#   this time from the caller's side down. Probes at three levels: the
#   layer loop in the batched evaluator, Whole_Layer around its call to
#   Products.Run, and Run around its wait. One 1419-token device prompt,
#   --repeats 1, so the figures carry the one-time weight upload of about
#   a third of a second inside Run.
#
#     the prompt                             1.500 s
#     the layer loop, twelve batches of
#       twenty-two layers                    1.458
#         before the device call (pre)         0.026
#         inside Whole_Layer                   1.407
#           building the seventeen steps         0.007
#           inside Run                           1.386
#           the answers out of it                0.013
#         after it (post)                      0.025
#     outside the layer loop                 0.043
#
#   And inside Run, from the probe of the sitting before: the wait 1.093,
#   the command buffer recorded 0.010, the activation over 0.009, the
#   answers back 0.021, and the first-touch upload the rest.
#
#   So with the upload netted out, of a prompt of about 1.17 s the host
#   spends something like a hundred and ten to a hundred and thirty
#   milliseconds outside the wait, and it is in five places with nothing
#   above forty-five:
#
#     outside the layer loop        43 ms   the embedding copied in, the
#                                           last position projected out,
#                                           the batch set up
#     the answers back              21 ms   a megabyte a layer out of the
#                                           mapped result buffer
#     after the device call         25 ms   the keys and values into the
#                                           host's own cache
#     the command buffer recorded   10 ms
#     the activation over            9 ms
#     building the seventeen steps    7 ms
#     before the device call          3 ms  (twenty-three more on the first
#                                           batch, which is the table's
#                                           allocation and its first touch)
#
#   The sequence building was the thing worth suspecting and it is seven
#   milliseconds. There is no lever left here: the largest piece is the
#   work between batches, and the next is the megabyte a layer that comes
#   back because a layer's answer is handed to the host before the next
#   layer sends it again. Removing that one is the activation kept on the
#   device across layers, which was costed earlier in this file at about
#   five per cent and needs the submissions chained behind a semaphore.
#
#   Nothing kept, nothing restamped: the tree is as commit d9a3085 left it.
#
# 2026-08-31, the activation kept on the device between layers.
#
#   A layer's answer is the next layer's activation. It went to the host and
#   came back: 21 ms a prompt out of the mapped result buffer and 9 over
#   again, which the accounting of the sitting before priced at two to three
#   per cent.
#
#   Alternated, three rounds, medians of three, 1419-token device prompt:
#     carried        1.097, 1.105, 1.108 s
#     host-borne     1.134, 1.125, 1.137 s
#   Two and six tenths per cent, and the slowest carried run is faster than
#   the fastest uncarried one.
#
#   Written first with a copy -- a transfer command from the last step's
#   room to the front of the result buffer, with a barrier on each side --
#   it measured 1.116 against 1.130, one and two tenths per cent. Half the
#   gain went on two pipeline drains a layer and a megabyte the device had
#   no reason to move. Placing the last step at the front instead, so the
#   device writes the answer where the next layer reads it, is what the
#   figures above are. Safe because the only step that reads that room is
#   the first residual join, which a barrier publishes long before the last
#   step runs.
#
#   A layer carries out only where the next layer will be taken whole as
#   well: one that falls back reads the host's copy of the activation, and
#   the host's copy is what carrying does not write. The condition that
#   decides whether the device takes a layer is a function now and is asked
#   of the next layer first.
#
#   Five groups list the three files that changed. One sitting, each run
#   waiting for the machine to fall below 1.10:
#
#     twelve tokens, six-token prompt  cpu 0.421 s wall, 0.063 and 0.359,
#                                      1.91 s processor
#                                      device 0.277 s wall, 0.032 and 0.245,
#                                      0.07 s processor
#     the same at --arith f32          1.417 s, 10.27 s processor
#     110-token prompt                 cpu 0.423 s, 2.58 s processor
#                                      device 0.120 s, 0.02 s processor
#     1419-token prompt                cpu 6.168 s, device 1.095 s
#     64 generated, six-token prompt   cpu 1.942 s, device 1.325 s
#     four tokens, cpu                 0.064 s prompt, 0.118 s generating
#     four tokens, reference           5.894 s prompt, 3.933 s generating
#     the two-bit draft model          1.967 s twelve tokens
#     drafted, --draft-tokens 4        3.874 s, 24 proposed, 7 accepted
#     --batch-size 64/128/256/512/1024 1.846, 1.138, 1.124, 1.125, 1.150 s
#     llama-bench 95b8e33e1, -r 3      pp110 353.38 +/- 34.55 --device none,
#                                      1581.44 +/- 61.45 -ngl 99
#                                      pp1419 273.10 +/- 7.46 --device none,
#                                      1761.38 +/- 2.64 -ngl 99
#                                      tg64 39.42 and 56.17
#                                      pp110 -ngl 0 with a device present
#                                      746.70
#
#   The worker-count and share-count comparisons were not re-taken, for the
#   reason given in the entry above: they measure the worker pool and a
#   --threads pair, and neither reaches this.
#
#   Digests held: 33f48397f89839f6, 5abff916f9d83ca6, cbf29ce484222325,
#   448c2ed68ec342ee, 3248ac1bb7011de0, 1a26d24d33b8957b, 5f85f06494f88de4,
#   7fbdc947563a2fde. The sweep reads 28344 sequences, none outside
#   tolerance.
#
#   What is left is the fence a layer still waits on. The host wants nothing
#   from a carried layer but its keys and values, which it copies into its
#   own mirror of the cache; if that mirror went the submissions could chain
#   and the 68 ms a prompt spends between submit and wake would go with it.
#   That is a change to what a session guarantees.
#
# 2026-08-31, the host's copy of the cache filled once a batch instead of
# once a layer -- built, correct, and level.
#
#   The keys and the values a layer writes came back through the result
#   buffer, step by step, because the host keeps its own copy of the cache
#   for a session that later runs on the processor. They are already in the
#   cache the device holds, so Get_Cache was added beside Put_Cache, the two
#   steps stopped being kept, and the copy was made once at the end of a
#   batch out of the device's own cache instead.
#
#   Alternated, three rounds, medians of three, 1419-token device prompt:
#     filled once a batch   1.110, 1.103, 1.105 s
#     filled once a layer   1.104, 1.106, 1.107 s
#   Level. Digest 1a26d24d33b8957b throughout.
#
#   It was never going to pay on its own: it moves a copy rather than
#   removing one, and the copy was not what a layer waited for. What it was
#   for is the wait -- with the answer carried on the device and the keys
#   and values read out afterwards, a layer needs nothing from the device
#   at all, and the sixty-eight milliseconds a prompt spends between submit
#   and wake could go.
#
#   That needs the engine doubled, and this is where it stops. Submissions
#   on one queue can be made to execute in order, so the result buffer is
#   safe; what is not safe is that the host writes descriptor sets and
#   records a command buffer between layers, and neither may be touched
#   while a submission using them is still executing. So chaining needs two
#   command buffers, two fences and two sets of descriptor sets -- the
#   engine has one of each. Reverted rather than kept: it is complexity
#   with nothing measurable behind it until that is done, and it is written
#   here so that it is not re-derived.
#
#   Nothing kept, nothing restamped: the tree is as commit 3857d10 left it.
#
# 2026-08-31, two of everything a submission holds, so a layer does not
# wait for its own work.
#
#   Alternated, three rounds, medians of three, 1419-token device prompt:
#     chained    1.038, 1.041, 1.054 s
#     waiting    1.113, 1.117, 1.113 s
#   Six and a half per cent, and the slowest chained run is faster than the
#   fastest waiting one. It recovers what the accounting two sittings ago
#   priced: ninety-four per cent of a layer's wait was the device computing
#   and six was the gap around it, sixty-eight milliseconds a prompt.
#
#   Two command buffers, two fences, two arrays of descriptors, and one
#   semaphore. The rules each of those answers:
#
#     A command buffer may not be re-recorded while it executes and
#     descriptors may not be written while a submission reads them. Swapped
#     each sequence; the slot taken up is waited for first, which is the
#     sequence before the previous one.
#
#     Two submissions to one queue are not ordered against each other
#     unless made to be, and the second reads what the first left at the
#     front of the result buffer. One semaphore, waited on and signalled by
#     every submission: a binary semaphore may not be signalled while
#     already signalled, and a signal nothing waits for -- the last
#     sequence of a batch -- leaves it that way. Two semaphores deadlocked
#     seven device tests with BACKEND_DEVICE_STALLED before that was
#     understood, and the armed flag left set across a close deadlocked
#     them again: a new engine's semaphore has signalled nothing.
#
#     A matrix the sequence in flight is reading may not be given back, so
#     the eviction floor is one clock lower where there is one, and a
#     sequence that borrowed anything waits before giving it back.
#
#     Anything the host writes that a submission may read -- the activation
#     sent over, a buffer grown, an engine closing, the two other paths
#     that record into the same buffer -- settles everything in flight
#     first.
#
#   Run waits only where a step is kept, a matrix was borrowed, or the
#   answer is the last one. In a twenty-two layer batch that is the last
#   layer, which is why this needed the answer carried and the keys and
#   values read out of the device's own cache first: both are in place.
#
#   293 tests pass. The sweep reads 28344 sequences, none outside
#   tolerance, none unlearned. Digests held: 33f48397f89839f6,
#   5abff916f9d83ca6, cbf29ce484222325, 448c2ed68ec342ee,
#   3248ac1bb7011de0, 1a26d24d33b8957b, 5f85f06494f88de4,
#   7fbdc947563a2fde.
#
#   Five groups moved. One sitting, each run waiting for the machine to
#   fall below 1.10:
#
#     twelve tokens, six-token prompt  cpu 0.417 s wall, 0.061 and 0.355,
#                                      1.91 s processor
#                                      device 0.279 s wall, 0.034 and 0.246,
#                                      0.09 s processor
#     the same at --arith f32          1.383 s, 10.15 s processor
#     110-token prompt                 cpu 0.427 s, 2.59 s processor
#                                      device 0.113 s, 0.02 s processor
#     1419-token prompt                cpu 6.218 s, device 1.048 s
#     64 generated, six-token prompt   cpu 1.928 s, device 1.343 s
#     four tokens, cpu                 0.066 s prompt, 0.119 s generating
#     four tokens, reference           5.900 s prompt, 3.939 s generating
#     the two-bit draft model          1.790 s twelve tokens
#     drafted, --draft-tokens 4        3.959 s, 24 proposed, 7 accepted
#     --batch-size 64/128/256/512/1024 1.729, 1.065, 1.047, 1.073, 1.061 s
#     llama-bench 95b8e33e1, -r 3      pp110 347.10 +/- 37.23 --device none,
#                                      1617.30 +/- 38.42 -ngl 99
#                                      pp1419 271.59 +/- 5.48 --device none,
#                                      1760.88 +/- 6.79 -ngl 99
#                                      tg64 39.01 and 56.11
#                                      pp110 -ngl 0 with a device present
#                                      691.53
#
#   The conformance figures under "An independent implementation" were
#   re-taken as well: they had gone out of date when the cache became half
#   precision and nothing had said so, because the gate reports the sweep's
#   verdict and the sweep's own command is what compares its numbers with
#   the README's. Worst absolute 2.24e-05 against 1.75e-05 published, and
#   the four other buckets by less.
#
#   The worker-count and share-count comparisons were not re-taken, for the
#   reason given twice above.
#
# 2026-08-31, the per-step table retaken after the three changes that
# invalidated it, and two mechanisms for the down projection eliminated.
#
#   Per-step GPU timestamps again, 264 layer-batches of a 1419-token
#   prompt, span 1.038 s. Per position per layer, against llama.cpp's own
#   logger at a 512-position ubatch:
#
#                          ours   theirs   gap
#     down projection      6.27     4.95   +1.32
#     gate and up         10.76     9.58   +1.18
#     key and value        2.08     0.97   +1.11
#     attention            3.95     2.85   +1.10
#     the two norms        1.02     0.63   +0.39
#     query and output     4.25     4.04   +0.21
#     the three joins      1.94     2.07   -0.13
#     rotate and cache     0.45     0.46   -0.01
#     total               30.72    25.55
#
#   As rates: gate and up 4.20 and 4.38 teraflops, query and output 3.92
#   and 3.97, down 3.68, key and value 1.01. Theirs: 4.82 on gate and up,
#   4.15 on query and output, 4.66 on down, 2.16 on key and value.
#
#   So down is the largest single gap and the shape of it is peculiar:
#   on identical flop counts llama.cpp is FASTER on the deep matrix than on
#   the square ones, and this program is slower. Depth helps them and hurts
#   us.
#
#   Two mechanisms tested and both eliminated:
#
#     The activation reads. Replaced with a constant, down falls 22 per
#     cent and gate 20 -- proportionally the same, so the deep matrix does
#     not pay more for reading its activations. With them free, down still
#     reads 4.69 teraflops against gate's 5.27.
#
#     The iteration count. Down walks 176 steps of thirty-two columns where
#     gate walks 64, so the step was deepened to sixty-four -- the decode
#     run twice over, every format reading the same. Everything got worse:
#     span 1.120 s against 1.038, gate up ten per cent, down up seven.
#     Fewer, deeper steps are not what this kernel wants.
#
#   What is left is the workgroup count -- down makes sixty-four of them
#   against a feed-forward arm's hundred and seventy-six, on a part with
#   ninety-six wave slots -- and that is the hypothesis this file has
#   already recorded four measurements against, in other steps. It cannot
#   be tested for down without also multiplying its weight decode by two or
#   four, which is a fifth to three fifths of the step, so the test would
#   not answer the question. Written down rather than guessed at.
#
#   Nothing kept, nothing restamped: the tree is as commit 303e81b left it.
#
# 2026-08-31, llama.cpp's mul_mm read for the deep matrix, and the one
# thing it does that this does not, measured three ways and not kept.
#
#   Their k loop is: load A into shared memory, load B into shared memory,
#   barrier, compute, barrier. Two barriers a block, as here, and a block
#   of sixteen columns against this program's thirty-two -- so for a
#   five-thousand-six-hundred-column matrix they take three hundred and
#   fifty-two turns where this takes a hundred and seventy-six, and they
#   are the faster of the two. That kills the iteration-count explanation
#   from the entry above twice over.
#
#   The structural difference is B. They stage both operands and read both
#   out of shared memory at a stride of their own choosing; this reads the
#   activation where it lies, column major at the matrix's own column
#   count -- four kilobytes between one vector and the next for a
#   two-thousand-column matrix and eleven for a five-thousand-six-hundred
#   one. That is a real asymmetry and it shows: with the activation reads
#   replaced by a constant the down projection falls twenty-two per cent
#   and a feed-forward arm twenty, on identical element counts.
#
#   So the activation was staged here too, with the tile and the single
#   subgroup left exactly as they are -- which is what the taller-tile
#   attempt earlier in this file confounded. Three ways of writing the
#   staging loop, 1419-token device prompt against 1.042 s as it stands:
#
#     a vector to an invocation, each walking its own thirty-two
#     components -- sixty-four runs a column count apart      1.982 s
#     thirty-two invocations to a half, one component each,
#     one cache line a half                                   1.809 s
#     four invocations to a vector, eight components a read   1.688 s
#
#   Coalescing it properly is worth three tenths of a second and it is
#   still sixty-two per cent slower than not staging at all. Reading the
#   operand where it lies beats copying it first on this part, whatever the
#   stride, and that is why the two kernels are different shapes rather
#   than one of them being behind on a detail.
#
#   The down projection is therefore still unexplained. Eliminated so far:
#   the activation reads (proportional to a shallow matrix's), the
#   iteration count (deeper steps are worse, and their kernel takes twice
#   as many), and staging (three ways, all much worse). What is left is the
#   workgroup count, which cannot be tested here without multiplying the
#   weight decode.
#
#   Nothing kept, nothing restamped: the tree is as commit 2b4b6ca left it.
#
# 2026-08-31, sixty-four cached positions to an attending tile, and the
# rest of that kernel priced.
#
#   attention_matrix.comp's BC goes from thirty-two to sixty-four. The
#   softmax gave each invocation one column of its row, which is what a
#   tile as wide as half a subgroup allows; it takes BC/32 of them now, and
#   at thirty-two that is the same code -- measured neutral, 1.038 s.
#
#   Alternated, 1419-token device prompt, medians of three:
#     sixty-four      1.025, 1.032, 1.033, 1.046 s
#     thirty-two      1.041, 1.043, 1.046 s
#   Never behind in four rounds, ahead in three, and worth about one per
#   cent. A hundred and twenty-eight reads 1.058: the scores of a tile live
#   in shared memory, and BR by BC floats doubled costs more occupancy than
#   the halved tile count buys.
#
#   And the rest of the kernel priced, so that it is not guessed at again.
#   Against 1.045 s:
#     the round trip the weighted values make through shared memory,
#       taken out entirely                                   1.040 s
#   Half a per cent. That is the one thing the two-pass form removes -- the
#   form that walks the tiles once for each row's largest score and once to
#   weigh against it, so that nothing is rescaled and the answer stays in
#   the cooperative matrices. It was built earlier in this file and measured
#   1.785 s against 1.535 when staging still dominated; with the staging
#   gone it would still lose, because what it removes is half a per cent and
#   what it adds is a second pass over the scores.
#
#   So attention is 3.95 microseconds a position a layer against llama.cpp's
#   2.85 and no piece of the difference is in an overhead this kernel can be
#   rid of.
#
#   Two groups moved -- the shader digest reaches the device and outside
#   figures through model_runner-shaders.ads and nothing else. One sitting,
#   each run waiting for the machine to fall below 1.10:
#
#     six-token prompt, 12 generated   cpu 0.422 s wall, 0.060 and 0.355,
#                                      1.91 s processor
#                                      device 0.273 s wall, 0.032 and 0.242,
#                                      0.09 s processor
#     110-token prompt                 cpu 0.414 s, 2.57 s processor
#                                      device 0.116 s, 0.02 s processor
#     1419-token prompt                cpu 6.316 s, device 1.040 s
#     64 generated, six-token prompt   cpu 1.922 s, device 1.333 s
#     llama-bench 95b8e33e1, -r 3      pp110 368.59 +/- 8.00 --device none,
#                                      1614.44 +/- 95.69 -ngl 99
#                                      pp1419 270.69 +/- 5.67 --device none,
#                                      1764.85 +/- 2.31 -ngl 99
#                                      tg64 39.56 and 55.95
#                                      pp110 -ngl 0 with a device present
#                                      704.76
#
#   Digests held: 33f48397f89839f6, 5abff916f9d83ca6, cbf29ce484222325,
#   448c2ed68ec342ee, 3248ac1bb7011de0, 1a26d24d33b8957b.
#
# 2026-08-31, the generated token carried too, and the host's copy of its
# cache read back once a token rather than once a layer.
#
#   Three commits ago the batched evaluator stopped bringing a layer's
#   answer home between layers and stopped mirroring its keys and values a
#   layer at a time. The single-position evaluator -- which is what
#   generating uses, and which is where the host's share of a token's time
#   is -- never got either. It is the same three things there:
#
#     Carry_In and Carry_Out, so the first layer of a token reads what the
#     host has, the last writes what it read, and the ones between hand the
#     activation on where the device left it.
#
#     Mirror off while chaining, so a layer does not wait on its own fence
#     to copy one position of keys and values back.
#
#     A pass after the loop that reads those positions out of the device's
#     own cache for every layer that deferred them. One position apiece,
#     which is what a token writes, against twenty-two waits.
#
#   Whole_Layer_Fits is asked of the next layer as well as of this one: a
#   layer that falls back to the host reads the host's copy of the
#   activation, and the host's copy is exactly what carrying does not
#   write.
#
#   Alternated, sixty-four generated on the device, medians of three:
#     chained    1.269, 1.273, 1.286 s
#     waiting    1.351, 1.336, 1.369 s
#   Ahead in all three rounds, about five per cent. An earlier sitting on
#   the same pair read 1.249 and 1.272 against 1.316 and 1.331, ahead in
#   five of five. Eight pairs, eight wins.
#
#   What said to look here: a generated token dispatches 18.29 ms of work
#   on the device against llama.cpp's 17.9 -- level, and ahead of it on
#   attention by six times and on the keys and values by two -- while the
#   token takes 20.8 ms of wall. The kernels were not the thing left.
#
#   Five groups moved -- llama.adb reaches engine, reference, device,
#   drafting and outside. One sitting:
#
#     six-token prompt, 12 generated   cpu 0.425 s wall, 0.066 and 0.357,
#                                      1.92 s processor
#                                      device 0.259 s wall, 0.032 and 0.227,
#                                      0.09 s processor
#     the same at --arith f32          1.246 s, 9.54 s processor
#     four tokens, cpu against
#       reference                      cpu 0.065 and 0.117
#                                      reference 5.914 and 3.942
#     110-token prompt                 cpu 0.431 s, 2.62 s processor
#                                      device 0.112 s, 0.01 s processor
#     1419-token prompt                cpu 6.253 s, device 1.047 s
#     64 generated, six-token prompt   cpu 1.928 s, device 1.319 s
#     twelve tokens at two bits        1.899 s (0.369 and 1.529)
#     drafted, four proposed a round   4.051 s, 24 proposed, 7 accepted
#     llama-bench 95b8e33e1, -r 3      pp110 379.67 +/- 3.13 --device none,
#                                      1625.63 +/- 32.72 -ngl 99
#                                      pp1419 271.14 +/- 3.13 --device none,
#                                      1775.71 +/- 9.21 -ngl 99
#                                      tg64 39.72 and 56.48 beside the
#                                      short prompt, 39.07 and 56.06 beside
#                                      the long one
#                                      pp110 -ngl 0 with a device present
#                                      769.64 +/- 10.63
#
#   Digests held: 33f48397f89839f6, 5abff916f9d83ca6, cbf29ce484222325,
#   448c2ed68ec342ee, 3248ac1bb7011de0, 1a26d24d33b8957b, 5f85f06494f88de4,
#   7fbdc947563a2fde -- which is the whole of the check on this change:
#   twenty-two layers that no longer send their keys and values home a
#   layer at a time answer the same tokens.
#
#   Two figures were taken twice and both readings are here rather than the
#   kinder one. llama-bench's pp1419 --device none read 224.85 +/- 20.18 at
#   a load of 2.3 and 271.14 +/- 3.13 when the machine was quiet; the
#   second is published. Sixty-four generated on the device read 1.319 s in
#   the sitting and 1.364 s an hour later on the same binary; the first is
#   published, and the spread between them is as wide as the change the
#   alternated pairs measure, which is why the pairs and not the cell are
#   what the claim rests on.
#
# load:  0.88 to 3.44 across the sitting, each pair adjacent
# model: TinyLlama-1.1B-Chat Q8_0
#
# 2026-08-31, llama.cpp's matrix tile, built here and slower, with its two
# halves separated; and the one part of it that was kept.
#
#   Where the device's prompt stands, profiled on both sides at 1419
#   tokens -- `speed --budget` here, GGML_VK_PERF_LOGGER there:
#
#     matrix products   0.828 s (79.1 %)   against 0.581 s (77 %)  1.42x
#     attention         0.160 s (15.3 %)   against 0.078 s (10 %)  2.06x
#     norm/rope/join    0.024 s            against 0.093 s         ahead
#
#   Eighty-four per cent of the whole gap is the first row. The kernel runs
#   that work at 3.4 teraflops against llama.cpp's 4.9.
#
#   Their tile was read out of ggml-vulkan.cpp rather than derived. For an
#   AMD device with VK_KHR_cooperative_matrix on the open driver the large
#   quantized warptile is {256, 128, 128, 32}: BM 128, BN 128, BK 32, two
#   hundred and fifty-six invocations as four subgroups, with both operands
#   staged in shared memory at a padded stride and every subgroup loading
#   its quarter from there. Ours is 32 by 128 by 32 on one subgroup, with
#   the weights staged and the batch loaded where it lies.
#
#   Built. It answers 1a26d24d33b8957b, which is the published digest, and
#   it is thirty-one per cent slower. So the two halves were separated,
#   because a change that loses says nothing about which of its parts lost.
#
#   Alternated round by round, medians of three, the 1419-token device
#   prompt, three rounds each:
#
#     32 rows, one subgroup, batch in place   1.026, 1.029, 0.990 s
#     the same, batch staged                  1.195, 1.195, 1.196 s
#     128 rows, four subgroups, in place      1.210, 1.170, 1.152 s
#     128 rows, four subgroups, staged        1.375, 1.355, 1.344 s
#
#   Both halves lose alone and the costs compose. Staging the batch costs
#   sixteen per cent with one subgroup, where a barrier is free and cannot
#   be blamed for it. The taller tile costs thirteen with the batch left
#   where it is.
#
#   And neither is an artefact of a cheap implementation. The first staged
#   arm wrote the tile one half-precision value at a time; rewritten to read
#   four at a time out of an f16vec4 alias of the same binding, as llama.cpp
#   reads it, the two staged arms read 1.195 and 1.419-1.445 -- the same and
#   slightly worse. Every arm answered the published digest.
#
#   What the tall tile buys is real and is not enough: a 32-row tile
#   re-reads the batch four times as often as a 128-row one, thirty-two
#   megabytes against eight for a 2048-row layer. Those re-reads are
#   second-level cache hits, and this part would rather serve them than pay
#   a shared-memory round trip and a four-wave barrier for each. That is the
#   third measurement in this file to reach that conclusion from a different
#   direction.
#
#   Kept: the shared weight tile's stride, padded. Sixteen rows are read at
#   once by a sixteen-by-sixteen load and a power-of-two stride lands all
#   sixteen in the same banks. Two sittings, alternated:
#
#     plain, stride 32    1.026, 1.029, 0.990, 1.047, 1.039, 1.052, 1.049 s
#     padded by four      1.023, 0.963, 0.984, 1.032, 1.038, 1.040, 1.034 s
#
#   Ahead in seven of seven pairs, about one per cent. And the pad swept,
#   three rounds each, so that the number is not a guess:
#
#     pad  2   1.088, 1.110, 1.097 s
#     pad  4   1.026, 1.041, 1.039 s
#     pad  8   1.034, 1.051, 1.040 s
#     pad 16   1.041, 1.049, 1.038 s
#
#   Four, eight and sixteen are the same, so what this buys is a stride that
#   is not a power of two rather than any particular bank pattern. Two is
#   worse than none, which no bank arithmetic here predicts and which is
#   recorded as not understood: a stride of sixty-eight bytes is the only
#   one of these that a sixteen-byte load cannot be assembled from, so
#   alignment rather than banking is the likely reason. Four is kept for
#   being as fast as the others and smallest.
#
#   Two groups moved -- the shader digest reaches the device and outside
#   figures through model_runner-shaders.ads and nothing else. One sitting,
#   each half of it starting under a load of 1.15:
#
#     six-token prompt, 12 generated   cpu 0.420 s wall, 0.064 and 0.354,
#                                      1.90 s processor
#                                      device 0.272 s wall, 0.032 and 0.240,
#                                      0.07 s processor
#     110-token prompt                 cpu 0.447 s (0.444), 2.63 s processor
#                                      device 0.112 s (0.110), 0.02 s
#     1419-token prompt                cpu 6.280 s, device 1.026 s
#     64 generated, six-token prompt   cpu 1.993 s, device 1.363 s
#     llama-bench 95b8e33e1, -r 3      pp110 373.77 +/- 3.78 --device none,
#                                      1648.44 +/- 37.11 -ngl 99
#                                      pp1419 268.30 +/- 2.09 --device none,
#                                      1760.74 +/- 14.37 -ngl 99
#                                      tg64 39.57 and 56.78 beside the short
#                                      prompt, 39.01 and 55.70 beside the
#                                      long one
#                                      pp110 -ngl 0 with a device present
#                                      679.89 +/- 23.13
#
#   Digests held: 33f48397f89839f6, 5abff916f9d83ca6, cbf29ce484222325,
#   448c2ed68ec342ee, 3248ac1bb7011de0, 1a26d24d33b8957b.
#
#   The generating cells moved and this shader is not on that path: a
#   product of one vector never reaches the tile, which the engine keeps out
#   below thirty-two. Sixty-four generated read 1.319 s in the sitting
#   before this one and 1.363 here, which is the spread that entry already
#   recorded, and it is the machine rather than the change.
#
# load:  0.63 rising to 2.90, then 1.00 rising to 4.56
# model: TinyLlama-1.1B-Chat Q8_0
#
# 2026-08-31, a prompt reads the weights three times where it read them
# twelve; and two careful changes to the decode that measured nothing, which
# is what found it.
#
#   The entry above leaves the matrix product 1.42 times behind and says the
#   shape is not it. So the decode was taken apart. Replacing it with a
#   constant takes the 1419-token device prompt from 1.04 s to 0.81 --
#   twenty-three per cent -- and the disassembly of the executed Q8_0 path
#   says why it looked like arithmetic:
#
#     32 WMMA, 152 VALU, 39 buffer loads, 16 ds_load_b64,
#     16 ds_store_b16, 2 ds_store_2addr_b64, 41 waits, 28 SALU
#
#   Twenty other instructions for every matrix instruction, and a shared
#   tile written one sixteen-bit value at a time.
#
#   Two changes, both of which do what they say and neither of which is
#   worth anything:
#
#     The tile held four values to an element rather than one, padded the
#     same way, so the decode's sixteen writes become four and the
#     instruction's loads read the same units. Confirmed: 16 ds_store_b16
#     and 6 ds_store_2addr_b64 become 2 ds_store_2addr_b64.
#
#     The block's scale applied to a whole four-value vector instead of
#     value by value. Confirmed: 8 v_pk_mul_f16 appear and the VALU count
#     goes 152 to 144.
#
#   Alternated, three rounds, medians of three:
#     one value at a time   1.025, 1.031, 1.046 s
#     four out              1.030, 1.039, 1.039 s
#     four and packed       1.032, 1.029, 1.036 s
#   Level. Not kept.
#
#   So the twenty-three per cent was split, because taking the decode out
#   had taken the loads that feed it out as well. Keeping the loads and the
#   scale and throwing away only the conversion chain -- wrong answers on
#   purpose, and a probe rather than a candidate:
#
#     the whole decode                        1.042, 1.052 s
#     its arithmetic gone, its loads kept     1.010, 1.012 s
#     both gone                               0.812, 0.807 s
#
#   The arithmetic is three and a half per cent and the weight reads are
#   nineteen. A hundred and twenty instructions of decoding are worth almost
#   nothing, which is why two careful attempts at them were worth nothing.
#
#   And the weights were being read twelve times. A batch is one pass over
#   the model and Max_Batch was a hundred and twenty-eight, so a 1419-token
#   prompt made twelve passes where llama.cpp, whose n_ubatch is five
#   hundred and twelve, makes three.
#
#   Swept, medians of three, the device prompt:
#     128    1.038 s
#     256    0.991 s
#     512    0.987 s
#     1024   1.026 s
#     1419   1.028 s
#     2048   1.028 s
#
#   Alternated, three rounds on the device and two on the processor:
#     device 128   1.027, 1.026, 1.036 s
#     device 512   0.961, 0.995, 0.987 s
#     cpu 128      6.440, 6.336 s
#     cpu 512      6.126, 6.248 s
#   Ahead in all five pairs. Four per cent on the device, three on the
#   processor, and the same tokens both ways.
#
#   Above five hundred and twelve it goes back, which is the part worth
#   keeping: a batch holds the activations of every position in it, and at a
#   thousand and twenty-four those stop fitting in whatever they were
#   fitting in. This is a shape with an optimum rather than a direction, and
#   the optimum is where the other runtime already had it.
#
#   Two faults found on the way, both in the machinery rather than the
#   engine, and both of the kind this file exists for:
#
#     tests speed defaulted --batch-size to a hundred and twenty-eight, a
#     copy of a default the command had just moved to five hundred and
#     twelve. A whole sitting was taken at a batch nobody would ever run
#     before the two numbers were compared. It reads the default out of the
#     request record now. Second time this tool has differed from the
#     command it publishes figures for; the first is recorded under "The
#     device backend".
#
#     Max_Batch lives in model_runner-llama.ads, which no group listed. Two
#     defaults elsewhere moved with it and those were listed, so the check
#     fired -- by luck. That file is in all five groups now, which is why
#     these fingerprints differ from the ones the gate named before the
#     lists were changed.
#
#   Also measured and not kept, from the same sitting: llama.cpp reaches its
#   speed through the cooperative matrix and not through its integer path.
#   pp1419 -ngl 99 reads 1882 t/s by default, 1800 with
#   GGML_VK_DISABLE_INTEGER_DOT_PRODUCT and 1263 with
#   GGML_VK_DISABLE_COOPMAT. Four per cent against thirty-three. A pipeline
#   per format instead of a six-way branch on a push constant is worth 0.4
#   per cent -- 1.031, 1.028 against 1.034, 1.034 -- so the branch is not a
#   cost either.
#
#   Five groups moved. One sitting, each part of it started under a load of
#   1.10:
#
#     six-token prompt, 12 generated   cpu 0.425 s wall, 0.065 and 0.357,
#                                      1.90 s processor
#                                      device 0.264 s wall, 0.031 and 0.233,
#                                      0.07 s processor
#     the same at --arith f32          1.360 s, 10.14 s processor
#     four tokens, cpu against
#       reference                      cpu 0.068 and 0.118
#                                      reference 5.907 and 3.932
#     110-token prompt                 cpu 0.417 s (0.415), 2.62 s processor
#                                      device 0.116 s (0.113), 0.02 s
#     1419-token prompt                cpu 6.106 s, device 0.968 s
#     64 generated, six-token prompt   cpu 1.950 s, device 1.340 s
#     twelve tokens at two bits        1.766 s (0.335 and 1.390)
#     drafted, four proposed a round   3.867 s, 24 proposed, 7 accepted
#     llama-bench 95b8e33e1, -r 3      pp110 353.57 +/- 25.04 --device none,
#                                      1606.37 +/- 55.63 -ngl 99
#                                      pp1419 266.48 +/- 2.08 --device none,
#                                      1764.35 +/- 1.34 -ngl 99
#                                      tg64 39.53 and 56.22 beside the short
#                                      prompt, 39.08 and 55.94 beside the
#                                      long one
#                                      pp110 -ngl 0 with a device present
#                                      827.79 +/- 30.00
#
#   Digests held: 33f48397f89839f6, 5abff916f9d83ca6, cbf29ce484222325,
#   448c2ed68ec342ee, 3248ac1bb7011de0, 1a26d24d33b8957b, 5f85f06494f88de4,
#   7fbdc947563a2fde.
#
#   The device reads the long prompt at 1466 tokens a second and the gap to
#   llama.cpp is 1.20, from 1.27.
#
# load:  0.79 to 2.52 across the sitting, each part started under 1.10
# model: TinyLlama-1.1B-Chat Q8_0
#
# 2026-08-31, a wider tile now that a batch can fill one: measured, refused,
# and the wall behind each of the four directions named.
#
#   The entry above makes a batch five hundred and twelve, so the tile can
#   finally be wider than a hundred and twenty-eight. The argument for it is
#   arithmetic: a workgroup reads TILE_R rows of weights by the whole depth
#   to produce TILE_R by TILE_V answers, so the weight reads per answer fall
#   as the tile widens, and the weight reads are a fifth of a prompt. The
#   sweep that once called tile width neutral was taken when a batch was a
#   hundred and twenty-eight and a wider tile could never be full.
#
#   Two rounds each, medians of three, digests 1a26d24d33b8957b and
#   cbf29ce484222325 throughout:
#
#     TILE_V   1419-token prompt      110-token prompt
#       128    0.954, 0.964 s         0.114, 0.113 s
#       256    1.223, 1.176 s         0.208, 0.209 s
#       512    14.678, 14.420 s       4.713, 4.756 s
#
#   And the part says why, which is the point of taking the shader
#   statistics as well as the time:
#
#     TILE_V 128   168 registers, 0 spilled, 6 subgroups a SIMD
#     TILE_V 256   256 registers, 67 spilled, twelve kilobytes of shared
#                  memory taken to spill into, 4 subgroups a SIMD
#
#   A tile's accumulators are its registers -- TILE_R/16 by TILE_V/16 of
#   them, four apiece -- so sixteen fit and thirty-two do not. At five
#   hundred and twelve the accumulators alone want the whole register file,
#   and fifteen times is what a kernel reads when every one of them is a
#   memory access.
#
#   So the four directions out of this tile have all been measured now, and
#   they are held by different things:
#
#     wider, 256 and 512 vectors        +23 %, +1400 %   the register file
#     taller, 128 rows on four
#       subgroups                       +13 %            the barrier
#     both operands staged in shared
#       memory                          +16 %            the round trip
#     deeper, 64 and 128 columns        slower           measured earlier
#     the shared tile's stride padded   -1 %             kept
#
#   Thirty-two rows by a hundred and twenty-eight vectors on one subgroup is
#   a corner rather than a choice: more answers per workgroup needs either
#   more registers apiece or more subgroups sharing them, and this part
#   refuses both.
#
#   The loads are already as wide as they go, which was the other thing to
#   check before concluding. The executed Q8_0 loop holds thirty-four
#   buffer_load_b128 and three buffer_load_b32, and the three are the block
#   scales -- so the compiler is already merging four_words into one
#   four-word read, and there is no narrow load left to widen.
#
#   Where the prompt stands on the current kernel, two rounds each:
#
#     as it is                                       0.960, 0.960 s
#     the decode and the weight reads feeding it,
#       gone                                         0.740, 0.728 s
#     thirty-one of every thirty-two matrix
#       instructions, gone                           0.813, 0.793 s
#
#   Twenty-four per cent is the weights arriving and becoming
#   half-precision values, sixteen is the multiply, and the shape around
#   both is at a local optimum in every direction anyone has pushed it.
#
#   And where the prompt's time goes, by the engine's own accounting on the
#   same build -- the instrument costs about eighteen per cent, so read the
#   shares and not the seconds:
#
#     PROJECTING    75.6 %
#     ATTENDING     17.0 %
#     READING_OUT    2.7 %
#     NORMALIZING    2.6 %
#     ROTATING       2.0 %
#
#   Nothing kept and nothing restamped: the tree is as commit abe381d left
#   it, and no group's sources changed.
#
# load:  0.82 to 1.6 across the sitting
# model: TinyLlama-1.1B-Chat Q8_0
#
# 2026-08-31, the half-precision batch turned depth-major so that a tile's
# operand would be contiguous: thirty-two per cent slower, and the layout
# was already the one the instruction wants.
#
#   The last lever on the weight-and-activation reads. The instruction's
#   second operand is sixteen depths by sixteen vectors. The batch is
#   vector-major, so reading one is sixteen runs of thirty-two bytes a whole
#   vector apart -- four kilobytes on this model. Kept depth-major, the
#   sixteen vectors a tile wants at one depth are one contiguous run of two
#   hundred and fifty-six bytes.
#
#   Contained: half_batch.comp writes that copy and matrix_product.comp is
#   its only reader, so it is two shaders and one push constant. Count is
#   pushed as the padded width rather than the real one, which the tile
#   never used.
#
#   Taken in two pieces, because a change that loses says nothing about
#   which part lost. The middle arm turns the copy and leaves the reader,
#   which computes the wrong answers on purpose -- cda91dab1bdea4fe rather
#   than 1a26d24d33b8957b -- and times the transpose by itself:
#
#     both as they were             0.968, 0.963 s
#     the copy turned only          1.052, 1.066 s
#     the reader turned with it     1.273, 1.269 s
#
#   The copy is ten per cent. "Once per product" is seven times a layer,
#   twenty-two layers, three batches -- four hundred and sixty-two
#   dispatches over a five-hundred-and-twelve by two-thousand-and-forty-
#   eight activation each -- and turning it round makes its own reads the
#   strided ones. That is the smaller surprise and it was foreseeable.
#
#   The reader is twenty-two, and that is the thing worth writing down. The
#   turned version reads contiguous memory where the original strides four
#   kilobytes, and it is much slower anyway.
#   gl_CooperativeMatrixLayoutColumnMajor is what this instruction's second
#   operand natively is on this part -- element (k, n) with k contiguous is
#   the way the lanes already hold it -- and asking for RowMajor buys a
#   rearrangement in registers that costs more than every byte of locality
#   it wins.
#
#   So the batch's vector-major layout was never an accident to be fixed. It
#   is the layout the instruction reads for free, and a four-kilobyte stride
#   that looks wrong on paper is invisible beside a transpose that is not.
#
#   Nothing kept, nothing restamped: the tree is as commit c1719b3 left it.
#
# load:  under 1.5 throughout
# model: TinyLlama-1.1B-Chat Q8_0
#
# 2026-08-31, attention split four ways as llama.cpp splits it: right,
# faster on every static measure, and fourteen per cent slower.
#
#   Attention is what is left of the device prompt -- seventeen per cent of
#   it and 2.1 times behind, 0.16 s against llama.cpp's 0.078 by their own
#   per-node timings. An earlier entry priced every overhead inside this
#   kernel and found none, so what is left is structural.
#
#   Read out of ggml-vulkan.cpp rather than derived.
#   get_fa_tuning_params_coopmat1 gives this device block_rows 16 and
#   block_cols 16 * 4 -- the same sixteen-by-sixty-four tile this kernel
#   already had -- and row_split 4: four subgroups, two hundred and fifty-six
#   invocations, where this had one subgroup of sixty-four.
#
#   flash_attn_cm1.comp shows the split falling in three different places,
#   and the third is the one that makes it work:
#
#     scoring      sixteen of the tile's sixty-four cached positions
#     the softmax  four of the block's sixteen query rows
#     weighing     sixteen of the head's sixty-four components
#
#   Splitting the weighing by component means a subgroup weighs every
#   cached position of a tile against its own slice of the head, so the
#   slice it accumulates is its own from the first tile to the last and
#   nothing is reduced across the four. Only the rescale crosses, through
#   sixteen floats of shared memory -- carried, beside summed for the
#   division at the end, because the softmax owns rows and the weighing owns
#   components and neither can hand the other a register.
#
#   Built the same way here. It answers 1a26d24d33b8957b, which is the
#   published digest, and it is fourteen per cent slower. Alternated,
#   medians of three, the 1419-token device prompt:
#
#     one subgroup      0.968, 0.951, 0.964 s
#     four subgroups    1.108, 1.099, 1.092 s
#
#   Behind in each of three rounds, and behind having won every static
#   measure the change was supposed to win:
#
#                          one subgroup   four
#     registers                     128     64
#     subgroups a SIMD                8     16
#     code                        17588   6032 bytes
#     matrix instructions a
#       subgroup a tile              32      8
#
#   Half the registers, twice the occupancy, a third of the code, a quarter
#   of the sequential arithmetic. What it buys instead is four real barriers
#   a tile where a workgroup of one subgroup needs none, and on this part
#   that trade loses.
#
#   Occupancy was not the constraint, and this is the third independent
#   measurement to say so: a 128-row product tile over four subgroups cost
#   thirteen per cent, staging both that kernel's operands cost sixteen, and
#   attention over four subgroups costs fourteen. Three kernels, three
#   shapes, one answer -- a workgroup on this part wants to be one subgroup,
#   and what it saves by being one is more than more waves in flight win
#   back.
#
#   One caution about the instrument, which this run exposed. --budget puts
#   ATTENDING at 0.241 s before the change and 0.236 s after -- no change at
#   all -- while the run it is part of is a seventh slower. Its spans are
#   host-side and the device's work runs on past them, so the phase shares
#   say where work is issued and not where the machine spends its time. The
#   end-to-end figure is the one to read, and the shares are for proportions
#   between phases rather than for judging a change.
#
#   Nothing kept, nothing restamped: the tree is as commit 46e4dae left it.
#
# load:  under 1.4 throughout
# model: TinyLlama-1.1B-Chat Q8_0
#
# 2026-08-31, what attention actually costs, measured by removal rather
# than by the instrument that has been reporting it.
#
#   The entry above records a caution: --budget puts attention at 0.241 s
#   before the four-subgroup split and 0.236 after, no change, while the run
#   is a seventh slower. Its spans are host-side. Every attention figure in
#   this file came from it, so the kernel was priced again the way the
#   matrix product was -- by taking pieces out and reading the whole run.
#
#   Two rounds each, medians of three, the 1419-token device prompt:
#
#     nothing removed                          0.957, 0.973 s
#     the scores' matrix instructions          0.959, 0.953 s
#     the weighing's matrix instructions       0.964, 0.970 s
#     the keys, a constant matrix instead      0.953, 0.958 s
#     the values                               0.943, 0.950 s
#     both                                     0.953, 0.954 s
#     the queries staged and nothing after     0.847, 0.840 s
#     the whole kernel                         0.854, 0.859, 0.853 s
#
#   Attention is 0.111 s -- a ninth of a prompt, where the instrument said a
#   sixth. Against llama.cpp's 0.078 s of FLASH_ATTN_EXT nodes that is 1.4
#   times behind rather than 2.1, and everything else in the prompt is 0.854
#   against their 0.726, which is 1.18.
#
#   So the account this file has been giving of where the device prompt's
#   gap lies was skewed: attention is a fifth of what is left rather than a
#   third, and the matrix product is more of it than the ratios said.
#
#   And nothing inside the kernel is individually attributable. Removing
#   either product's matrix instructions changes nothing. Removing the keys,
#   the values, or both changes nothing. Staging the queries is free. Only
#   removing the whole tile loop shows. That is what a loop reads like when
#   its loads, its arithmetic and its shared-memory traffic overlap and none
#   of them is the wall on its own -- the same plateau the four-subgroup
#   split found from the other direction.
#
#   The transferable part is about the instrument, not the kernel. Both
#   numbers are real measurements of different things: --budget times the
#   host's spans and says where work is issued, and removal says what the
#   machine spends. A ratio quoted against another runtime's per-node
#   timings wants the second. The phase shares stay useful for proportions
#   between phases of one build and are not to be used to judge a change --
#   which is exactly how they were used to rank attention as the next thing
#   to do.
#
#   Two probes would not run and are recorded as not run rather than
#   quietly dropped: the softmax with its exponentials replaced by a linear
#   weight, and the tile loop with everything after the scores skipped. Both
#   produce values the run rejects before it publishes anything, which is
#   the speed tool refusing to report a run that did not complete.
#
#   Nothing kept, nothing restamped: the tree is as commit 6625de1 left it.
#
# load:  under 1.4 throughout
# model: TinyLlama-1.1B-Chat Q8_0
#
# 2026-08-31, where a device prompt goes with every kernel taken out in
# turn; and a layer converting four activations where it converted seven.
#
#   The entry above prices attention by removal because the instrument that
#   had been reporting it times host-side spans. So everything was priced
#   that way: each of the eight kernels a prompt runs, voided in turn by an
#   early return at the top of main -- computing nothing and leaving the
#   dispatch, the descriptors and the barriers where they were.
#
#   Two rounds each, medians of three, against a 0.964 s baseline:
#
#     matrix_product     0.323, 0.322 s      costs 0.642 s   67 %
#     attention_matrix   0.845, 0.851 s            0.116 s   12 %
#     combine            0.884, 0.889 s            0.078 s    8 %
#     half_batch         0.888, 0.888 s            0.076 s    8 %
#     norm               0.919, 0.935 s            0.037 s    4 %
#     rotate             0.954, 0.956 s            0.009 s
#     place              0.961, 0.955 s            0.006 s
#     row_product        0.957, 0.966 s            0.002 s
#
#   The eight add to 0.966 s against a prompt of 0.964, which is the check
#   that matters: nothing is unaccounted for, so this is the whole of where
#   a device prompt goes rather than a sample of it. It is also the first
#   account of it in this file that does not come from --budget.
#
#   half_batch is the surprise. It is the smallest kernel in the program --
#   a binary32 activation copied into half precision, two values a word, no
#   arithmetic -- and it costs as much as attention's keys, its values and
#   both its matrix products together.
#
#   It costs that because it ran once for every product. A layer asks for
#   seven and hands them four activations: the query, the key and the value
#   read one normalization, and both feed-forward arms read another. Three
#   of the seven copies were of what the copy before them had just made.
#
#   The engine already knows which earlier step a step reads -- it computes
#   it for the barrier that decides whether a step must wait. A tiled
#   product now remembers what the copy holds and skips the conversion when
#   the product before it converted the same answer at the same width. A
#   step of any other kind clears it, because anything else may have written
#   what the copy holds, so only adjacent products share -- which is exactly
#   the query/key/value and the two feed-forward arms.
#
#   Alternated, medians of three, the 1419-token device prompt:
#     converted once per product      0.944, 0.969, 0.969 s
#     converted once per activation   0.933, 0.921, 0.937 s
#   Ahead in all three rounds, 3.7 per cent, at the same tokens.
#
#   How nearly this was measured wrong, recorded because the failure is not
#   obvious. The first A/B toggled the two arms with a sed whose pattern
#   matched the one arm and not the other -- "Fresh => not Again," became
#   "Fresh => True," and then nothing matched "Again" again -- so every
#   round after the first built the same program. It read 0.960 against
#   0.949 and looked like a small loss, which would have thrown the change
#   away. What caught it was the twelve-token run coming back identical to
#   the thousandth of a second in both arms, which two different binaries do
#   not do. A toggle that edits a file in place has to be able to edit it
#   back; this one now keeps two files and copies whichever it wants.
#
#   What is left is the other four sevenths, and they need the conversion to
#   stop being a kernel: the step that produces an activation could write
#   the half-precision copy beside the binary32 one, which is what place.comp
#   already does for the cache. Then there is no second pass and no dispatch
#   to wait for.
#
#   Two groups moved. One sitting, each part started under a load of 1.10:
#
#     six-token prompt, 12 generated   cpu 0.461 s wall, 0.063 and 0.399,
#                                      2.06 s processor
#                                      device 0.263 s wall, 0.031 and 0.233,
#                                      0.07 s processor
#     110-token prompt                 cpu 0.427 s (0.424), 2.59 s processor
#                                      device 0.106 s (0.103), 0.02 s
#     1419-token prompt                cpu 6.068 s, device 0.946 s
#     64 generated, six-token prompt   cpu 1.929 s, device 1.373 s
#     llama-bench 95b8e33e1, -r 3      pp110 358.56 +/- 29.51 --device none,
#                                      1495.63 +/- 38.24 -ngl 99
#                                      pp1419 269.26 +/- 1.22 --device none,
#                                      1761.66 +/- 1.07 -ngl 99
#                                      tg64 39.39 and 56.39 beside the short
#                                      prompt, 39.09 and 55.99 beside the
#                                      long one
#                                      pp110 -ngl 0 with a device present
#                                      747.03 +/- 24.13
#
#   Digests held: 33f48397f89839f6, 5abff916f9d83ca6, cbf29ce484222325,
#   448c2ed68ec342ee, 3248ac1bb7011de0, 1a26d24d33b8957b.
#
#   The device reads the long prompt at 1500 tokens a second and the gap to
#   llama.cpp is 1.17, from 1.20. The processor's six-token run is high in
#   this sitting -- 0.461 s where the last two read 0.420 and 0.425 -- and
#   it started from a load of 1.07 rising to 1.39, which is the column this
#   file has always said to read with that caveat.
#
# load:  0.72 to 2.49 across the sitting
# model: TinyLlama-1.1B-Chat Q8_0
#
# 2026-08-31, the half-precision conversion fused into the kernel that
# produces the activation: built, right, and level -- with the arithmetic of
# why.
#
#   The entry above leaves half_batch costing 0.047 s, made once an
#   activation rather than once a product. The way to be rid of the rest is
#   to stop it being a kernel: the step that produces an activation writes
#   the half-precision copy beside the binary32 one, which is what place.comp
#   already does for the cache. Two bytes written instead of four read and
#   two written, and no dispatch and no barrier.
#
#   norm.comp makes two of the four activations a layer converts -- the
#   query, key and value read one normalization and both feed-forward arms
#   read another -- so it went first. It writes the copy beside its own
#   answer, zeroes the positions the rounding invented, and the engine skips
#   the conversion for any product reading it.
#
#   Alternated, medians of three, the 1419-token device prompt:
#     the conversion a kernel of its own   0.928, 0.931, 0.943 s
#     left behind by the normalization     0.926, 0.939, 0.916 s
#   Level. Ahead in two of three and behind in one, and the same tokens.
#
#   The diagnostic is what makes this worth writing down rather than just
#   reverting. Voiding half_batch, two rounds each:
#
#     before the normalization wrote the copy   0.047 s
#     after                                     0.011 s
#
#   So the fusing removed thirty-six milliseconds of the conversion's work
#   and the run did not get thirty-six milliseconds faster. The work
#   reappeared in the normalization.
#
#   What a conversion costs is the writing, and the writing costs the same
#   wherever it is done. What fusing saves on top of that -- one read of the
#   activation, one dispatch, one barrier -- is small enough to be swallowed
#   by making a kernel that was already store-bound write two arrays instead
#   of one. The normalization's stores are strided a lane across a
#   two-thousand-wide row and it now does that twice.
#
#   The other two producers -- the combining step for the gated arm and the
#   attention kernel for the blend -- were not built. They can recover at
#   most the 0.011 s that is left and would pay the same kind of extra store
#   to do it.
#
#   One correctness trap, recorded because it was silent on the run that
#   matters and loud on the one that does not. A run whose products all take
#   the row shader has no half-precision buffer at all -- the engine sizes
#   it only when some step uses the tile -- and the binding that would carry
#   it is then the batch's own. The normalization wrote its copy through
#   that binding, and a generated token came back 695d2a94ba6aec9d where it
#   should have been 5abff916f9d83ca6, while the 1419-token prompt's digest
#   stayed right the whole time. A kernel that trusts a binding rather than
#   the caller will do this; the count came from the caller after that, with
#   zero meaning there is nothing to write.
#
#   Nothing kept, nothing restamped: the tree is as commit bc8aab5 left it.
#
# load:  under 1.4 throughout
# model: TinyLlama-1.1B-Chat Q8_0
#
# 2026-08-31, a generated token priced by removal: it is one kernel, and the
# two things that are not it are not what they looked like.
#
#   Every entry above is about a prompt. A generated token was priced the
#   same way -- each kernel voided in turn by an early return, sixty-four
#   tokens, two rounds each:
#
#     nothing voided     1.261, 1.279 s
#     row_product        0.181, 0.181 s      costs 1.089 s   86 %
#     attention          1.270, 1.276 s      nothing
#     combine            1.273, 1.269 s      nothing
#     norm               1.248, 1.249 s      nothing
#     rotate             1.270, 1.275 s      nothing
#     place              1.281, 1.282 s      nothing
#
#   A generated token is the row product and nothing else. Attention, the
#   normalizations, the joins, the rotation and the cache write all measure
#   zero at a batch of one: each is a few thousand operations against a pass
#   over every weight in the model.
#
#   Per token: 19.8 ms, of which 17.0 is the row product and 2.8 is no
#   kernel at all. llama.cpp's whole token on this device is 17.9 ms, so
#   this program's one kernel is about as fast as their entire token, and
#   the 2.8 ms around it is what puts it behind.
#
#   Two things were then tested rather than assumed, and neither is what it
#   looked like.
#
#   The kernel is at the bus and not at its instructions. It moves 1.17
#   gigabytes a token in 17.0 ms, which is 69 GB/s of a part whose peak is
#   about a hundred. A Q8_0 block is thirty-four bytes and a row begins on a
#   word, so half the blocks of every matrix straddle, and the straddling
#   path read both words of every pair -- sixteen reads for what nine words
#   cover, because the second word of one element is the first word of the
#   next. Carrying it forward reads each word once, which is a quarter off
#   every weight read this kernel makes. Bit-exact -- 448c2ed68ec342ee
#   throughout -- and level:
#
#     both words read    1.278, 1.282, 1.288 s
#     one carried        1.276, 1.282, 1.280 s
#
#   Ahead in two of three by three thousandths, which is nothing. A kernel
#   waiting on memory does not care how few instructions ask for it. Not
#   kept.
#
#   And the 2.8 ms is not the descriptors. The host writes four of them a
#   step and nine steps a layer, a hundred and ninety-eight a token, which
#   was the obvious suspect. Writing every one of them twice costs nothing:
#   1.264 s against 1.270. So what is left in the 2.8 ms is the twenty-two
#   submissions a token makes -- one a layer -- and the gap around each.
#
#   One more reading of the instrument, and the sharpest this file has.
#   --budget on a generated token says ATTENDING 75.3 per cent and
#   PROJECTING 18.7, where removal says attention nothing and the row
#   product eighty-six. The single-position path waits for a layer's fence
#   inside its attending span, so the whole layer's time lands there. The
#   instrument is not wrong; it answers where the host waits, and that is
#   where the host waits. It is not an account of where the machine spends,
#   and the entry above this one already says not to read it as one.
#
#   Nothing kept, nothing restamped: the tree is as commit 69cfd1a left it.
#
# load:  under 1.4 throughout
# model: TinyLlama-1.1B-Chat Q8_0
#
# 2026-08-31, the two and a half milliseconds that are not a kernel: not the
# submissions, not the descriptors, and a walk over the vocabulary.
#
#   The entry above leaves 2.8 ms of a 19.8 ms token in no kernel and names
#   the twenty-two submissions a token makes as what is left. That was the
#   wrong suspect. Three probes, no code, and then a change somewhere else.
#
#   Not the submissions. A profile of the run with every kernel voided --
#   nothing left but the host and the queue -- puts a memory copy at the top
#   at 16.5 per cent, and it is the model going to the device once: 199
#   matrices and 1.099 GB, which --show-stats confirms with nothing given
#   back. The first token pays it and the other sixty-three do not. Under it
#   are the wait paths, which is the host correctly idle: with a layer
#   carried, every step is Kept => False and nothing is borrowed, so the
#   only Await in a token is the last layer's.
#
#   Not the descriptors either. The host writes four a step and nine steps a
#   layer, a hundred and ninety-eight a token. Writing every one of them
#   twice costs nothing: 1.264 s against 1.270.
#
#   What the profile names is Sampling.Adjusted, at 6.8 per cent of a run
#   with the kernels gone. Repeat_Penalty defaults to 1.1 over a
#   Repeat_Window of 64, and In_History -- is this token in the window? --
#   walked the ring. It is asked once for every token of the vocabulary at
#   every step, so thirty-two thousand walks of sixty-four entries is two
#   million comparisons a token, between one submission and the next.
#
#   The window is sorted and deduplicated once a token instead, in
#   Record_Token, which is the one place the history changes; In_History is
#   a binary search over it. Six comparisons rather than sixty-four, and an
#   insertion sort of at most sixty-four entries against the two million it
#   removes.
#
#   Alternated, medians of three, sixty-four generated on the device:
#     the window walked    1.279, 1.282, 1.283 s
#     the window sorted    1.239, 1.251, 1.250 s
#   Ahead in all three, two and a half per cent, and bit-identical:
#   448c2ed68ec342ee both ways. The same token by the same arithmetic in a
#   different order of asking.
#
#   On the processor it is level -- 1.910 against 1.915 s -- which is what a
#   fixed cost does against a token three times as long.
#
#   And the check did not fire on it. model_runner-sampling is in no group's
#   source list, so a change that made every generated figure in this file
#   faster passed the gate in silence; the fingerprints below moved only
#   because the list was corrected. That is the second time in three days --
#   model_runner-llama.ads was the first, two entries above -- and both were
#   found by the change being faster rather than by the check. A file the
#   figures depend on that no group names is a figure with nothing watching
#   it. The sampler is in the five groups that publish a generating figure
#   now: engine, reference, device, drafting and outside.
#
#   Five groups moved. One sitting, each part started under a load of 1.10:
#
#     six-token prompt, 12 generated   cpu 0.422 s wall, 0.066 and 0.355,
#                                      1.94 s processor
#                                      device 0.262 s wall, 0.033 and 0.228,
#                                      0.08 s processor
#     the same at --arith f32          1.293 s, 9.80 s processor
#     four tokens, cpu against
#       reference                      cpu 0.055 and 0.118
#                                      reference 5.911 and 3.954
#     110-token prompt                 cpu 0.427 s (0.426), 2.77 s processor
#                                      device 0.108 s (0.107), 0.01 s
#     1419-token prompt                cpu 6.111 s, device 0.925 s
#     64 generated, six-token prompt   cpu 1.897 s, device 1.266 s
#     twelve tokens at two bits        1.704 s (0.323 and 1.373)
#     drafted, four proposed a round   3.888 s, 24 proposed, 7 accepted
#     llama-bench 95b8e33e1, -r 3      pp110 334.94 +/- 21.23 --device none,
#                                      1520.41 +/- 99.54 -ngl 99
#                                      pp1419 267.02 +/- 2.10 --device none,
#                                      1757.83 +/- 3.98 -ngl 99
#                                      tg64 39.41 and 55.70 beside the short
#                                      prompt, 38.76 and 55.76 beside the
#                                      long one
#                                      pp110 -ngl 0 with a device present
#                                      806.76 +/- 59.32
#
#   Digests held: 33f48397f89839f6, 5abff916f9d83ca6, cbf29ce484222325,
#   448c2ed68ec342ee, 3248ac1bb7011de0, 1a26d24d33b8957b, 5f85f06494f88de4,
#   7fbdc947563a2fde.
#
#   The device reads the long prompt at 1534 tokens a second, gap 1.15, and
#   generates at 50.6, gap 1.10 -- the closest either row has been. The
#   110-token processor cell was taken twice: 0.496 s at a load of 1.08 and
#   0.427 s at 0.73, and the second is published, which is the caveat this
#   file has always put on that column.
#
# load:  0.73 to 2.58 across the sitting
# model: TinyLlama-1.1B-Chat Q8_0
#
# 2026-08-31, the binary32 nobody reads: three of the four activations a
# layer converts are no longer written in it.
#
#   The entry two above has the normalization write the half-precision copy
#   beside its own answer, and it is level: the fusing added a store and
#   removed a pass, and on a part where everything is at the bus those
#   cancel. The question was the wrong one. Not "can this kernel write half
#   precision" but "does anything want the binary32", and for three of the
#   four activations a layer converts the answer is nobody:
#
#     the first normalization    read by the query, the key and the value
#     the second                 read by both feed-forward arms
#     the gated middle           read by the projection down
#
#   Every one of those readers is a tile, and a tile's operand is half
#   precision -- it reads the copy and never the original. So the original
#   was written and never read.
#
#   The engine works it out rather than being told. For each step it walks
#   the steps after it; where every step that reads it is a tiled product at
#   this shape and the host keeps none of it, the step writes only the copy.
#   Two bytes a value instead of four, into the place the conversion would
#   have put it, and the conversion does not run. Where any reader is not a
#   tile -- a rotation, a placing, a join, or a product whose rows the tile
#   shape does not take -- nothing changes, which is what the sweep checks
#   across nine architectures and fifteen formats.
#
#   Alternated, medians of three, the 1419-token device prompt:
#     both written      0.938, 0.940, 0.944 s
#     halves only       0.897, 0.905, 0.905 s
#   Ahead in all three, four per cent, at the same tokens. The 110-token
#   prompt goes 0.112 to 0.107.
#
#   What it removes, per layer of a five-hundred-and-twelve batch: the first
#   normalization 2 MB of binary32 write and a 6 MB conversion pass, the
#   second the same, and the gated middle 5.8 MB of write and a 17.3 MB
#   pass -- about forty megabytes a layer, and sixty-six layer-batches in a
#   1419-token prompt.
#
#   Two groups moved. One sitting, each part started under a load of 1.10:
#
#     six-token prompt, 12 generated   cpu 0.420 s wall, 0.063 and 0.353,
#                                      1.88 s processor
#                                      device 0.263 s wall, 0.035 and 0.227,
#                                      0.07 s processor
#     110-token prompt                 cpu 0.426 s (0.424), 2.76 s processor
#                                      device 0.108 s (0.107), 0.01 s
#     1419-token prompt                cpu 5.972 s, device 0.900 s
#     64 generated, six-token prompt   cpu 1.883 s, device 1.266 s
#     llama-bench 95b8e33e1, -r 3      pp110 350.23 +/- 28.97 --device none,
#                                      1651.33 +/- 61.30 -ngl 99
#                                      pp1419 274.09 +/- 6.31 --device none,
#                                      1763.71 +/- 3.54 -ngl 99
#                                      tg64 39.22 and 56.25 beside the short
#                                      prompt, 38.66 and 55.80 beside the
#                                      long one
#                                      pp110 -ngl 0 with a device present
#                                      742.68 +/- 10.97
#
#   Digests held: 33f48397f89839f6, 5abff916f9d83ca6, cbf29ce484222325,
#   448c2ed68ec342ee, 1a26d24d33b8957b.
#
#   The device reads the long prompt at 1577 tokens a second, gap 1.12 --
#   the closest that row has been. The 110-token processor cell was taken
#   twice again, 0.485 s at a load rising to 1.46 and 0.426 s at 1.01, and
#   the second is published.
#
#   What is left of the conversion is the attention blend, read by the
#   projection after it, which could go the same way and is one of four.
#
# load:  0.89 to 2.83 across the sitting
# model: TinyLlama-1.1B-Chat Q8_0
#
# 2026-08-31, the fourth activation, and the conversion kernel that no
# longer runs.
#
#   The entry above takes three of the four activations a layer converts and
#   writes them only in half precision, because nothing reads the binary32.
#   The fourth is the attention blend, read by the projection after it and by
#   nothing else. The same rule, and the engine now applies it -- but only
#   where the tile kernel is the one that will attend, because the scalar
#   attention writes its blend as it always has and the engine already picks
#   between the two by that test.
#
#   Alternated, medians of three, the 1419-token device prompt:
#     the blend written    0.901, 0.904, 0.901 s
#     the blend halved     0.886, 0.900, 0.905 s
#   Ahead in two rounds of three and level. Predicted at rather less than one
#   per cent -- eight megabytes a layer against a prompt's nine hundred
#   milliseconds -- and measured at less than that, which is what a kernel
#   this file has already found to have no attributable piece inside it does
#   with one more store removed.
#
#   Kept anyway, for a reason that is not the time. With all four gone the
#   conversion kernel does not run. Voiding half_batch entirely -- its body
#   an immediate return -- leaves the 1419-token prompt at 0.885 s and the
#   110-token one at 0.105, and both still answer 1a26d24d33b8957b and
#   cbf29ce484222325. A kernel whose absence changes neither the answers nor
#   the time is a kernel nothing is dispatching. It stays in the program: the
#   single-product path still uses it, and so does any sequence whose
#   producer feeds something that is not a tile.
#
#   How the room was found for it, which is worth recording because the
#   obvious way was closed. The attention kernel's push block may not grow:
#   widening it by one field once corrupted this device's answers, which this
#   file records and nobody has explained. So the count of positions the copy
#   is to hold rides in the causal flag's word -- the flag in the low bit and
#   the count above it. One word, no new field, and the shader takes them
#   apart.
#
#   And one thing nearly went wrong. The first reading of this change was
#   taken from a binary that did not contain it. The build helper this work
#   uses redirected its own compiler output to nowhere, so an Ada error -- an
#   arithmetic expression on Interfaces.C.unsigned that needed a conversion
#   -- failed the build silently and the measurement ran the previous
#   executable. It read 0.893 s and every digest held, which is exactly what
#   a correct change looks like. What caught it was a second script that
#   reported the failure instead of hiding it, and then only because the
#   alternation kept failing at the same step. The helper prints its errors
#   now. A measurement harness that cannot fail loudly is a harness that will
#   publish somebody's last build.
#
#   Two groups moved. One sitting, each part started under a load of 1.05:
#
#     six-token prompt, 12 generated   cpu 0.418 s wall, 0.066 and 0.351,
#                                      1.90 s processor
#                                      device 0.259 s wall, 0.032 and 0.228,
#                                      0.06 s processor
#     110-token prompt                 cpu 0.439 s (0.438), 2.83 s processor
#                                      device 0.106 s (0.105), 0.02 s
#     1419-token prompt                cpu 6.064 s, device 0.892 s
#     64 generated, six-token prompt   cpu 1.887 s, device 1.283 s
#     llama-bench 95b8e33e1, -r 3      pp110 343.66 +/- 28.94 --device none,
#                                      1638.72 +/- 37.60 -ngl 99
#                                      pp1419 264.48 +/- 2.73 --device none,
#                                      1759.81 +/- 4.17 -ngl 99
#                                      tg64 39.37 and 56.20 beside the short
#                                      prompt, 39.25 and 55.73 beside the
#                                      long one
#                                      pp110 -ngl 0 with a device present
#                                      819.78 +/- 42.18
#
#   Digests held: 33f48397f89839f6, 5abff916f9d83ca6, cbf29ce484222325,
#   448c2ed68ec342ee, 3248ac1bb7011de0, 1a26d24d33b8957b.
#
#   The device reads the long prompt at 1591 tokens a second, gap 1.11, and
#   generates at 49.9, gap 1.12.
#
# load:  0.65 to 2.90 across the sitting
# model: TinyLlama-1.1B-Chat Q8_0
#
# 2026-08-31, where a device prompt goes with the conversion gone, and the
# normalization's second read of its row, which is free.
#
#   The budget again, each kernel voided in turn, two rounds each against a
#   0.897 s prompt:
#
#     matrix_product     0.264, 0.259 s     costs 0.635 s   70.8 %
#     attention_matrix   0.789, 0.793 s           0.106 s   11.8 %
#     combine            0.830, 0.821 s           0.071 s    7.9 %
#     norm               0.834, 0.857 s           0.051 s    5.7 %
#     rotate             0.879, 0.880 s           0.017 s    1.9 %
#     half_batch         0.899, 0.900 s          -0.003 s
#     place              0.903, 0.898 s          -0.004 s
#
#   The five that cost anything add to 0.880 s of 0.897, and the conversion
#   kernel measures below nothing -- which is what a kernel reads when it is
#   compiled, bound, and never dispatched. That is the entry above,
#   confirmed from the other side.
#
#   Which leaves the normalization at six per cent, walking its row twice:
#   once to sum the squares and once to scale by the root of their mean. A
#   workgroup is two hundred and fifty-six lanes and a row is two thousand
#   and forty-eight values, so sixteen a lane holds the whole row and the
#   second walk need not happen. Held in an array indexed by a literal
#   inside an unrolled loop, which is what keeps it in registers rather than
#   scratch -- the shader report confirms scratch at zero -- and a row too
#   wide to hold keeps what fits and reads the rest twice as before.
#
#   Bit-exact: 1a26d24d33b8957b and 5abff916f9d83ca6 both ways, which is
#   what summing the same values in the same order gives.
#
#   Alternated, medians of three, the 1419-token device prompt:
#     the row read twice   0.883, 0.895, 0.902 s
#     the row kept         0.899, 0.906, 0.885 s
#   Level, and ahead in one round of three. The 110-token prompt reads 0.107
#   against 0.109 the same way round.
#
#   The shader report says why, and it is the second half of the same
#   sentence:
#
#                          registers   subgroups a SIMD
#     the row read twice          24                 32
#     the row kept                48                 20
#
#   A third of the occupancy for a read that was never going to memory. A
#   row is eight kilobytes and the kernel that wrote it ran a moment before,
#   so the second read is a cache hit: what it costs is an instruction, and
#   what holding it costs is twenty-four registers and twelve waves.
#
#   Not kept. This is the fourth measurement in this file to find that
#   occupancy is what this part is short of -- after a hundred-and-
#   twenty-eight-row product tile, its operands staged, and attention split
#   four ways -- and the first of the four where the change removed work
#   rather than adding it. The lesson is not that bigger tiles or more
#   subgroups are wrong; it is that on this part a register is worth more
#   than an instruction, and a cached read costs an instruction.
#
#   Nothing kept, nothing restamped: the tree is as commit 4ac4807 left it.
#
# load:  under 1.3 throughout
# model: TinyLlama-1.1B-Chat Q8_0
#
# 2026-08-31, the processor's worker loop, its two gaps, and the
# normalization's fold: three things that looked like levers and are not.
#
#   A profile of sixty-four tokens generated on the processor:
#
#     model_runner__quantization__integers__deep__rows   73.5 %
#     model_runner__backend__cpu__worker                 12.1 %
#     model_runner__backend__cpu__mat_vec                 2.7 %
#     model_runner__tensors__mat_mul_range_packed         1.8 %
#
#   Annotating the worker puts ninety-five per cent of its samples in four
#   instructions -- a load of a shared counter, a compare, a pause, a
#   decrement -- which is the Spin_Budget loop. Seven cores waiting, a
#   hundred and fifty-four times a token. It reads like pure overhead.
#
#   It is not. The budget swept, medians of three:
#     20_000   1.883, 1.923 s
#      4_000   1.927, 1.925 s
#        400   2.032, 2.057 s
#          0   2.061, 2.088 s
#   Taking the spin out costs nine per cent: the wake it avoids is worth
#   more than the core it burns.
#
#   Nor is the false sharing real, which was the other reading of the
#   annotation. Ticket and Left are adjacent fields of one record, so a
#   worker's decrement of Left as it finishes its share takes the line away
#   from the six still reading Ticket for the next job. Given each its own
#   sixty-four-byte line, aligned:
#     one line   1.907, 1.902, 1.916 s generating; 5.972 s on the prompt
#     apart      1.919, 1.880, 1.911 s generating; 6.085 s on the prompt
#   Level, and slightly worse on the prompt. Not kept.
#
#   What the workers are waiting for is the bus. The worker count swept over
#   sixty-four generated tokens:
#     3   1.875 s      6   1.913 s
#     4   1.911 s      7   1.927 s
#     5   1.916 s      8   1.918 s
#   Flat from three to eight and three the fastest. Generating reads every
#   weight once and does one multiply with each, and three cores of this
#   part saturate whatever carries them; the other four have nothing to do.
#   The spin is what makes having them cheap rather than what makes them
#   expensive.
#
#   The prompt is a different program on the same cores and it scales:
#     1   25.142 s     4   6.841 s
#     2    9.207 s     5   6.350 s
#     3    7.677 s     7   5.944 s
#   A batch shares one reading of the weights between five hundred and
#   twelve tokens, so it is arithmetic where generating is traffic. Four and
#   a quarter times on seven cores, sublinear because it is partly traffic
#   too.
#
#   So the processor's two gaps are two different problems, and this file
#   has been quoting them as one. Generating is 1.16 times behind at 39.7
#   gigabytes a second against llama.cpp's 46, on a wall both are near. The
#   prompt is 1.11 behind with real scaling left in it, and that is where
#   the row kernel is worth attacking.
#
#   And the normalization's six per cent of a device prompt is neither of
#   the two things it looked like. Its second read of the row was refused in
#   the entry above for occupancy. Its fold -- two hundred and fifty-six
#   lanes halved through eight workgroup barriers, which the shader's own
#   comment flags as what a subgroup reduction would replace -- costs
#   nothing: with the halvings removed entirely and a finite constant in
#   their place, the prompt reads 0.892 and 0.903 s against 0.892 and 0.885.
#   What is left in that kernel is the streaming and the dispatch.
#
#   One probe would not run and is recorded as not run: the fold removed
#   without a constant in its place makes a gain that is not finite, and the
#   run refuses to publish rather than reporting nonsense -- which is the
#   check working.
#
#   Nothing kept, nothing restamped: the tree is as commit dee23ec left it.
#
# load:  under 1.4 throughout
# model: TinyLlama-1.1B-Chat Q8_0
#
# 2026-09-01, five hundred and twelve bits on a part that has none; and
# what is left in the two elementwise kernels.
#
#   A profile of the processor reading the 1419-token prompt:
#
#     deep__rows_by_strips         62.3 %
#     kernels__head_scores          6.5 %
#     kernels__blend_run            5.6 %
#     deep__rows                    5.5 %
#     backend__cpu__worker          4.1 %
#     tensors__mat_mul_range_packed 3.4 %
#     quantize_blocks               2.0 %
#     llama__blend_exact            1.9 %
#     kernels__silu                 1.1 %
#
#   Attention is 14.1 per cent of it, which is more than the 1.11 times that
#   prompt is behind, and it is the largest thing on that side that is not
#   the hand-written strip kernel.
#
#   Nothing in this library uses a five-hundred-and-twelve-bit lane. Every
#   hand-written vector kernel is ymm, and the three files model_runner.gpr
#   gives -march=x86-64-v3 and -v4 to are quantization units.
#
#   So the blending run was widened. It weighs a whole head of values
#   against one score -- eight vfmadd231ps a position, which carry
#   sixty-five per cent of that kernel's samples by annotation -- and four
#   in zmm do the same work. Gated on Platform.Byte_Products, which is the
#   same test the v4 compilation uses and means the whole of that set.
#
#   Bit-exact, and not by luck: every component of the sum accumulates the
#   same terms in the same order, and only how many of them a register holds
#   has changed. 1a26d24d33b8957b and 3248ac1bb7011de0 throughout.
#
#   Alternated, medians of three, the 1419-token processor prompt:
#     two hundred and fifty-six bits   6.049, 6.086, 6.199 s
#     five hundred and twelve          6.176, 6.266, 6.166 s
#   Behind in two rounds of three, about one and a half per cent.
#
#   Zen 4 executes a five-hundred-and-twelve-bit instruction on
#   two-hundred-and-fifty-six-bit hardware, two passes to the instruction,
#   so halving the instruction count leaves the cycle count where it was and
#   adds the transitions. Which is exactly why the byte dot product in the
#   quantization units is worth compiling for x86-64-v4 and this is not:
#   that is a new instruction doing four multiplies in a lane, and this is
#   only a wider lane. Not kept.
#
#   head_scores would not have gone the same way even if the lanes had paid.
#   Its annotation is led not by its multiply-adds but by six vhaddps -- the
#   horizontal reduction that ends every dot product -- and a wider lane
#   lengthens that. It would also move every digest this file publishes for
#   the processor, because a dot product's lanes are added to one another at
#   the end and their number decides the association.
#
#   The device's combining step, eight per cent of a device prompt, is
#   entirely traffic. Replacing the gated unit -- sigmoid-weighted or
#   Gaussian on one arm -- with a plain addition:
#     the unit there   0.892, 0.895 s
#     the unit gone    0.890, 0.896 s
#     the whole gone   0.823, 0.823 s
#   The arithmetic is nothing and the kernel is 0.070 s of reading two arms
#   and writing one. The only lever left is fewer bytes: the two
#   feed-forward arms exchanging half precision rather than binary32, about
#   twenty-three megabytes a layer. That changes what the program computes
#   and would be the sweep's to judge rather than a digest's.
#
#   And the processor's two small kernels are at a hundred and twenty-eight
#   bits. quantize_blocks annotates as addps, cmpleps and movups on xmm --
#   which is the SSE2 baseline this library compiles to -- and
#   mat_mul_range_packed moves its tile with movsd and movhpd, sixty-four
#   bits at a time. Neither unit gets wide flags, and that is deliberate:
#   the wide code is isolated into units chosen at run time so that the
#   binary runs on a processor that has none of it. What those two want is
#   the treatment the row kernels already have, a wide variant beside the
#   baseline -- and the measurement above says to widen them to two hundred
#   and fifty-six bits and stop.
#
#   Nothing kept, nothing restamped: the tree is as commit f6c5ae1 left it.
#
# load:  under 1.4 throughout
# model: TinyLlama-1.1B-Chat Q8_0
#
# 2026-09-01, the activation quantizer widened: seven tenths of a per cent,
# and it does not answer the same.
#
#   The entry above finds quantize_blocks running on a hundred and
#   twenty-eight bit lanes -- addps, cmpleps and movups on xmm -- because
#   its unit compiles at the baseline this library targets, while only three
#   files in the project file carry -march=x86-64-v3 or -v4.
#
#   Widened as a probe rather than as a change: the whole unit compiled for
#   x86-64-v3, which is not shippable, because the binary would then fault
#   on a processor without that set and the whole point of isolating the
#   wide code into units chosen at run time is that it does not. The probe
#   answers what the width is worth before four files of plumbing are
#   written to get it safely.
#
#   It is worth seven tenths of one per cent. The wall clock read 5.809 s
#   against about 6.05, which looks like four -- but the processor's prompt
#   column swings by more than that between sittings, and this file has said
#   so for months. The profile is the honest measure:
#
#                          baseline   widened
#     quantize_blocks         2.0 %    1.27 %
#     everything else                 unmoved
#
#   And it does not answer the same. The 1419-token prompt comes back
#   b8887185cdd328a7 where it has always been 1a26d24d33b8957b, and
#   reverting the switch restores it.
#
#   Nothing in that unit reduces across lanes: its only accumulation is an
#   Integer_32 total, which is exact in any order, and Block_Extent takes a
#   maximum, which is associative. So what changed is the rounding.
#   Real'Rounding is ties away from zero; the baseline reaches that by
#   adding and truncating and the vectorised form reaches for a rounding
#   instruction, and a value landing exactly on a half then goes the other
#   way. Three million activations a batch is enough for that to show, and
#   it is a reminder that "no reduction, therefore bit-exact" is not a proof
#   -- it was the reasoning that made this look safe to widen.
#
#   Seven tenths of a per cent, four files of plumbing, and a different
#   answer. Not built.
#
#   Worth setting beside the entry above it, because the two are the same
#   lesson from opposite ends. Widening the blending run was bit-exact and
#   slower. Widening the quantizer is faster and not bit-exact. Neither is
#   what a wider lane was expected to give, and in both the reason is a
#   property of the instruction rather than of the lane: one is
#   double-pumped on this part, and the other rounds differently.
#
#   Nothing kept, nothing restamped: the tree is as commit 6dbbf11 left it.
#
# load:  under 1.4 throughout
# model: TinyLlama-1.1B-Chat Q8_0
#
# 2026-09-01, the tile write-back widened: bit-exact, level, and a strided
# read is why.
#
#   The entry above widens the activation quantizer and finds seven tenths
#   of a per cent and a different answer. The other half of that item is
#   mat_mul_range_packed, three and a half per cent of a processor prompt,
#   whose annotation is movsd and movhpd -- sixty-four bits at a time.
#
#   It narrows a tile of binary64 accumulators into binary32 and lays them
#   out the way the target wants them. Unlike Real'Rounding there is no
#   emulation that can differ: a double narrowed to a float is
#   round-to-nearest-even, which is one instruction at any width. So this
#   one is bit-exact, and the probe confirms it -- 1a26d24d33b8957b
#   throughout, where the quantizer's probe moved to b8887185cdd328a7.
#
#   Alternated, medians of three, the 1419-token processor prompt, the unit
#   compiled for x86-64-v3 as a probe:
#     baseline   5.786, 6.137, 6.220 s
#     wide       6.266, 6.053, 6.218 s
#   Ahead in one round of three. Level.
#
#   The profile says why: the share moves 3.26 per cent to 3.04, which is
#   nothing. The loop writes the target contiguously and reads the tile with
#   a stride of Count, and a wider lane does not help a strided read -- the
#   gather on this part is slow enough that the compiler keeps the paired
#   sixty-four-bit moves whichever instruction set it is given.
#
#   One of the two sides has to be strided. Which one is the subject of "A
#   tile written the way the target is laid out" in the README, where the
#   loops were swapped so that the target is the contiguous side; widening
#   the lanes does not revisit that choice, it only makes the contiguous
#   side wider while the strided side still sets the pace.
#
#   And the reading that started this was noise. The first measurement of
#   the widened unit read 5.648 s against about 6.05 and looked like six and
#   a half per cent; three alternated rounds put it at nothing. The
#   processor's prompt column swings five per cent between sittings, which
#   this file has recorded for months, and this is the third time in this
#   session that a single reading of it has pointed the wrong way -- after
#   the descriptor probe and the first blend-widening reading.
#
#   So both halves of that item are answered: the quantizer is worth seven
#   tenths of a per cent and does not answer the same, and the write-back is
#   bit-exact and worth nothing. Neither is built.
#
#   Nothing kept, nothing restamped: the tree is as commit 4d7ec90 left it.
#
# load:  under 1.4 throughout
# model: TinyLlama-1.1B-Chat Q8_0
#
# 2026-09-01, the feed-forward arms in half precision: worth five and a half
# per cent, and the buffer they need to be correct in costs twenty-two.
#
#   The two arms of a gated feed-forward are read by the step that combines
#   them and by nothing else, and that step reads half precision already. So
#   the products that make them need not write binary32 -- the rule of "the
#   binary32 nobody reads" one step further: a product whose only reader is
#   a combining step that reads halves.
#
#   Built. matrix_product converts its accumulator and stores it where the
#   answer is wanted; combine reads both arms out of the half-precision
#   buffer rather than through bindings nought and one; the engine works out
#   which products qualify by walking the steps after each one, as it
#   already does for the tiles.
#
#   Alternated, medians of three, the 1419-token device prompt:
#     arms in binary32   0.904, 0.896, 0.901 s
#     arms in halves     0.848, 0.853, 0.862 s
#   Five and a half per cent, ahead in all three rounds. The answers change,
#   as they must: the gate's activation is computed from half-precision
#   values, so this is the sweep's to judge rather than a digest's.
#
#   And the answers changed every run. Three runs of one binary gave
#   2b4795a8775ecaa4, 70a2599caf019dd7 and 47d5e948c41ae62a. That is not a
#   wrong answer, it is a race, and it was caught only because the measuring
#   tool prints a digest for every run rather than a pass or a fail. A
#   conformance bound would have accepted all three.
#
#   The race is the layout. The half-precision buffer held one region and
#   everything wrote at the front of it, which was safe while only one
#   answer was ever live at a time. Two arms need two places -- but three
#   answers are live at once, not two: the normalization both arms read is
#   still sitting in the first region when the first arm writes over it, and
#   whether the second arm reads it before or after is timing.
#
#   So three regions. And three regions is where it dies. The buffer goes
#   from eleven and a half megabytes to seventeen, and that alone costs
#   twenty-two per cent. The control is exact: the arms left in binary32,
#   the shaders and the engine as they were, and nothing changed but the
#   bytes asked of Take --
#
#     one region and a half (11.5 MB)   0.901 s, digest 1a26d24d33b8957b
#     three regions          (17.3 MB)  1.102, 1.106 s, same digest
#
#   -- and the same layout at two regions read 0.848 with the arms halved.
#   It is not the regions and not the arms; it is the size of the
#   allocation.
#
#   Why a seventeen-megabyte buffer should cost a fifth of a prompt on a
#   part already holding 1.1 gigabytes of weights is not explained here.
#   Something about where an allocation of that size lands -- a memory type,
#   a page size, a translation structure -- but this measures rather than
#   explains, and it is recorded because it is repeatable and controlled and
#   larger than anything else in this section.
#
#   Five and a half per cent for the arms, twenty-two for the room they need
#   to be correct in. Not kept.
#
#   Nothing kept, nothing restamped: the tree is as commit 09f195f left it.
#
# load:  under 1.4 throughout
# model: TinyLlama-1.1B-Chat Q8_0
#
# 2026-09-01, the feed-forward arms in half precision, kept -- and the
# control in the entry above corrected.
#
#   The entry above records this change as refused, on a control that said
#   the buffer it needs costs twenty-two per cent. That control was wrong.
#   It was two readings at --repeats 1 with no test of what else the machine
#   was doing, and it does not reproduce.
#
#   The allocation swept, five sizes, medians of three, two rounds, with the
#   arms left in binary32 throughout so that nothing but the bytes asked of
#   Take differs:
#
#     one region     0.893, 0.894 s
#     two            0.882, 0.894 s
#     three          0.891, 0.881 s
#     four           0.894, 0.910 s
#     six            0.890, 0.894 s
#
#   Flat. The size of that allocation costs nothing.
#
#   So all four cells were measured together, alternated, each waiting for
#   the machine to fall below 1.05:
#
#     binary32 arms, one region     0.881, 0.891 s   published digests
#     binary32 arms, three regions  0.893, 0.885 s   published digests
#     halved arms, two regions      0.845, 0.847 s   a different answer
#                                                    every run
#     halved arms, three regions    0.851, 0.842 s   published digests
#
#   The three-region form is as fast as the racing two-region one and gives
#   the same tokens. Kept.
#
#   What the two-region form was doing is worth keeping too. Two arms need
#   two places, but three answers are live at once: the normalization both
#   arms read is still sitting in the first region when the first arm writes
#   over it, and whether the second arm reads it before or after is timing.
#   It showed as d2c51a052ab789a2, e7fae034fbfe106e, 2b4795a8775ecaa4,
#   70a2599caf019dd7 and 47d5e948c41ae62a from one binary -- caught only
#   because the tool prints a digest for every run rather than a pass or a
#   fail. A conformance bound would have accepted all five.
#
#   And the kept form gives the published digests, which is not luck and not
#   an accident of rounding either. The digest is of the generated text. The
#   arms crossing in half precision moves the logits in their last bits and
#   moves no argmax, so the text is the same; the sweep is what judges the
#   logits, and it reads 28344 sequences across nine architectures and
#   fifteen formats with none outside tolerance. That is also what says the
#   racing version's digests were a race and not rounding: rounding does not
#   change the text and garbage does.
#
#   Three readings taken at --repeats 1 have now pointed the wrong way in
#   this work -- the descriptor probe, the first blend widening, and this
#   control -- and this one nearly cost a change that is correct and worth
#   four and a half per cent. Alternating the arms, medians of three, and
#   waiting for the machine is not a formality, and a control deserves it as
#   much as a candidate does.
#
#   Two groups moved. One sitting, each part started under a load of 1.05:
#
#     six-token prompt, 12 generated   cpu 0.428 s wall, 0.063 and 0.360,
#                                      1.90 s processor
#                                      device 0.259 s wall, 0.030 and 0.229,
#                                      0.08 s processor
#     110-token prompt                 cpu 0.455 s (0.453), 2.84 s processor
#                                      device 0.103 s (0.102), 0.02 s
#     1419-token prompt                cpu 6.128 s, device 0.835 s
#     64 generated, six-token prompt   cpu 1.911 s, device 1.281 s
#     llama-bench 95b8e33e1, -r 3      pp110 343.15 +/- 34.94 --device none,
#                                      1577.69 +/- 21.54 -ngl 99
#                                      pp1419 265.13 +/- 1.18 --device none,
#                                      1811.02 +/- 10.30 -ngl 99
#                                      tg64 39.51 and 56.13 beside the short
#                                      prompt, 38.66 and 55.84 beside the
#                                      long one
#                                      pp110 -ngl 0 with a device present
#                                      701.40 +/- 38.88
#
#   Digests held: 33f48397f89839f6, 5abff916f9d83ca6, cbf29ce484222325,
#   448c2ed68ec342ee, 3248ac1bb7011de0, 1a26d24d33b8957b.
#
#   The device reads the long prompt at 1699 tokens a second and the gap to
#   llama.cpp is 1.07, from 1.11 -- the closest any row in this file has
#   been.
#
# load:  0.24 to 3.02 across the sitting
# model: TinyLlama-1.1B-Chat Q8_0
#
# 2026-09-01, both of attention's loops moved into the kernel that scores.
#
#   A profile of the long prompt on the processor puts head_scores at 6.7 per
#   cent of it. Forty-six per cent of that kernel's samples were not in the
#   run: ten arguments, six reach comparisons, an index check on each of
#   three arrays every time an address was taken, and a frame -- in front of
#   sixty-four multiply-adds, because the caller walks a block of eight keys
#   and one head at a time and that is one turn of the loop inside.
#
#   Head_Scores_Across takes the head range and the whole key run, proves the
#   reach once, and issues the run in place. The run is a constant both
#   procedures share, so it is the same instructions in the same order, and
#   the scores are bit for bit what they were. Head_Scores still does one
#   head and takes the tail and every host without the lanes.
#
#   Counted rather than timed, two alternated rounds:
#
#     a head at a time    1032457207147 and 1032422518763 instructions
#                          380124653266 and  378714944135 cycles
#     the range at once    991366405630 and  991291133539 instructions
#                          375974170333 and  370745329887 cycles
#
#   Four per cent of the instructions and 1.6 per cent of the cycles, both
#   reproducing to five figures. Nine wall-clock runs alternated over three
#   rounds read 6.057 s against 5.952 -- 1.7 per cent, and inside the spread
#   on its own. The counters are what settle this one.
#
#   Inside the kernel the multiply-adds went from 39 per cent of the samples
#   to 51 and the prologue from 46 to 30. The run is at both walls at once,
#   sixty-four loads and sixty-four multiply-adds to a block on a part that
#   does two of each a cycle, so there is nothing left to trade.
#
#   src/library/model_runner-kernels.ads was in no group's source list, and a
#   change to it would have passed the gate in silence. It is in the five
#   groups that name the body now, which is why their stamps move further
#   than the change alone would move them.
#
#   Five groups moved. Two sittings, and the reason for two is here:
#
#     six-token prompt, 12 generated   cpu 0.414 s wall, 0.060 and 0.352,
#                                      1.84 s processor; loading 0.075 s
#                                      device 0.267 s wall, 0.030 and 0.236,
#                                      0.07 s processor
#     the same at --arith f32          1.382 s, 10.27 s processor
#     110-token prompt                 cpu 0.447 s (0.445), 2.84 s processor
#                                      device 0.105 s (0.104), 0.01 s
#     64 generated, six-token prompt   cpu 1.967 s (1.903), device 1.320 s
#                                      (1.269)
#     seven workers against fifteen    0.414 s and 1.84 s processor against
#                                      0.485 s and 2.74 s
#     eight shares against fifteen     0.414 s and 1.84 s against 0.442 s
#                                      and 2.38 s
#     four tokens, cpu                 0.070 s prompt, 0.117 s generating
#     four tokens, reference           5.934 s prompt, 3.916 s generating
#     drafting                         model 0.430 s, two-bit 1.863 s,
#                                      drafted 3.922 s, 24 proposed 7
#                                      accepted
#     q4_k 110-token prompt            0.611 s; ten generated 0.491 s
#     llama-bench 95b8e33e1, -r 3      pp110 310.33 +/- 25.32 --device none,
#                                      1581.16 +/- 36.48 -ngl 99,
#                                      684.33 +/- 51.81 -ngl 0
#                                      tg64 39.19 +/- 0.29 and 55.80 +/- 0.05
#
#   The two 1419-token rows reached the end of that sitting at a load of
#   three, so both engines were taken again afterwards in one quiet window:
#
#     1419-token prompt, 12 generated  cpu 5.883 s wall, 5.450 evaluating,
#                                      35.81 s processor, load 0.45
#                                      device 1.140 s wall, 0.848 evaluating,
#                                      0.20 s processor, load 0.93
#     llama-bench, same window         pp1419 269.95 +/- 7.53 --device none,
#                                      1892.75 +/- 30.86 -ngl 99
#                                      tg64 39.00 +/- 0.31 and 56.94 +/- 0.11
#
#   Eleven per cent separates that prompt read settled and quiet from the
#   same prompt read at a load of three, and about four separates settled
#   from a part warm from rebuilding. The alternation says which arm is
#   faster; only the settled sitting may be published as a figure.
#
#   Digests held: 33f48397f89839f6, 5abff916f9d83ca6, cbf29ce484222325,
#   448c2ed68ec342ee, 3248ac1bb7011de0, 1a26d24d33b8957b, 5f85f06494f88de4,
#   7fbdc947563a2fde, 4b6e8e99ae285b2a.
#
#   The processor reads the long prompt at 260.4 tokens a second against
#   llama.cpp's 270.0, which is a gap of 1.04 -- the closest either side of
#   this comparison has been at either length.
#
# load:  0.45 to 3.72 across the two sittings
# model: TinyLlama-1.1B-Chat Q8_0, and Q4_K_M and Q2_K beside it
#
# 2026-09-01, four measurements of the generating row kernel, none kept, and
# a diagnosis corrected.
#
#   The list this came from said the generating kernel copies and biases
#   every weight before multiplying it. It does not. The staging copy is in
#   the four-row insertion, and a generated token does not go there: deleting
#   that loop outright moved the instruction count of a sixty-four token run
#   by four hundredths of a per cent -- 81216 against 81181 million -- which
#   is how a path is proved cold. What a token runs is Rows_Singly, and that
#   one already reads the weights where the file holds them, keeps the
#   accumulator in a register from a row's first block to its last, and
#   issues eleven instructions for every thirty-two multiply-adds.
#
#   What the counters do say is that the generating side is not waiting for
#   the bus. Sixty-four tokens: 81.2 billion instructions in 35.5 billion
#   cycles, which is 2.29 a cycle. Reading a gigabyte of weights a token at
#   about 31 GB/s on a part that does far more, at that rate of issue, is a
#   program limited by what it executes rather than by what it fetches. This
#   file has said the other thing for several sittings.
#
#   Four things were then measured. All four are refused.
#
#   The block scale, decoded by the hardware rather than by Ada. Scale_At
#   builds a sixteen-bit number out of two byte loads, a shift and an or
#   before vcvtph2ps can start -- six instructions in a function every block
#   of every row calls. vpinsrw takes the two bytes at the address directly,
#   which is three:
#
#     a head at a time     81220, 81274, 81204 million instructions
#     the pair loaded      74404, 74411, 74396
#
#   Eight and four tenths per cent of every instruction a generated token
#   executes, reproducing to five figures, and the tokens are identical
#   because it is the same convert of the same two bytes. It is not faster.
#   Q8_0 cycles read 35.50 against 34.77 in one sitting and 35.61 against
#   35.85 in the next, which is noise around zero, and Q4_K generation is
#   clearly worse: 9.86 billion cycles against 14.90, and 0.40 s generating
#   against 0.69. The two byte loads issue in parallel with everything
#   around them; vpinsrw is a load and an insert into one register, and the
#   convert waits behind both. Fewer instructions on a longer chain.
#
#   The four kilobytes of `rep stosq` at the top of that kernel. Row_Scale
#   has room for a thousand blocks and is zeroed on entry to set the
#   hundred and seventy-six a row will overwrite; the instruction was among
#   the ten hottest in a profile of a generated token. Alternated, three
#   rounds: 1.893 s generating against 1.870, and Q4_K level. One and two
#   tenths per cent, under the floor.
#
#   The bias correction as an integer rather than a sum. The insertion's
#   unsigned operand is the weight byte with its sign bit flipped, and what
#   that adds comes back out as a running binary64 sum over every block of
#   every row -- seven instructions a block, and removing it outright takes
#   twelve per cent off the whole run. It need not be a sum at all: the
#   correction is an integer smaller than a million, it depends on the
#   activation block and not on the row, and the lane the multiply-add
#   accumulates into can start at it instead of at zero, which is a load
#   where the insertion had a xor.
#
#     base                 81220, 81274, 81204 million instructions
#                          35499, 36155, 35264 million cycles
#     the pair loaded       and the correction folded in
#                          66191, 66226, 66165 million instructions
#                          35432, 35420, 34861 million cycles
#
#   Eighteen and a half per cent of the instructions and nothing at all on
#   the clock -- the cycles read one and nine tenths per cent worse than the
#   pair-loaded arm alone. The xor it replaced is a zeroing idiom, which
#   costs no execution at all, and the load it becomes goes into the chain
#   the multiply-add is already waiting on. It moves the answers as well
#   (3740ed87be385f2d against 3248ac1bb7011de0, and the suite passes at 294
#   of 294): the correction becomes exact where it was a rounded sum. Being
#   more nearly right for nothing is not a reason to change published
#   digests.
#
#   Nothing is kept and no figure moves. What is worth keeping is the shape
#   of the answer: three of the four cut instructions by four to eighteen
#   per cent and none of them cut cycles, on a kernel issuing 2.29
#   instructions a cycle. There is no front-end slack left in it. The next
#   thing to try on this side has to shorten a dependency chain or move
#   fewer bytes, not execute fewer instructions -- and the entry two above
#   this one, which cut four per cent of the instructions and 1.6 per cent
#   of the cycles, is the same lesson read from the other end.
#
# load:  under 1.05 before every reading
# model: TinyLlama-1.1B-Chat Q8_0 and Q4_K_M
#
# 2026-09-01, the block scale in three instructions, in the one kernel that
# can spend them -- and two corrections to the entry above.
#
#   The entry above refused the pair-loading decode outright. That was one
#   refusal too many. It is worth eight and four tenths per cent of every
#   instruction a generated token executes, and where it hurts is Q4_K,
#   which does not share a code path with the kernel that gains. Rows_Singly
#   is Q8_0 and nothing else -- the block width is a literal 34 in its
#   insertion -- so the decode goes there as Scale_Pair_At and the shared
#   Scale_At is left alone.
#
#     two byte loads, a shift, an or   4.594, 4.530, 4.597 s at one worker
#                                      1.891, 1.879, 1.898 s at seven
#     the pair in one instruction      4.410, 4.354, 4.412 s at one worker
#                                      1.864, 1.876, 1.857 s at seven
#
#   Four per cent at one worker and 1.4 at seven, every reading of one arm
#   below every reading of the other, and the same tokens: it is the same
#   convert of the same two bytes. Q4_K reads 0.400 against 0.405, which is
#   the noise, because it never enters this kernel.
#
#   The first correction. The entry above says the generating side is not
#   waiting for the bus, on an instruction-per-cycle figure of 2.29. That
#   figure is an average over cycles that include seven workers spinning in
#   a four-instruction loop waiting for the next job, and that loop issues
#   at nearly four a cycle. Asked by worker count instead:
#
#     one worker      4.575 s generating, 16 GB/s, 3.46 instructions a cycle
#     three workers   1.894 s, 40 GB/s
#     seven workers   1.893 s, 40 GB/s
#
#   Issue-bound on one core and bus-bound on three, with the corner at
#   three -- which is what this file said before that entry and what it says
#   again. It also explains the entry above rather than contradicting it:
#   four changes cut instructions and none cut cycles because at seven
#   workers instructions are free. The one kept here is the same change
#   measured where they are not.
#
#   The second correction, and it is about method. The entry above published
#   the 1419-token processor prompt at 260.4 tokens a second and a gap of
#   1.04, from a retake that read 5.450 s. Every other reading of that same
#   binary that day lies between 5.9 and 6.1: nine alternated readings in
#   the A/B beside it, three in the sitting, and this sitting's own quiet
#   retake at 6.071. The 5.450 was a median of three taken inside one
#   unusually good window, and a median of three is no defence against the
#   window itself. Readings have to be spread across windows. The row now
#   says 233.7 and 1.16, and the rule this file already had -- alternate to
#   say which arm is faster, settle to say what the figure is -- needs a
#   third clause: take the figure in more than one sitting before publishing
#   it.
#
#   Blend_Run, two positions a turn, measured and refused. Its inner loop is
#   eight fused multiply-adds behind a broadcast, two pointer advances, a
#   decrement and a branch; two positions share one set of four and the
#   second advance becomes a lea of twice the stride, which is twenty-two
#   instructions where two turns were twenty-six. Bit for bit the same
#   answer.
#
#     a position a turn    331067, 331087, 331150 million instructions
#                          124327, 125318, 127700 million cycles
#     two a turn           329823, 329857, 329826 million instructions
#                          125319, 124914, 126582 million cycles
#
#   Thirty-eight hundredths of a per cent of the instructions and nothing on
#   the clock. The arithmetic said so first: fifteen per cent of a kernel
#   that is 5.7 per cent of a prompt is under a per cent of the prompt, and
#   this machine gives back more than half of any instruction cut.
#
#   Six groups moved. The sitting, and a quiet window for the long prompt:
#
#     six-token prompt, 12 generated   cpu 0.417 s wall, 0.056 and 0.357,
#                                      1.85 s processor; loading 0.070 s
#                                      device 0.263 s wall, 0.033 and 0.231,
#                                      0.08 s processor
#     the same at --arith f32          1.516 s, 11.13 s processor
#     the same, twelve-token, device   cpu 0.410 s (0.059 and 0.352), 1.83 s
#     110-token prompt                 cpu 0.473 s (0.472), 2.99 s processor
#                                      device 0.107 s (0.105), 0.02 s
#     64 generated, six-token prompt   cpu 1.959 s (1.899), device 1.337 s
#                                      (1.272)
#     seven workers against fifteen    0.417 s and 1.85 s processor against
#                                      0.575 s and 3.03 s
#     eight shares against fifteen     0.417 s and 1.85 s against 0.454 s
#                                      and 2.44 s
#     four tokens, cpu                 0.063 s prompt, 0.123 s generating
#     four tokens, reference           5.991 s prompt, 3.971 s generating
#     drafting                         model 0.421 s, two-bit 1.921 s,
#                                      drafted 3.922 s, 24 proposed 7
#                                      accepted
#     q4_k 110-token prompt            0.568 s; ten generated 0.501 s
#     row dot product, pinned          f32 0.27, q4_0 0.32, bf16 0.33,
#                                      q8_0 0.40, q4_k 0.41, q6_k 0.41,
#                                      q5_k 0.43, q3_k 0.51, q4_1 0.52,
#                                      iq4xs 0.52, q5_0 0.57, f16 0.59,
#                                      q5_1 0.61, iq4nl 0.67, q2_k 0.74
#                                      ns an element
#     shares, pinned, one vector       2406 to 13173 Me/s, seven at 11836
#     shares, pinned, thirty-two       4364 to 22131 Me/s, seven at 22352
#     shares, open, one vector         eight 10748, seven 11931
#     shares, open, thirty-two         eight 19082, seven 20936
#     q4_k against q8_0, pinned        13286 against 13173 with one vector,
#                                      20606 against 22131 batched,
#                                      2475 against 2406 serially
#     reference against cpu, serial    2.41x q8_0, 2.34x q4_k, 2.97x f32
#     llama-bench 95b8e33e1, -r 3      pp110 323.33 +/- 23.96 --device none,
#                                      1580.25 +/- 63.75 -ngl 99,
#                                      697.12 +/- 58.08 -ngl 0
#                                      tg64 39.24 +/- 0.11 and 55.70 +/- 0.27
#
#   The long prompt, in a window gated on a load under 0.6 and a part under
#   58 degrees:
#
#     1419-token prompt, 12 generated  cpu 6.489 s wall, 6.071 evaluating,
#                                      37.00 s processor, load 0.51
#                                      device 1.145 s wall, 0.849 evaluating,
#                                      0.21 s processor, load 0.48
#     llama-bench, same window         pp1419 270.44 +/- 8.76 --device none,
#                                      1848.26 +/- 11.63 -ngl 99
#                                      tg64 39.27 +/- 0.19 and 56.77 +/- 0.19
#
#   Every row of the row-dot table reads five to ten per cent above the
#   printing before it and no decoder changed: the sitting reached the
#   benchmarks after two hours of building and measuring, on a part that had
#   settled but a machine whose load average had not. Read that column
#   against itself.
#
#   Digests held: 33f48397f89839f6, 5abff916f9d83ca6, cbf29ce484222325,
#   448c2ed68ec342ee, 3248ac1bb7011de0, 1a26d24d33b8957b, 5f85f06494f88de4,
#   7fbdc947563a2fde, 4b6e8e99ae285b2a.
#
# load:  0.23 to 4.07 across the sitting, 0.48 to 0.51 in the quiet window
# model: TinyLlama-1.1B-Chat Q8_0, and Q4_K_M and Q2_K beside it
#