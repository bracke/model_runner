--  Root of the model_runner test and tooling crate.
--
--  Everything a developer runs against this repository lives here: the AUnit
--  suites, the synthetic-model fixtures, the malformed-input corpus, the fuzz
--  runner, the repository and dependency checks, and the documentation checks.
--  The production crate depends on none of it.
package Tests is
end Tests;
